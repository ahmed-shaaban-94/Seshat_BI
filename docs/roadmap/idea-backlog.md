# Seshat BI -- Idea Bank

> **This is a future-idea bank, not a roadmap and not a commitment.** Nothing here is
> planned, scheduled, or approved. It is exploratory brainstorming output to browse for
> inspiration. The authoritative roadmap is `docs/roadmap/roadmap.md` (F-numbered features).

_Re-running regenerates this file as a HISTORY-AWARE snapshot: each run still re-reasons every
_idea from scratch, but it now READS the prior bank and Ground's verified ship-status first, so
_an idea that has since SHIPPED is recorded as shipped (with its evidence) and a settled rejection
_is not re-litigated. This is still an idea BANK, not an evolving plan: nothing here is promoted,
_scheduled, or committed. A SHIPPED tag only records that a human already took an equivalent idea
_through the normal feature process -- the engine never moves an idea onto the roadmap itself. The
_verdicts and scores are an automated reviewer's triage opinion. An idea advances only through the
_normal spec/feature process, with a human decision._

_Generated by the `idea-engine` workflow. Generated on 2026-06-29._

> **DEGRADED RUN: adversarial skeptic did not challenge 1 candidate(s) -- those were clamped (killed, demoted from ADOPT) for unproven coverage. Treat this bank as partial.**

**46 ideas scored** (generated across 3 rounds; raw pre-dedupe 55). Verdicts: ADOPT 20, CONSIDER 18, PARK 6, REJECT 2. Horizon: NOW 30, HORIZON 16.

## Reviewer portfolio verdict

> 28 of 36 ideas survived to a non-REJECT verdict, and the panel is broadly aligned: only two ideas (Model-Wide KPI Answerability Rollup, Doc-Count & Single-Source Drift Guard) were rejected outright, both on thin/duplicative value, and four were PARKed (Ambiguity Blast-Radius Index, Net-Sales-Class Caveat Card, Two-Hop Reachability Walker, Two-Hop Contract Linter A2, Python-from-DAX Reflection, Confidence-Stamped Decision Pack). The clear build-now tier is the ADOPT cluster of cheap, test-only or pure-DEFINE-doc artifacts that depend on nothing deferred: Publish Approval Receipt (8/9, unanimous, the single highest-feasibility item), Rule Registry Snapshot Manifest, Severity-Posture Regression Lock, Live-Surface Protocol Conformance Test, Text/JSON Output Equivalence Test, Live-Surface Import Boundary Guard (B3), Stage 7 Answerability Summary, First-Hour Compass onboarding cockpit, the two KPI contract leads (Derivation-Lineage + Per-Contract Ambiguity Decision Ledger, both 8s), plus the repo-hygiene wins that kill verified live defects (Stale-Marker Sweep SC1, Customer Domain KPI Contracts / missing customer.md, Parked-On Map for the F016 bottleneck, IL1 idea-bank-to-roadmap hard link, A3 route-registry reconciler). Start there. The CONSIDER tier is real but second-priority and several items are gated on upstream work -- Invariant-Relationship Contract and its Definition-Schema/L3-hook sibling depend on the derives_from/invariants[] groundwork shipping first, and the Lineage Edge Closure rule rides on the same upstream. Two governance reminders the whole panel enforced: the trend/confidence ideas must stay categorical and observed-only (no statistical baselines, or they cross the settled F5/F6 ML rejection), and the new approval-adjacent rules (PP1, Publish Receipt) must only verify the sign-off slot exists, never populate it. Where the panel was unanimous, treat the verdict as solid; where it split (see dissents), the disagreement is over value/feasibility shading, not eligibility -- every surviving idea passed the eligibility gate.

## Legend

- **Verdict** (reviewer's *triage opinion only* -- not a decision to build) -- ADOPT (worth a closer look first; eligible, consistent, high value) - CONSIDER (interesting; needs a decision or dependency) - PARK (horizon / later) - REJECT (ineligible or conflicts -- kept for the record).
- **Horizon** -- `NOW` (fits the repo today) - `HORIZON` (future vision).
- **Eligibility** -- respects all hard principles, or violates one (named in the rationale).
- **V / F** -- value / feasibility (1-10), reviewer-assigned.
- **SHIPPED / SETTLED** -- a prior idea Memory matched to shipped work or a settled rejection; kept for the record, not an open candidate.

## ADOPT

### KPI Derivation-Lineage Contract (base-vs-derived dependency graph)

`NOW` - **V8 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Pure DEFINE-layer authoring over human-declared edges (net-sales.md already states 'Base for...' in prose); never computes a value, never executes. Eligible: touches only static contract front-matter. No fabricated number - edges are stated relationships. Strong cross-lens convergence. Prerequisite for T6.3. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] No shipped feature encodes a dependency graph; F8 scores per-table presence, F7 routes per-domain questions, neither expresses derives_from edges. Edges are human-declared facts (verified: net-sales.md says 'Base for growth, margin, ATV'), not fabricated. Pure DEFINE-layer, names what it extends. Strong cross-lens convergence. Genuine new artifact, fully consistent with shipped scorecard. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] High value: edges are human-declared facts already in net-sales.md prose ('Base for growth, margin, ATV'), and surfacing them as a graph turns one Net Sales VAT ruling into a visible six-KPI leverage point. Feasibility is genuine and undeferred -- it adds a derives_from field over committed contracts and a rendering doc; needs no live data, no F016, no human ruling to be useful. It is also the prerequisite that unblocks T6.3, so its value compounds. No hidden dependency.

**First step:** Add a derives_from field to the net-sales and one derived (e.g. ATV) contract front-matter, then render references/kpi-derivation-lineage.md from the declared edges.

### Per-Contract Ambiguity Decision Ledger (force A1-A10 to a recorded ruling)

`NOW` - **V8 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Records human rulings as status+evidence over the existing 4-status vocabulary; the agent never self-grants the ruling - an undecided ambiguity is a blocking_reason it cannot clear. Eligible: pure record, no APPROVE self-grant, no execution. Addresses a demonstrated denominator-swing risk (33.55->50.37). The DEFINE half whose CHECK half is AL1. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Records human rulings as blocking_reasons over the shipped 4-status vocabulary; never self-grants (consistent with DEFINE/CHECK/APPROVE separation). Does not restate any shipped feature -- no shipped artifact gates a contract on per-contract ambiguity resolution. Demonstrated stakes (DiscountedTransactionRate 33.55->50.37 denominator swing). Builds ON the shipped contract template. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Strong value anchored to a demonstrated stakes case (DiscountedTransactionRate 33.55%->50.37% on the denominator ruling). Feasibility honest: it records human rulings as blockers over the existing 4-status vocabulary; the agent never grants the ruling, so the 'unmade human ruling' is correctly modeled as a blocker, NOT hidden as feasible. Pure DEFINE authoring over committed contracts. Mild feasibility note: the blocking lifecycle needs the contract-store to honor the block, which is a small docs-convention seam, not deferred infra.

**First step:** Add an ambiguities[] block (id, decision_status, ruling, evidence) to the metric-contract template keyed to kpi-ambiguities.md; treat any undecided headline-moving ambiguity as a non-pass blocker.

### Stage 7 Answerability Summary (executive-readable)

`NOW` - **V7 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: composes only shipped F7+F8 with the no-% discipline; grants no approval (readability layer over the human seam). Fills the verified zero-idea PUBLISH stage for a distinct sponsor/finance audience the engineer-facing pack does not serve. Pure presentation, no execution. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Composes ONLY shipped F7+F8 with the shipped no-% discipline -- explicitly building on shipped artifacts, not restating them. Targets the verified zero-idea Stage 7 PUBLISH and a distinct sponsor/finance audience the engineer-facing handoff pack does not serve. Fully consistent; fills a real empty stage. High feasibility (presentation only). [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Fills the verified zero-idea PUBLISH stage with a distinct sponsor/finance audience the engineer-facing pack does not serve. Feasibility is clean and undeferred: it composes ONLY shipped F7 + F8 + contracts, all committed, with the no-% discipline; no live data, no F016, no ruling hidden. The cheapest-to-build, highest-certainty Stage 7 filler.

**First step:** Add templates/handoff/answerability-summary.md referenced (non-gating) from publish-ready.md, producing answerable-today / blocked-pending / out-of-scope lists from F7+F8.

### Publish Approval Receipt (record-and-STOP token)

`NOW` - **V8 / F9** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] The single most principle-aligned candidate in the set from my standpoint: the agent FILLS everything true but is forbidden to populate the sign-off line, then STOPs - the deliberately un-fillable field IS the never-self-grant seam made concrete. Cannot publish (F016 verified absent). Eligible decisively: no executor, no self-grant, no fabricated confidence. The receipt states F016 doesn't exist, so it cannot fake an authorization. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] The most principle-aligned candidate -- the deliberately un-fillable sign-off line is the cleanest expression of the never-self-grant seam. Cannot publish (F016 verified absent). Differs from shipped F8 (analytical reach) by being the terminal publish-authorization object. No shipped feature provides this. Fills verified empty Stage 7. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] The single most principle-aligned candidate AND among the most feasible: the deliberately un-fillable sign-off line is the cleanest expression of 'agent never self-grants' -- and crucially it makes the F016-absence (verified) HONEST rather than hiding it (the receipt literally states publish cannot proceed). Feasibility is trivial: one template, one note in publish-ready.md, the empty field IS the gate. The 'unmade human ruling' is the deliverable, not a hidden dependency. Highest feasibility certainty in the set.

**First step:** Add templates/handoff/publish-receipt.md whose approvals[] sign-off row is documented as agent-forbidden (Principle-IV seam); add a one-line evidence[] note in publish-ready.md.

### First-Hour Compass / New-Table Author Onboarding Cockpit

`NOW` - **V8 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: read-over-existing-state presenter parsing readiness-status.yaml for one table, cross-walking to the per-stage gate + authoring skill; reads files only, no git mutation, auto-advances nothing. Distinct from static F006 and status-only readiness-viewer (this is stateful you-are-here orientation for the verified-unserved human operator). Strong Creative+Architect convergence; the next_step.py resolver/scaffolder is additive read-only, not a violation. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Distinct from shipped readiness-viewer (status only) and shipped F006 onboarding-checklist (static DoD) -- this is the stateful 'you are here / next artifact / authoring skill' presenter, naming both parents and stating the difference. Read-only, auto-advances nothing (consistent with the shipped readiness engine). Serves the verified unserved new-table author. Strong cross-lens convergence. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] High value: it serves the verified-unserved human operator walking a table through the gates, making the static F006 checklist stateful ('you are at Stage N, next artifact is Y, here is the authoring skill'). Strong cross-lens convergence. Feasibility is genuine and undeferred -- it reads existing readiness-status.yaml + per-stage docs + blocking_reasons[], all committed; no live data, no F016. Mild scope note: the Architect's next_step.py resolver + scaffolder is a real-code add (still stdlib, read-only), so a thin docs-card version is the cheaper first slice.

**First step:** Build a read-only sibling of readiness-viewer that parses readiness-status.yaml for one table and emits a 'you are at Stage N / next artifact Y / authoring skill Z' orientation card.

### Text/JSON Output Equivalence Property Test

`NOW` - **V7 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: test-only over existing pure rules in read-only RuleContexts - no DB, no execution beyond the static gate, zero new rules (33-count untouched). I verified run() iterates inline and run_json() routes through _collect() as genuinely separate paths whose agreement currently rests only on docstring-asserted rule purity; the parity test converts that into a tested invariant. Asserts equality, not a metric. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Test-only, adds zero rules (33-count untouched, consistent with the shipped wiring test). Verified the two output paths run()/run_json() are genuinely separate (docstring-confirmed) so the parity invariant is currently untested. Does not restate shipped B2 -- it hardens B2's dual-path seam. No conflict with any shipped feature. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Verified real: runner.py says it 'iterates inline rather than reusing _collect', so run()/run_json() are genuinely separate and their agreement rests on rule purity asserted only by docstring. The parity test converts that convention into a tested invariant. Feasibility is maximal -- test-only over existing pure rules and tmp-repo fixtures, no DB, no execution, no new rule (count untouched). Nothing deferred; pure system-hardening value.

**First step:** Add tests/unit/test_runner_output_parity.py parsing run() stdout (inverse of _format) into a Counter and comparing to run_json findings + exit code over existing tmp-repo fixtures.

### Rule Registry Snapshot Manifest (golden-file rule inventory)

`NOW` - **V8 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: test + generated artifact from all_rules(), no execution, no new rule (inventories existing rules). The most robust of the three count-drift candidates - removes hand-typed counts entirely via a golden-file equality test, going upstream of the verified-real drift (constitution 26/23 vs glossary 33). No fabricated confidence (exact inventory equality). Consolidate the count problem here. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Verified the count drift is real (constitution carries '26 rules' on 5 lines vs glossary's authoritative 33). Most robust of the three count-drift candidates -- generates the manifest from all_rules() with a golden-file equality test, removing hand-typing entirely. Reuses the shipped registry + EXPECTED_RULE_IDS rather than restating the wiring test; it goes upstream of the symptom. Consolidate the count problem HERE (+ SC1 for general claims), retiring T7.7's count half. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Verified-real defect it fixes: the constitution carries stale '26 rules' on multiple lines (lines 60,78,377,381) while glossary is authoritative at 33, and a doc says '31 static rules' -- the count drift is live. This is the most robust of the three count-drift candidates: it removes hand-typed counts entirely by generating a manifest from all_rules() with a golden-file equality test, going upstream of the symptom. Feasibility maximal -- reuses registry.all_rules() + EXPECTED_RULE_IDS, no execution, no new rule. The realist's pick to OWN the count problem (retire T7.7's count half here).

**Panel dissent:** All three reviewers vote ADOPT, but feasibility spreads two points (realist 9, duplication auditor 7) over how cleanly the golden-file inventory can be frozen against the already-34-rule register; the ADOPT verdict is unanimous.

**First step:** Add a generate-rules-manifest helper writing docs/rules/rules-manifest.json from all_rules(); add tests/unit/test_rules_manifest_snapshot.py asserting live == committed with a regenerate hint; link the manifest from glossary/constitution/roadmap.

### Severity-Posture Regression Lock (golden severity table)

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: test-only over pure rules with planted-violation fixtures, no execution, no new rule. Fills a gap NO other candidate touches - they guard rule EXISTENCE/SHAPE/FORMAT but not gating SEVERITY. A silent ERROR->WARNING downgrade IS a readiness-gate bypass (the exact thing I audit for), so this directly protects the never-bypass principle. Exact severity equality, no fabricated confidence. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Fills a gap NO shipped test or other candidate touches: existing tests guard rule EXISTENCE, not gating SEVERITY. Verified posture is load-bearing and scattered (S6/S8 ERROR, L3 drift=ERROR/escalate=WARNING). A silent ERROR->WARNING is a readiness-gate bypass. Test-only, no rule added (consistent with the shipped wiring test). Not a restatement of any shipped feature. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Fills a gap NO other candidate or lens touches: existing tests guard rule EXISTENCE, route SHAPE, output FORMAT -- but not the gating SEVERITY that decides pass/fail. A silent ERROR->WARNING downgrade is a genuine readiness-gate bypass that every 'a finding fired' test still passes through. High system value. Feasibility clean -- test-only over pure rules with planted fixtures, no execution, no new rule; the only build effort is forcing each rule to fire to read its severity. Nothing deferred.

**First step:** Add tests/unit/test_severity_posture.py + a committed rule_id->severity-class golden table; assert observed (from planted fixtures forcing each rule to fire) == golden; a change requires editing the table in the same PR.

### Live-Surface Protocol Conformance Test (fake QueryRunner)

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: injects a RecordingQueryRunner FAKE implementing the verified Protocol - opens nothing, no real DB, honors never-execute. Proves the documented weakest point of the boundary (validate.py/value_proxy.py keeping driver imports lazy AND routing through the Protocol AND producing blocked-deferred not fake-pass on no rows). Asserts the blocked-deferred path fires - the anti-fabricated-pass guarantee I most want tested. Complements static B3. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Injects a recording fake (verified QueryRunner is a Protocol) opening nothing -- no execution. Proves the runtime contract at the documented weakest point of the shipped never-execute boundary (validate/value-check Protocol-only + blocked-deferred-not-fake-pass). Complements static B3, restates neither it nor shipped B1. Consistent. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Verified: QueryRunner IS a Protocol in validate.py (line 35) and value_proxy.py is real, so a RecordingQueryRunner fake opens nothing -- feasibility is honest (it does NOT secretly need a live DB). It proves the documented weakest point of never-execute: that the live surfaces stay Protocol-only AND produce blocked-deferred (never fake-pass) on no rows. Real runtime-contract value complementing the static B3. The realist note: the no-row->blocked-deferred path is the load-bearing assertion and it is testable today.

**Panel dissent:** Unanimous ADOPT, but feasibility spans two points (realist 8 vs duplication auditor 6) on how much the fake-runner harness leans on the still-deferred live value_proxy path; the verdict itself is not contested.

**First step:** Add tests/unit/test_live_surface_protocol.py with a RecordingQueryRunner injected into check_reconciliation/value-check; assert call-sites stay on Protocol methods and a no-row result yields a blocked-deferred Finding, never pass.

### Assumption Ledger Rule (AL1)

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: pure static text read over the metric-contract YAML semantic-check already parses (B1-safe - never evaluates DAX, never executes); ERRORs only when an unresolved-assumption marker coexists with a measure binding - CHECKs that a human DEFINED, never self-resolves (no APPROVE violation). Materially-new vs A1 (route targets) and B1 (module-scope imports): its target is unresolved-assumption tokens. The CHECK half of T1.2. Adds one EXPECTED_RULE_ID. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] A1/B1-ADJACENT but materially different and it NAMES the parents: A1 targets route targets, B1 targets module-scope imports, AL1 targets unresolved-assumption tokens coexisting with a measure binding. Pure static text over the YAML semantic-check already parses; never evaluates DAX (B1-safe), never self-resolves (CHECK half of T1.2's human DEFINE). Adds one EXPECTED_RULE_ID -- a new gate, NOT a restatement of shipped A1/B1. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Materially different target from shipped A1/B1 (unresolved-assumption tokens coexisting with a measure binding vs route targets / module-scope imports). It is the CHECK half of T1.2 and correctly fails closed until a human resolves -- so the 'unmade ruling' is honestly a blocker, not hidden. Feasibility good: pure static text read over the same YAML semantic.py already parses (verified present), never evaluates DAX (B1-safe). Adds one EXPECTED_RULE_ID. Best paired with T1.2 shipping the ledger it checks.

**First step:** Add rules/assumptions.py reading the same contract YAML semantic-check parses; emit ERROR when an unresolved-assumption marker coexists with a measure binding; add the id to EXPECTED_RULE_IDS + wiring test.

### Coverage Scorecard Linter

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: pure static parse of filled scorecard instances; the no-percentage check is the no-fabricated-confidence principle made fail-closed (the template literally says 'if tempted to compute a percentage, stop' - hard rule #9). Materially-new vs shipped F8 (F8 created the TEMPLATE; this enforces its laws on filled instances - status-enum, blocker-presence, contract-path-resolves, no-%). No execution. Adds one EXPECTED_RULE_ID. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Explicitly an F8-adjacent EXTENSION (named, not re-proposed): shipped F8 created the scorecard TEMPLATE; this enforces the template's own laws on FILLED instances (verified the template says 'If you are tempted to compute a percentage, stop' + cites hard rule #9). Makes that self-discipline a fail-closed check (status-enum, blocker-presence, contract-path-resolves, no-%). Materially-new CHECK over shipped DEFINE; one new EXPECTED_RULE_ID. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Materially-new enforcement over shipped F8: F8 created the TEMPLATE; this enforces its own laws on FILLED instances (status-enum, blocker-presence, contract-path-resolves, no-percentage). The template literally says 'If you are tempted to compute a percentage, stop' and cites hard rule #9 -- so the linter makes a stated self-discipline fail-closed, real governance value. Feasibility clean: pure static parse, no execution, one EXPECTED_RULE_ID. Must state how it differs from F8 (enforcement-of-instances, not template-creation) -- it does.

**First step:** Add rules/scorecard.py detecting *-coverage-scorecard.md files, parsing the status table and asserting status-enum + blocker-presence + contract-path-resolves + no-percentage; register + wire.

### Live-Surface Import Boundary Guard (B3)

`NOW` - **V7 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: reuses B1's exact stdlib-ast parse-not-import mechanism (the guard never executes). Materially-new vs shipped B1: B1 FORBIDS any module-scope connection import in the static core; B3 PERMITS the import but REQUIRES it be lazy in the live surfaces (validate.py/value_proxy.py/semantic.py/dax_gen.py) - inverted policy on a different module set. I verified validate.py documents the lazy discipline as POLICY ('built lazily in the CLI handler, never here'), not a structural guarantee; B3 makes it structural at the verified weakest point. Adds one EXPECTED_RULE_ID. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] B1-ADJACENT but inverted policy on a different module set, and it NAMES B1: shipped B1 forbids ANY module-scope connection import in the static core; B3 PERMITS the import but requires it be LAZY in the live surfaces (validate/value_proxy/semantic/dax_gen). Verified validate.py documents lazy-import as POLICY not structural guarantee -- B3 makes it structural at the weakest point. Reuses B1's AST helper; one new EXPECTED_RULE_ID. Materially-new sibling, not a restatement. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Materially-new sibling of shipped B1 with INVERTED policy: B1 forbids ANY module-scope connection import in the static core; B3 PERMITS the import but requires it be LAZY in the live surfaces (validate.py et al.). Verified value: validate.py documents the lazy-driver discipline as POLICY ('built lazily in the CLI handler, never here'), not a structural guarantee -- B3 makes it structural at the verified weakest never-execute point. Feasibility clean: reuses B1's module_scope_violations AST helper, parse-not-import, no execution. Adds one EXPECTED_RULE_ID. Must state how it differs from B1 -- it does (different module set, inverted policy).

**First step:** Add a rule reusing never_execute.module_scope_violations over a _LIVE_SURFACE set; ERROR on a forbidden root at module scope in those files (laziness required, dependency not banned); add to EXPECTED_RULE_IDS.

### Publish-pack completeness gate (PP1)

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible - and I scrutinized the self-grant risk hardest here: PP1 verifies the approval-evidence SLOT exists and is non-placeholder (G6-style filled-vs-placeholder check) WITHOUT granting it - the approval stays the human seam. Pure static structural check keyed off template structure, never a table's columns (generic, not C086). Fills the verified zero-gate-code Stage 7. Composes with the Publish Receipt (PP1 could check its presence). Adds one EXPECTED_RULE_ID. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Verifies the approval-evidence SLOT is present and non-placeholder (G6-style filled-vs-placeholder, reusing the verified g6.py mechanism) WITHOUT self-granting -- approval stays the human seam. Fills the verified zero-gate-code Stage 7. Keys off template structure (generic). Composes with the Publish Receipt. Not a restatement of any shipped rule; one new EXPECTED_RULE_ID. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Fills verified zero-gate-code Stage 7 and respects DEFINE/CHECK/APPROVE: it checks the approval-evidence SLOT is present and non-placeholder (G6-style filled-vs-placeholder, verified g6.py mechanism) WITHOUT self-granting -- so the 'unmade approval' stays an honest human seam. Feasibility good: keys off template structure, no execution, one EXPECTED_RULE_ID. Value moderate and overlaps the Receipt (PP1 could check the receipt's presence) -- sequence behind the Receipt so PP1 has a concrete slot to verify.

**Panel dissent:** 2 reviewers vote ADOPT (7/6--7/7); the value/feasibility realist dissents down to CONSIDER (6/7), treating it as solid but a notch below the must-build tier. The principle auditor stresses PP1 must only verify the sign-off slot exists, never populate it.

**First step:** Add rules/publish_pack.py (PP1) keyed on presence of mappings/<table>/handoff/, checking filled-vs-placeholder sections (dictionary, caveats, carried contracts, reconciliation, approval slot present-not-granted); register + wire.

### Route-Registry Coverage Reconciler (A3) - map rows == routes.yaml ids

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: static text parse of two tracked docs, no execution; reports missing/extra ids, never a score (no fabricated confidence). Materially-new vs shipped A1 (which I confirm only validates manifest targets against the filesystem and never reads the knowledge-map). routes.yaml is hand-mirrored from the map with nothing checking completeness - a bijection check closes a real, currently-unguarded drift class. Highest-value A-family member. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Verified A1's routes.py reads ONLY routes.yaml and NEVER the knowledge-map -- so the hand-mirrored map<->manifest boundary is genuinely unguarded. A3 closes that drift class with a bijection check; materially distinct from shipped A1, names it, highest-value A-family member per the skeptic. One new EXPECTED_RULE_ID. Not a restatement. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Highest-value A-family member: routes.yaml is hand-mirrored from knowledge-map ('mirror a row') with nothing checking the mirror is complete, and A1 verifiably never reads the knowledge-map (it validates manifest targets against the filesystem only). A bijection check closes a real, currently-unguarded drift class at the map<->manifest boundary -- genuine system value. Feasibility good: static text parse of two tracked docs, one EXPECTED_RULE_ID. Materially different from A1 (which never reads the map) -- stated.

**First step:** Add rule A3 with a small markdown-table extractor for the knowledge-map routing section (id column) compared as a set against routes.yaml ids; ERROR on asymmetric difference; add A3 to EXPECTED_RULE_IDS.

### Stale-Marker Sweep / Status-Claim Reconciler (SC1)

`NOW` - **V7 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: A1's status-vs-file-resolution mechanism lifted to prose claims, fail-closed and read-only, no execution; manifest-driven and evidence-pinned (no fabricated drift). Materially-new vs A1 (guards prose status claims, not routing targets). Verified-needed: capability-state calls the net-sales trace 'planned' when filled, constitution carries stale claims. Scope the count check OUT (delegate to T5.5) and keep SC1 for the general prose-claim class. Adds one EXPECTED_RULE_ID. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Lifts shipped A1's built/planned resolver onto prose status claims -- names A1, materially different target (prose claims vs routing targets). Verified-needed: capability-state calls the filled net-sales trace 'planned' and the constitution carries stale claims. The count facet is correctly delegated OUT to T5.5; SC1 keeps the broader general prose-claim class. Not a restatement; one new EXPECTED_RULE_ID. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Verified-real defect class broader than count alone: capability-state calls the net-sales trace 'planned' when it is filled (10514 bytes, verified) and the constitution carries stale claims -- SC1's general prose-claim guard ('(planned)'/'NOT BUILT'/file-exists vs filesystem) catches all of these. Feasibility caveat: the prose-scan/manifest must be precise to avoid false positives (justifying a manifest-driven, evidence-pinned design), and the COUNT facet should be delegated to T5.5 (the more robust fix) -- keep SC1 for the general class only. Value genuine; lifts A1's resolver onto prose claims.

**First step:** Add docs/quality/status-claims.yaml (doc, anchor, claimed-artifact, claimed-status) + rules/status_claims.py (SC1) resolving each against tracked_files; ERROR on contradiction; seed with the two known stale-doc defects; add to EXPECTED_RULE_IDS.

### Parked-on dependency map / Parked-On Map (F016 bottleneck edge)

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible - I checked the YAGNI risk: it adds only the SEAM (the parked_on edge), NOT F016 machinery (verified no F016/execution code), matching the repo's 'add the seam, not the implementation' rule. The DF1 rule fails loud on a nonexistent blocker or a parked-but-shipped contradiction - static, fail-closed, read-only, no execution. F016 is a verified-real shared blocker (appears in 5 roadmap places, gates pbi-tools/L3-ops/F031-33). No fabricated confidence (each edge cites a deferred-spec file). Adds one EXPECTED_RULE_ID. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Adds only the dependency SEAM (the edge), not F016 itself (verified no F016/execution code) -- consistent with the repo's shipped 'seam-not-implementation' rule. F016 is a verified real shared blocker (appears in 5 roadmap places, gates pbi-tools/L3-ops/F031-33, all DEFERRED in ship-status). Static fail-closed rule on a nonexistent-blocker or parked-but-shipped contradiction. Not a restatement; reviewer picks rule-vs-diagram depth. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Directly addresses the realist's own core concern: F016 is verified absent from src/ yet is the hidden shared blocker behind pbi-tools extract, L3 new ops, F010 CHECK half, and F031-F033 -- today scattered as independent deferrals in prose (F016 appears in ~5 roadmap places). Drawing it as one explicit, machine-checkable edge makes the latent bottleneck queryable and prevents a 'parked-but-shipped' contradiction. Feasibility honest: it adds only the SEAM (deferrals.yaml + a fail-closed rule), NOT F016 itself, matching 'add the seam, not the implementation.' Genuine system value; adds one EXPECTED_RULE_ID for the rule variant.

**First step:** Add docs/roadmap/deferrals.yaml (item -> parked_on -> blocker-status) + rules/deferrals.py (DF1) failing on a nonexistent blocker or parked-but-shipped contradiction; register + wire. (Lighter Creative variant: ship the read-only diagram only.)

### Idea-Bank Memory Seam / Shipped-row->roadmap-F-row hard link (IL1)

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible - I checked the auto-promotion risk: the link is evidence-OF-shipped (idea-id -> PR/SHA/F-row), NEVER auto-promotion; the human still rules every promotion, so it respects 'output is an idea bank, not a roadmap'. Fixes the verified self-referential gap (the idea-engine regenerates with no memory, so shipped A1/B2/B1/F7/F8 could be re-pitched) - it mirrors THIS task's KNOWN-HISTORY guard as a persistent on-disk seam. Pure JS workflow edit + yaml, no executor, no DB. The optional IL1 rule fails closed on a SHIPPED row lacking evidence (one EXPECTED_RULE_ID). [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Directly the auditor's-eye candidate: fixes the verified self-referential orphaning loop -- the idea-engine regenerates the backlog with no memory, so the SHIPPED A1/B2/B1/F7/F8 rows could be re-pitched next run. Mirrors THIS task's KNOWN-HISTORY guard as a persistent on-disk shipped-ledger (idea-id -> PR/SHA/F-row). Evidence-of-shipped, never auto-promotes (consistent with 'idea bank, not a roadmap'). Also surfaces the three idea-engine workflows verified to sit in NO F-row. Optional IL1 rule fails closed on a SHIPPED row lacking evidence. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Fixes a verified self-referential gap: the idea-engine regenerates the backlog with no memory, so the shipped A1/B2/B1/F7/F8 rows could be re-pitched/orphaned next run -- this very review's KNOWN-HISTORY guard made persistent on disk. Value is real and system-level (prevents wasted re-litigation). Feasibility honest: a read-only ledger (idea-id -> PR/SHA/F-row) + a workflow read step, no executor, no DB; crucially the link is evidence-OF-shipped, NOT auto-promotion -- so it respects 'output is an idea bank, not a roadmap' and does not smuggle in a promotion. Optional IL1 rule adds one EXPECTED_RULE_ID.

**Panel dissent:** Unanimous ADOPT, but the duplication auditor scores value a point higher (8 vs 7), rating the orphaned-workflow fix (idea-engine/idea-to-spec/implement sit in no roadmap F-row) as especially load-bearing; no disagreement on the verdict.

**First step:** Add docs/roadmap/shipped-ideas.yaml (idea-id -> {pr_sha, f_row|none}) consumed by the idea-engine's Explore/Memory stage and passed to the lenses as known-history; human still rules promotion. Add IL1 rule separately if the rule budget allows.

### Customer Domain KPI Contracts + missing domains/customer.md

`NOW` - **V7 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: pure generic KPI business-meaning content (decision-questions, PII rulings) mirroring the 11 existing domain files - no table specifics (not C086), no executor, no approval. Carries the Principle-V PII stop-and-ask verbatim. Closes a verified dangling route (domains/ has 11 files, none named customer.md, while INDEX lists a customer row). The layer-CONTENT work every gate-focused candidate avoided; complements A4 (which would gate it). [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Fills a VERIFIED dangling route (11 domain files, no customer.md, INDEX lists a customer row). Authors generic KPI business-meaning content mirroring the 11 shipped domain files; carries the Principle-V PII stop-and-ask. The layer-CONTENT work every gate-focused candidate avoided -- complements A4 (which would gate it). Not a restatement of any shipped feature; extends the shipped retail-kpi domains set by one missing member. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Fills a verified dangling route: the INDEX lists a customer domain row but domains/customer.md does not exist (verified -- 11 domain files, none named customer.md). This is the layer-CONTENT work every gate-focused candidate avoided, and it has the highest feasibility certainty of the content ideas: author one generic markdown file mirroring the 11 existing domain files, carrying the Principle-V PII stop-and-ask. No code, no live data, no F016, no deferred dependency. Complements A4 (which would gate it). Pure, immediate value.

**First step:** Author skills/retail-kpi-knowledge/domains/customer.md mirroring the 11 existing domain files (decision-questions + customer KPI list + PII/identity owner-ruling triggers + data dependencies); update the INDEX route to resolve.

### DQ-Signal Interpretation Note (-1 unknown-member counts as business caveat)

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: composes ONLY the -1 unknown-member counts the reconciliation-report ALREADY recorded (validate.py's orphan-FK/unknown-member checks via run_live_checks), mapping each recorded signal to the KPIs it distorts and the DIRECTION of distortion in plain language - no new number, no executor (it does not run the query, it reads recorded output). Fills the verified zero-idea Stage 4 GOLD + stakeholder-communication gap. Distinct authoring artifact. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Composes ONLY the -1 counts the shipped validators already record (verified validate.py surfaces orphan-FK/unknown-member checks) -- no new number, no executor. Fills the verified zero-idea Stage 4 GOLD + stakeholder-communication gap. Distinct authoring artifact, restates nothing shipped; builds on the recorded reconciliation-report output. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Genuine stakeholder-communication value: maps each recorded -1 unknown-member count to the KPIs it distorts and the direction of distortion, filling the verified zero-idea Stage 4 GOLD. Composes only already-recorded evidence, invents no number. Feasibility honesty flag the realist owns: it depends on the -1 counts existing in a reconciliation-report, which only appear AFTER a live validate run -- for a table with no live run yet the note has no content. Compliant and buildable as a template, but its filled value is gated on the deferred live validation. CONSIDER as a template now, value realized later.

**Panel dissent:** 2 reviewers vote ADOPT (7/7); the value/feasibility realist dissents down to CONSIDER (6/6), seeing the -1 unknown-member caveat as useful but not top-tier. No eligibility concern.

**First step:** Add templates/handoff/dq-signal-interpretation.md the analyst fills after a live validate run: read the reconciliation-report's DQ-signal rows -> 'signal -> affected KPI -> direction caveat' table; feed Stage 7 caveats.

### Live-validation evidence recorder (validate.py Findings -> readiness-status block)

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: a pure SERIALIZER mapping the Finding[] from run_live_checks (verified to return structured Findings over the QueryRunner Protocol) + run mode into the existing readiness-status.yaml schema - no new validator, no execution beyond validate's existing lazy-driver path. Emits status:blocked with [PENDING LIVE PROFILE] when no DSN, NEVER a fabricated pass - it mechanically enforces blocked-deferred-not-fake-pass, the anti-fabricated-confidence guarantee I most value. Closes the verified Stage 4 gap. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Pure serializer mapping the shipped validate.py Finding[] (verified structured over the QueryRunner Protocol) + run mode into the existing readiness-status.yaml schema -- no-DSN emits blocked-deferred, never a fabricated pass (consistent with 'a pass without evidence is a defect'). Closes the verified Stage 4 gap (validators produce raw findings with no honest path into the readiness file). No new validator/execution; restates nothing shipped. Pairs with T5.7. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Verified: validate.py returns structured Findings over the QueryRunner Protocol, so this is a pure serializer mapping Finding[] + run mode into the existing readiness-status.yaml schema -- and crucially it emits status:blocked with [PENDING LIVE PROFILE] when no DSN, NEVER a fabricated pass. That is the honest-feasibility design: it does NOT pretend a live result exists; it records the blocked-deferred state. Genuine plumbing value closing the verified Stage 4 gap (validators produce raw findings with no path into the readiness file). No new validator, no execution beyond the existing lazy-driver path. Pairs with T5.7.

**First step:** Add src/retail/validate_record.py mapping Finding[] + mode into a gold_ready dict matching readiness-status.yaml; call from the validate CLI handler after run_live_checks; no-DSN -> blocked with a PENDING-LIVE-PROFILE blocker.

## CONSIDER

### Stage-Gated Answerability Snapshot

`HORIZON` - **V7 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: read-only reporter over committed contracts + stage docs + scorecards; explicitly never auto-advances a gate (I confirm it does not touch the readiness engine). Adds the materially-new readiness-STAGE axis (definable-but-not-yet-computable) the flat rollup cannot express - the data-owner's real decision context at each human seam. Strongest answerability variant. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Adds the materially-new readiness-STAGE axis (definable-vs-computable) on top of shipped F7+F8 -- a genuine extension that names its parents, read-only, never auto-advances a gate (consistent with the shipped readiness engine). Strongest answerability variant per the skeptic. No conflict with shipped work; CONSIDER rather than ADOPT because it should be picked over the two weaker answerability siblings as one workstream. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Genuine new value: the readiness-STAGE axis expresses 'definable but not yet computable' -- the data owner's real decision context that the flat rollup cannot. Feasibility caveat the realist must flag: a KPI is 'computable' only once its silver/gold fields reach a stage, which depends on live profiling that does not exist for a fresh table -- so for an unmapped table every line reads 'blocked-on-stage-N'. That is honest (named blocker) but means near-term output is sparse, justifying HORIZON. Read-only, never advances a gate.

**First step:** Draft templates/stage-gated-answerability-snapshot.md crossing one contract's Required-fields confidence with the stage its fields have reached; render status + named blocking stage.

### Semi-Additive Grain Guardrail Index

`HORIZON` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: DEFINE-only matrix sourced from each contract's additivity field + kpi-additivity-and-grain.md; no executor, no fabricated confidence. Consolidates scattered prose into a cross-KPI matrix that pre-empts a real, invisible DAX aggregation footgun (semi-additive snapshots / non-additive ratios); bridges retail-kpi -> bi-dax. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Consolidates additivity prose already in each contract into a cross-KPI matrix no shipped feature provides; bridges shipped retail-kpi to shipped bi-dax. Not a restatement -- it is a new scannable index over existing per-contract fields. Pre-empts a real DAX footgun. Consistent with shipped layers. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Highest genuine analytical value in the set: wrong aggregation of a semi-additive snapshot or non-additive ratio is a high-frequency, invisible retail BI error, and no consolidated cross-KPI matrix exists today. It bridges retail-kpi to bi-dax (the layer that authors the recompute-at-grain measure) -- real system value, not a convenience view. Feasibility is excellent: pure DEFINE derivation over each contract's additivity field, nothing deferred. HORIZON is conservative; value is NOW-grade.

**Panel dissent:** 2 reviewers vote CONSIDER (7/7); the value/feasibility realist dissents upward to ADOPT (8/8), rating the additivity-matrix's pre-emption of a real DAX footgun as genuine system value rather than a second-tier nice-to-have.

**First step:** Build references/additivity-by-dimension.md (KPI x date/branch/product/customer -> additive/semi-additive/non-additive + safe-aggregation note) from existing contract additivity fields; route bi-dax to it.

### Dimension Coverage Matrix (sliceable-by today)

`HORIZON` - **V6 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: read-only derivation crossing each contract's Recommended-dimensions against mapping source keys - never executes a GROUP BY, never invents a dimension (a dimension is available only when a mapping artifact names its key; empty cells are honest blockers). Orthogonal slicing axis distinct from F8 presence. HORIZON correct given mapping-maturity dependence. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Distinct slicing axis orthogonal to shipped F8's presence axis -- not a restatement. Read-only derivation; empty cells are honest named blockers (consistent with no-fabricated-confidence). Near-term value gated on mapping maturity, hence HORIZON, but no conflict with shipped work. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Distinct orthogonal value (slicing axis vs F8's presence axis). But the realist must flag the feasibility honesty gap: 'available' requires mapping artifacts naming dimension keys, which for a fresh/unmapped table do not exist -- so for most tables every cell is 'blocked-missing-key' and the artifact is near-empty until mapping matures. The empty state is honest, but the useful output is gated on mapping maturity (a deferred-per-table input). HORIZON is the correct hedge; value is real but back-loaded.

**First step:** Define references/dimension-coverage-matrix.md (KPI x dimension -> available/blocked-missing-key/planned) sourced from contract Recommended-dimensions + mapping source-map keys.

### KPI Invariant-Relationship Contract (Net <= Gross etc.)

`HORIZON` - **V7 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible AND clears the F5/F6 boundary I enforce hardest: Net<=Gross, Discount=Gross-Net, Returns in [0,1] are DETERMINISTIC ARITHMETIC IDENTITIES with no tolerance band or statistical baseline - materially-new vs the rejected statistical forecast/anomaly packs. Ships DEFINE-only (seam, not implementation); the live consumer (value_proxy, verified behind the Protocol) is not invoked from the knowledge layer. Caveat for builder: any identity that smuggles in a tolerance band would cross into F6 territory - keep it exact-arithmetic only. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Sits in the same area as rejected F5/F6 but is MATERIALLY DIFFERENT: Net<=Gross, Discount=Gross-Net, Returns in [0,1] are deterministic arithmetic identities with no tolerance band or statistical baseline -- not the statistical forecast/anomaly KIND that got F6 rejected. Ships as DEFINE-only spec (seam, not implementation, per YAGNI); the live consumer value_proxy.py is verified to exist. Clears the settled boundary by stating exactly how it differs. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Materially different from rejected F5/F6 -- these are deterministic arithmetic identities (no tolerance band, no statistical baseline), so the ML boundary is not crossed. Feasibility note the realist owns: as a DEFINE-only spec it is fully buildable now, BUT its actual CHECK value lives in the deferred live value_proxy recompute path -- so it ships as a seam, not a live guarantee. The synthesis is honest about this (YAGNI seam). Value is the authoring clarity, not enforcement, until the live consumer runs.

**First step:** Add an invariants[] block (lhs/op/rhs over named KPIs) to the relevant contracts expressing only deterministic identities; ship as spec, no live recompute.

### Reconciliation-Intent Roll-Forward (validation-checks worklist + recon_intent rule)

`NOW` - **V7 / F7** - consistency: consistent - **ELIGIBILITY SPLIT -- a reviewer flagged a hard-principle concern; needs human review**

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: the worklist half is pure DEFINE aggregation of committed validation prose + Owner; the rule half only FLAGS orphaned validation intent (WARNING via static text scan), never runs the reconciliation query - the actual reconciliation stays behind the deferred live validate/QueryRunner surface I verified. Neither half executes or self-grants sign-off. The rule half adds one EXPECTED_RULE_ID, consistent with B1's static posture.

**Panel dissent:** Panel gate is flagged as a split: only the hard-principle auditor scored this entry, voting ADOPT (7/7), so the lower CONSIDER verdict reflects the absence of corroborating reviews on this record rather than a contested ruling. No reviewer found it ineligible.

**First step:** Build templates/reconciliation-roll-forward.md collecting each contract's Validation-checks bullets + Owner into a worklist (KPI, check, source-of-truth, owner, status); add the recon_intent WARNING rule separately if the rule budget allows.

### Handoff Pack Diff-Mirror (promised vs shipped)

`NOW` - **V6 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: read-only renderer over pack files already present (bi-handoff-pack.md + carried contracts + reconciliation-report.md); binds nothing live, runs no query, ends on the existing human handoff checklist. Every cell cites a file or shows 'caveat: TBD' (no fabricated confidence). Distinct promise-vs-shipped mirror, not the receipt/gate/summary. Treat T4.1/T4.2/T2.2/T6.6 as ONE Stage-7 workstream, not four parallel ships. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Pure presentation over files already in templates/handoff/ -- composes shipped artifacts, restates none. Distinct from the receipt (sign-off token), PP1 (static gate), and Stage-7 summary (business reach): this is the promise-vs-shipped column mirror. Fills verified zero-idea Stage 7. Reviewer should treat the four Stage-7 fillers as ONE workstream to avoid shipping near-siblings at once. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Distinct promise-vs-shipped mirror, pure presentation over files already in the handoff pack -- feasibility is clean and undeferred. Value discounted only because it is one of four Stage-7 siblings (T4.1/T4.2/T2.2/T6.6); the realist would not ship all four at once. Solid but should be sequenced behind the highest-value Stage-7 picks (the Receipt and the Answerability Summary).

**Panel dissent:** 2 reviewers vote CONSIDER; the shipped-duplication auditor dissents upward to ADOPT (7/7), reading the promised-vs-shipped mirror as shipped-ledger evidence rather than a redundant presentation. All three agree it is eligible.

**First step:** Prototype the read-only mirror renderer beside evidence-pack-generator, laying data-dictionary columns against carried contracts + reconciliation evidence into the pack.

### Drift Storyboard (F014 9-class taxonomy as filmstrip)

`HORIZON` - **V6 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: renders per-class MEASUREMENTS (allowed by source-drift.md) and never emits a rolled-up drift score (forbidden); the four Principle-V classes render as STOP panels routing to unresolved-questions.md - never auto-rejudged. No comparator/runtime assumed (every panel [PENDING LIVE RE-PROFILE] absent the live boundary). Uniquely targets the orphaned Stage 1 / F014 end. Boundary-clean: classification, not scoring. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Extends the shipped-but-design-only F014 source-drift taxonomy (PARTIAL, Later tier) -- names its parent. Renders per-class MEASUREMENTS (allowed) with NO rolled-up score (forbidden by source-drift.md, verified), Principle-V classes as STOP panels. Only candidate touching the orphaned Stage 1 SOURCE/F014 end. Consistent extension of an open-prior design. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Uniquely targets the orphaned Stage 1 / F014 end and is principle-clean (per-class measurements, no rolled-up score, Principle-V classes as STOP panels). Feasibility honesty flag the realist owns: absent the live re-profile boundary (which is the deferred live source), EVERY panel renders [PENDING LIVE RE-PROFILE] -- so the artifact is a template skeleton with no live content until the source-drift comparator exists. Real value is latent and gated on the deferred live profiling. HORIZON correct; build as template-only.

**First step:** Draft templates/source-drift-storyboard.md (one panel per fired class: baseline / observed / human ruling or PENDING) with STOP panels for the four Principle-V classes; add a routing note in source-drift.md.

### Finding Schema Conformance Test (freeze B2 JSON contract)

`NOW` - **V6 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: test-only over the existing pure gate, no DB, adds zero rules. Distinct from the parity test (frozen external SHAPE consumers depend on, vs renderings-agree); guards a real silent consumer-break across the four docs/tools skills. Validates structure, no fabricated confidence. Bundle with T5.1/T5.4 as one 'pin the gate output contract' workstream. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Pins the shape of shipped B2's output that four docs/tools skills consume -- a guard ON shipped B2, not a restatement of it. Distinct from the parity test (frozen external shape vs renderings-agree). Test-only, zero new rules. Incremental given B2 is stable and consumers are few; best bundled with T5.1/T5.4 as one 'pin the gate output contract' workstream. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Distinct from parity (frozen external SHAPE vs renderings-agree) and guards a real silent consumer-break across four docs/tools skills. Feasibility maximal -- test + a tiny committed JSON Schema, stdlib-only, no DB, no new rule. Value is incremental given B2 is stable and consumers are few, so the realist scores it useful-but-bundleable: ship it as part of one 'pin the gate output contract' workstream with T5.1, not as a standalone priority.

**Panel dissent:** The hard-principle auditor votes ADOPT while the duplication auditor and the realist hold at CONSIDER (all 6/8--6/9); the disagreement is purely about whether freezing the B2 contract clears the ADOPT bar, not about eligibility.

**First step:** Commit schemas/findings.schema.json + tests/unit/test_findings_schema.py (a stdlib hand-rolled validator: keys == FindingDict.__annotations__, severity in Severity, exit_code in {0,1}) over planted-violation fixtures.

### Recorded Routing-Walk Harness (18-scenario smoke test as evidence)

`HORIZON` - **V6 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: STATICALLY resolves each scenario's declared route through routes.yaml via A1's resolver and records the path chain - no agent, no LLM, no execution (I confirm A1 is a read-only resolver). Records resolution evidence, not a routing-quality judgment, so no fabricated confidence. Turns the verified 'Not run by design' smoke test (open-prior) into recorded evidence rows - materially-new mechanism over the prior doc. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Turns the shipped-but-'Not run by design' agent-routing smoke test into recorded evidence by STATICALLY resolving each scenario via A1's resolver -- no agent, no LLM, no execution (consistent with no-execute). Extends an open-prior doc, names it, and does not restate the per-route A1 check (this is per-scenario). Consistent. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Genuinely converts the verified 'Not run by design' smoke test into recorded PASS/BLOCKED evidence rows by STATICALLY resolving each scenario's route through routes.yaml (reusing A1's resolver) -- no agent, no LLM, no execution, so feasibility is honest (it does NOT secretly need a model eval). Value is real but moderate: it records resolution evidence, not routing-quality judgment. Buildable now; HORIZON reflects priority, not a deferred dependency.

**First step:** Add a docs/quality harness + a small static checker reusing A1's resolver: read each scenario's expected-route, resolve via routes.yaml, emit a filled status + path-chain evidence table.

### Gate-Run Summary Manifest (run-level rollup over B2)

`NOW` - **V6 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: pure aggregation over findings B2 already produces - no execution, no new data source, no new rule (family() derives from existing id prefixes, 33-count untouched). Counts only (no fabricated confidence). Materially above shipped B2's per-finding emission. Share the rule_id->family helper with T5.5. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Materially above shipped B2 (per-finding emission) -- this is the run-level aggregate (by_severity/by_family/exit_code/rules_evaluated) consumers currently re-derive. Pure aggregation, no new data source, no new rule (family() derives from existing id prefixes, 33-count untouched). Names B2 as parent, states the difference. Should share the family() helper with T5.5. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Materially above shipped B2 (run-level rollup vs per-finding emission) and pure aggregation -- no execution, no new data source, no new rule (family() derives from existing id prefixes). Feasibility clean. Value moderate: consumers currently re-derive these counts, so it removes minor duplication; the realist would bundle it with T5.5 (shared family() helper) rather than ship standalone.

**First step:** Add runner.summarize(findings) producing {by_severity, by_rule, by_family, exit_code, rules_evaluated}; expose via --format summary; unit-test counts match the findings list.

### Lineage Edge Closure Rule (AL-edge)

`HORIZON` - **V6 / F5** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: pure static read of contract front-matter (derives_from + status); ERRORs only on an impossible status combination (derived 'Covered' while base 'Blocked') over the 4-status ordering, never invents an edge (undeclared edge -> no check), never grants readiness. No execution, no fabricated confidence. Weakened: hard-gated on T1.1's derives_from shipping first - cannot ship independently, so readiness is contingent. CONSIDER only after T1.1. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Sound fail-closed CHECK (derived KPI cannot out-claim its weakest base) over the shipped 4-status ordering; never invents an edge. Not a restatement of any shipped rule. But it hard-depends on T1.1's derives_from field shipping first (synthesis: 'ships only if T1.1 ships first'), so its readiness is contingent -- skeptic weakened it. Consistent in mechanism with shipped static rules. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Sound fail-closed CHECK (forbids derived-KPI status more advanced than min(parents) over the 4-status order, never invents an edge). But feasibility is hard-gated on T1.1's derives_from field shipping first -- the synthesis admits 'ships only if T1.1 ships first.' The realist read: its 'feasible' rests on an unbuilt upstream artifact, so it cannot ship independently. Real value, but strictly contingent -- PARK until T1.1 lands and proves the edges are populated.

**Panel dissent:** 2 reviewers vote CONSIDER (6/5); the value/feasibility realist dissents to PARK (6/4), judging that the edge-closure value only materializes after the upstream derives_from/invariants groundwork ships, making today's feasibility weaker.

**First step:** After T1.1 ships derives_from, add a rule reading the contract store; ERROR when status(derived) is more advanced than min(status(parents)); add one EXPECTED_RULE_ID.

### Invariant-Relationship Definition Schema + L3 drift hook

`HORIZON` - **V6 / F5** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible and boundary-clean: deterministic arithmetic identities, NO tolerance band/statistical baseline (clears F5/F6). I verified semantic.py/L3 has zero @register and only maps Verdict->Finding, so this correctly adds NO EXPECTED_RULE_ID per ADR 0007 and reuses the existing static contract-vs-DAX comparison - no live recompute, no execution. Materially-new vs T3.3 (binds the identities to the EXISTING L3 mechanism as a static structural check). Weakened: hard-gated on T3.3's invariants[] block. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Reuses the shipped L3 contract<->DAX drift machinery (verified semantic.py has zero @register, so it correctly adds NO EXPECTED_RULE_ID per ADR-0007 -- consistent). Deterministic arithmetic identities, materially distinct from rejected F5/F6 statistical baselines (no tolerance band). Hard-depends on T3.3's invariants[] block, so weakened. Names L3 as the mechanism it extends. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Principle-clean (deterministic identities, not statistics) and correctly adds NO EXPECTED_RULE_ID -- verified semantic.py has 0 @register, so L3 is a tagged surface per ADR 0007. But feasibility is hard-gated on T3.3's invariants[] block shipping first; without it there is nothing for the L3 verdict to read. Value is real (reuses existing L3 Verdict->Finding machinery, no new live path) but contingent on the upstream DEFINE half. PARK until T3.3 lands.

**Panel dissent:** 2 reviewers vote CONSIDER (6/5); the value/feasibility realist dissents to PARK (6/4) because the schema's payoff is gated on the upstream invariants[] candidate landing first, so it is not buildable-with-value today.

**First step:** After T3.3 ships invariants[], add an L3 Verdict checking the bound measure's DAX structurally honors the identity's referenced terms (e.g. gross-margin DAX subtracts a cost term); no new registered rule.

### Skill INDEX Endpoint Integrity Checker (A4)

`HORIZON` - **V6 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: static text/link extraction over tracked INDEX.md files, no execution, fails closed. Materially-new vs A1 (validates targets named INSIDE each layer's INDEX.md, not the central manifest). Targets verified-real dangling endpoints (bi-python cleaning-review-checklist 'planned', missing domains/customer.md). The extractor must be scoped to avoid prose false-positives - an implementation caveat (hence HORIZON), not a principle issue. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Targets verified-real dangling endpoints (bi-python cleaning-review-checklist marked planned; retail-kpi customer domain with no customer.md -- 11 domains verified). Per-INDEX scope is materially distinct from shipped A1's central-manifest scope and names it. Real bug class. HORIZON + CONSIDER because the extractor must be carefully scoped to avoid prose false-positives; one of four A-family siblings. Not a restatement. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Targets verified-real dangling endpoints: bi-python INDEX routes its cleaning path to cleaning-review-checklist.md marked 'planned/not yet implemented' (verified), and retail-kpi lists a customer domain with no domains/customer.md (verified -- 11 domains, no customer.md). Per-INDEX scope is materially distinct from A1's central manifest. Feasibility caveat the realist owns: the in-repo path extractor must be carefully scoped to avoid false positives on prose mentions -- a real implementation risk that justifies HORIZON over NOW. Value genuine.

**First step:** Prototype a conservative in-repo-relative-path extractor over skills/*/INDEX.md; ERROR when a referenced file under that skill dir is neither tracked nor marked 'planned' on the same line; tighten scope before registering.

### Route Trace Replay (retail trace CLI helper)

`NOW` - **V6 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible - I checked the executor risk: a CLI subcommand that READS only the tracked manifest and checks file existence (the same read-only posture as A1) never runs a query or DAX. It is an agent/human helper, not a product feature; a planned route prints 'planned/deferred', never a fake path (no fabricated confidence). Makes the Not-run smoke test's intent a deterministic trace. Minor overlap with T5.3 (records evidence) but distinct (prints on demand). [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Read-only manifest trace + file-existence check (same posture as shipped A1, verified) -- never runs a query or DAX. Human/agent helper distinct from the gates/tests; reuses A1's resolver, restates nothing. Planned routes print honestly. Consistent with the shipped agent-first/CLI-as-helper model. Minor overlap with the Routing-Walk Harness (prints on demand vs records evidence). [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Read-only manifest trace + file-existence check (same posture as A1, verified) -- never runs a query or DAX, so feasibility is honest (a CLI subcommand here is NOT an executor). Genuine helper value: makes the 'Not run by design' smoke test's intent a deterministic on-demand trace for agent/human. Buildable now, reuses the A1 loader. Value moderate and overlaps the Recorded Routing-Walk Harness (T5.3 records evidence; this prints on demand) -- pick one as primary.

**First step:** Add a cli.py `trace` subcommand reusing the A1 manifest loader: resolve id/symptom -> ordered hops + built/planned status; read-only, no query.

### Single-source de-duplication guard for twin docs (DP1)

`HORIZON` - **V6 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: registry-driven static check, read-only, no execution, generic. Verified-real duplication (two readiness-pipeline.md, two aggregation-grain-checklist.md) violates the repo's 'referenced not repeated' rule. Requires the non-canonical file to carry a canonical-pointer header; human picks canonical (hence HORIZON), the rule only enforces point-not-repeat - no self-grant. Absorbs T7.7's duplicate-pipeline half. Adds one EXPECTED_RULE_ID. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Verified-real twin docs (two readiness-pipeline.md, two aggregation-grain-checklist.md) violate the repo's shipped 'referenced not repeated' rule. Registry-driven static enforcement (non-canonical file must carry a canonical-pointer header). Absorbs T7.7's duplicate half. Not a restatement of any shipped feature; one new EXPECTED_RULE_ID. HORIZON because picking canonical owners is a human design call. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Verified-real duplication: two readiness-pipeline.md (docs/readiness + docs/architecture) AND two aggregation-grain-checklist.md (bi-python + bi-bigdata) genuinely co-exist, violating the repo's 'referenced not repeated' rule. DP1 enforces a canonical-pointer header on the non-canonical twin. Feasibility honesty flag: choosing the canonical owner is an unmade human design call (hence HORIZON) -- the rule only locks in the human choice, it does not make it. Registry-driven static check; absorbs T7.7's dupe half. Value real but gated on the human canonical-owner decision.

**First step:** After a human designates canonical owners, add docs/quality/duplicate-pairs.yaml + rules/dedup.py (DP1) requiring the non-canonical file to begin with a 'canonical: <path>' pointer header; register + wire.

### Cross-Layer Handoff Readiness Card (per-contract build-readiness)

`HORIZON` - **V5 / F5** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: read-only derivation over a single committed contract, never executes, never authors DAX/SQL, never grants build approval; every unmet item a named blocker over the 4-status vocabulary (no fabricated confidence). Checks SEMANTIC build-readiness, distinct from structural linters and F8. Weakened: a downstream aggregator whose completeness depends on T1.2/T3.1/T3.2 fields existing - value gated on multiple upstream candidates. CONSIDER after its inputs land. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Checks SEMANTIC build-readiness per contract -- distinct from shipped F8 (table coverage) and from the structural linters; names them. Read-only, never authors DAX/grants approval. But it is a downstream aggregator whose completeness depends on several other candidates' fields (T1.2/T3.1/T3.2), so value is gated on its inputs -- skeptic weakened it. Consistent, not a restatement. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Principle-clean per-contract build-readiness check, but it is a downstream aggregator whose completeness depends on FOUR other candidates' fields existing (T1.2 ambiguity resolution, confidence-stamping, T3.1 additivity, T3.2 dimension keys). The realist read: its 'feasible' rests on a stack of unbuilt upstream artifacts -- value is gated on its inputs and degrades to a near-empty checklist until they ship. PARK until at least T1.2 and T3.1 are live.

**Panel dissent:** 2 reviewers vote CONSIDER (5/5--6/5); the value/feasibility realist dissents to PARK (5/4), holding that per-contract readiness value is too speculative against today's committed files.

**First step:** After the upstream DEFINE fields land, define a per-contract handoff-readiness-card (ambiguities resolved? Required-fields confidence-stamped? additivity stated? dimension keys named?) with named blockers; route bi-dax/bi-sql/bi-python INDEX to it.

### Reconciliation Ledger Trend Entry (run-over-run drift)

`NOW` - **V5 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible AS WRITTEN - and this is the candidate that walks NEAREST to the rejected F5/F6 boundary I guard hardest: it records OBSERVED totals + a plain delta_vs_prior_run with 'recorded warning, escalate to owner, never auto-pass' - no statistical baseline, no threshold model, no anomaly score (so materially-new vs F5/F6, not a relitigation). It is evidence-only and compliant. CONSIDER strictly CONDITIONAL on the builder adding NO tolerance threshold and NO auto-flagging - the instant it acquires statistical drift logic it crosses into settled-rejected F6 territory and becomes ineligible. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Extends the shipped reconciliation-ledger-entry.md template (referenced from gold-ready.md) with a run-over-run table -- names its parent. As written it records OBSERVED totals + a plain delta with 'recorded warning, escalate, never auto-pass' -- materially different from rejected F6 (no statistical baseline, threshold, or anomaly score). But the trend framing sits CLOSEST to the settled F5/F6 boundary; survives ONLY under strict no-statistics enforcement -- a single tolerance-band slip crosses into rejected territory. Minor-tension and CONSIDER reflect that boundary proximity. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Two compounding realist concerns. (1) Boundary proximity: the trend/delta framing sits closest of any candidate to the rejected F5/F6 anomaly boundary -- compliant ONLY as 'observed totals + plain delta, escalate-never-auto-pass', and a single implementation slip (a tolerance threshold) crosses into F6. (2) Feasibility: a run-over-run trend needs MULTIPLE dated live validate runs to have any content -- which depends on the deferred live path AND repeated runs that do not exist yet. So 'feasible' hides both a missing live source and a boundary risk. PARK under strict no-statistics enforcement.

**Panel dissent:** 2 reviewers vote CONSIDER (5/6--6/6); the value/feasibility realist dissents to PARK (4/4), judging the run-over-run drift value too thin until a live recompute path exists. The principle auditor's standing condition is that this trend entry stay categorical/observed-only and never acquire a statistical baseline.

**First step:** Extend templates/reconciliation-ledger-entry.md with a dated per-measure run table (run_date, measure, silver_total, gold_total, delta_vs_prior_run, verdict); a non-zero delta with both runs penny-exact is a recorded escalate-to-owner note, NEVER an auto-pass and NEVER a statistical flag.

### Reconciliation-Intent Roll-Forward (validation-checks worklist)

`NOW` - **V7 / F7** - consistency: consistent - **ELIGIBILITY SPLIT -- a reviewer flagged a hard-principle concern; needs human review**

**Why this verdict:** [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Worklist half is pure aggregation of committed validation prose + Owner field; rule half (recon_intent.py) only flags ORPHANED validation intent as a WARNING and never runs the reconciliation query (stays behind the deferred live validate/QueryRunner surface). No shipped feature does this. The rule half adds one EXPECTED_RULE_ID -- a new gate, not a restatement of any shipped rule. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Good value: turns scattered validation-checks prose into one owner-routed worklist so a finance owner knows which reconciliations are owed before sign-off. Feasibility honest on both halves -- the worklist half is pure aggregation of committed prose + Owner field (buildable now); the static-rule half (recon_intent.py) only flags ORPHANED intent as a WARNING and never runs the reconciliation query (which stays behind the deferred live surface). The 'unmade reconciliation' is correctly an 'owed' blocker, not hidden as done. Note the rule half adds an EXPECTED_RULE_ID.

**Panel dissent:** Panel gate is flagged as a split: this entry was scored only by the shipped-duplication auditor (CONSIDER) and the value/feasibility realist (ADOPT, 7/7), with no principle audit on the record; the realist would promote it while the duplication auditor holds at CONSIDER, landing the final verdict at CONSIDER.

**First step:** Build templates/reconciliation-roll-forward.md aggregating each contract's Validation-checks + Owner into a worklist; defer the recon_intent.py WARNING rule as the optional CHECK half.

## PARK

### Ambiguity Blast-Radius Index

`NOW` - **V4 / F5** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible on principle: counts NAMED affected artifacts (KPIs/pages a tag is declared in), not a fabricated prioritization score - I confirmed the count-of-named-artifacts framing stays clear of fabricated-confidence. But doubly dependent on T1.1's derives_from and the ambiguity-tag convention, and only a thin re-ranking of data the lineage graph + ledger already expose. Park until both prerequisites ship. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Not a duplicate of shipped work -- it ranks by reach, a function no shipped feature performs. But it is doubly-dependent on the not-yet-shipped derives_from edges (T1.1) and the ambiguity-tag convention, and collapses to a thin re-ranking of data the lineage graph + ledger already surface. Minor-tension flows from dependence on two unbuilt siblings, not from any shipped conflict. Skeptic weakened it. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Feasibility is doubly-deferred-on-internal-work: its value is near-zero until BOTH T1.1's derives_from edges AND the ambiguity-tag convention ship, and even then it is a thin re-ranking of data the lineage graph + ledger already expose. The realist read: 'feasible' here hides two unbuilt prerequisites and the prioritization value collapses to a convenience view. Real value only as a late add-on once T1.1 and T1.2 are live and proven thin.

**First step:** None.

### Net-Sales-Class Caveat Card (returns/VAT/discount-denominator rulings)

`HORIZON` - **V5 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: the magnitude-delta field must read an OBSERVED recompute (the real 33.55->50.37), not invent one - principle-clean as written, and explicitly distanced from the rejected F5/F6 statistics (relitigation materially-new: records chosen convention + observed consequence, not a forecast). But redundant with T1.2 ledger and T8.4 decision pack. HORIZON is correct. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Principle-clean (records an OBSERVED magnitude delta, not invented; explicitly distanced from F5/F6). Differs from shipped F7 by recording the chosen convention + numeric consequence rather than the question -- a named extension, not a restatement. But it is the third presentation of the same denominator-ruling concern alongside T1.2 ledger and T8.4 decision-pack; redundancy, not duplication of shipped work, drives the minor-tension and HORIZON. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Principle-clean (the magnitude-delta reads an OBSERVED recompute, not an invented number) and buildable, but it is the third presentation of the same denominator-ruling concern alongside T1.2 (ledger) and T8.4 (decision pack). Value is discounted for redundancy: the realist will not reward three artifacts that all say 'which convention is baked in and what it costs.' The magnitude-delta field also quietly assumes a recorded live recompute exists, which for a fresh table is the deferred live path. HORIZON is correct.

**First step:** None.

### Two-Hop Contract Linter (A2) - verify route shape

`NOW` - **V5 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: pure static read of the tracked manifest + tracked files, no execution, fails closed. Materially-new vs A1 (A1 checks targets exist; A2 checks the two-hop ORDER SKILL->INDEX->named file, same dir). Weakened: one of four A-family siblings competing for the same EXPECTED_RULE_ID budget, and shape-checking is the lower-value facet vs A3's map<->manifest bijection. Keep the highest-value subset, not all four. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Materially different from shipped A1 (route SHAPE/order vs target existence) and names it -- not a restatement. But A2 was already triaged in the prior idea-backlog as 'better folded into A1' (open-prior), and the skeptic weakened it as the lower-value member of four A-family siblings competing for the same EXPECTED_RULE_ID budget. Minor-tension: route-shape overlaps A1's resolver surface. Pick A3 over A2. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Materially-new (route SHAPE/order vs A1's existence check) and feasible (static read of the manifest + tracked files, reuses A1 resolver). But it is the lower-value member of a four-rule A-family competing for the same EXPECTED_RULE_ID budget; route-shape checking is less load-bearing than A3's map<->manifest bijection (which closes an actually-unguarded drift). The realist would select the highest-value A-family subset (A3, A4) and PARK A2 to avoid rule over-proliferation.

**Panel dissent:** Split verdict: the hard-principle auditor votes CONSIDER (5/7) while both the duplication auditor and the realist vote PARK (5/6--5/7); the majority parks it as a thin route-shape check, and the final verdict follows them to PARK.

**First step:** If the A-family budget allows after A3, add rules/routes_shape.py (A2) asserting knowledge-route ordering SKILL.md->INDEX.md->[named file] within one skill dir; add to EXPECTED_RULE_IDS.

### Two-Hop Reachability Walker (whole-graph orphan check)

`HORIZON` - **V5 / F4** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: pure static graph build over tracked files, no execution; lists unreachable files, never a coverage % (no fabricated confidence). Materially-new reverse-direction check (is every artifact reachable FROM the root). Weakened: highest false-positive surface of the four A-family members - graph reachability over heterogeneous markdown is brittle; the synthesis itself downgrades it to reporter-first HORIZON. Park until the false-positive surface is bounded. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Genuinely distinct reverse-direction check (is every artifact reachable FROM the root) vs shipped A1's forward target-existence -- not a restatement. But the skeptic weakened it as the highest-false-positive, lowest-maturity A-family member; the synthesis itself downgrades it to reporter-first HORIZON. Minor-tension from brittleness over heterogeneous markdown, not from shipped conflict. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Distinct reverse-direction check (is every artifact reachable FROM the root) but the realist's feasibility concern dominates: graph-reachability over heterogeneous markdown is brittle and high-false-positive -- the synthesis ITSELF downgrades it to 'reporter first, promote to a rule once the false-positive surface is bounded.' That is an admission the rule form is not yet feasible. Least-ready A-family member; PARK until the reporter proves the false-positive surface is tractable.

**First step:** None.

### Python-from-DAX Reflection (auto-derive source-prep checklist)

`HORIZON` - **V5 / F5** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible: reasons from a committed contract to a checklist instance, never executes pandas, never runs a notebook, never defines KPI meaning. Weakened by a verified dangling-endpoint HAZARD: the natural endpoint (cleaning-review-checklist.md) is 'planned / not yet implemented' - if the generator ends there it ships exactly the dangling route A4 flags. Survives ONLY if re-pointed to the existing aggregation-grain-checklist.md. Park until that endpoint exists or it is re-pointed. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Attacks the verified maturity asymmetry (bi-python is thinnest) by deriving a checklist from a shipped contract -- not a restatement. But VERIFIED endpoint hazard: the natural endpoint (cleaning-review-checklist.md) is genuinely planned/nonexistent, so as pitched it would ship a dangling route -- exactly what A4 flags. Survives ONLY if re-pointed to the existing aggregation-grain-checklist.md. Minor-tension from the endpoint hazard; skeptic weakened it. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Verified endpoint hazard: the natural endpoint cleaning-review-checklist.md is 'planned / not yet implemented' (confirmed in INDEX.md lines 34,43) and does NOT exist -- only aggregation-grain-checklist.md exists. The realist read: 'feasible' here hides a missing endpoint; if built as pitched it ships a dangling route (exactly the bug A4 flags). It survives ONLY if re-pointed to the existing aggregation-grain checklist, which narrows its value substantially. PARK until either the cleaning checklist lands or the scope is re-pointed and proven.

**First step:** None.

### Confidence-Stamped Decision Pack (assumption-grade per answer)

`NOW` - **V4 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible AS WRITTEN: a CATEGORICAL roll-up (firm/qualified/provisional) of the contract's declared confirmed-concept/assumption markers, explicitly never a probability/percentage - reads the source-field-requirements.md legend, no execution. This is the boundary I enforce hardest: the moment any implementation computes a NUMERIC confidence score it violates no-fabricated-confidence, so the categorical-only constraint is load-bearing. Also redundant with T1.4 caveat card and the answerability views. Park - the latent numeric-grade risk plus redundancy outweigh near-term value. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Principle-clean as a CATEGORICAL roll-up (firm/qualified/provisional) of declared markers -- explicitly never a probability, so it clears no-fabricated-confidence. Differs from shipped F7 (question->contract routing) by adding answer-provenance; names F7. But it is the third 'how hard to lean on this number' artifact alongside T1.4 and the answerability views, and carries a latent risk of drifting to a numeric grade -- skeptic weakened it. Minor-tension from redundancy, not shipped conflict. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Principle-compliant as written (a CATEGORICAL roll-up of declared confirmed-concept/assumption markers, never a probability), but it is the THIRD consumer-facing 'how hard to lean on this number' artifact alongside T1.4 caveat card and the answerability views -- redundant presentation. Plus it carries a standing risk the realist must flag: an implementation could drift into a numeric grade, which would violate no-fabricated-confidence. Redundancy + latent-violation risk => PARK; pick T1.2/T1.4 as the canonical convention artifact instead.

**First step:** None.

## REJECT

### Model-Wide KPI Answerability Rollup

`HORIZON` - **V3 / F6** - consistency: conflict - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible (read-only rollup of F7+F8, status+blocker, no %), but the skeptic KILLED it - dominated by its own two stronger variants (T2.2 publish-facing, T2.3 stage-gated) which the synthesis itself recommends keeping over this stage-agnostic base. A killed idea is never ADOPT. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Not a restatement of shipped F8/F7 (it is the cross-table rollup neither provides) -- so it clears the duplication bar against shipped work. But the skeptic killed it as dominated by its own two stronger variants (T2.2 publish-facing, T2.3 stage-gated); the synthesis itself recommends dropping the stage-agnostic base. A KILLED idea cannot be ADOPT. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Skeptic killed it and the realist agrees: dominated by its own two stronger variants (T2.2 publish-facing, T2.3 stage-gated). It is buildable but its incremental value over the variants is zero -- the synthesis itself recommends dropping the stage-agnostic base. No standalone value to reward.

**First step:** None.

### Doc-Count & Single-Source Drift Guard (constitution/capability/pipeline dupes)

`HORIZON` - **V3 / F5** - consistency: conflict - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only: an idea is ineligible unless it provably touches ONLY reasoning/static/read-only seams. I name the violated principle for anything that executes, bypasses a gate, fabricates confidence, self-grants approval, or bakes in C086 specifics. I verified the four load-bearing seams against the repo: B1 (never_execute.py) forbids module-scope connection imports in the governed core via stdlib ast (parse-not-import, never executes); semantic.py/L3 has zero @register and only maps Verdict->Finding (so an L3 hook adds no EXPECTED_RULE_ID per ADR 0007); validate.py and value_proxy.py both route every query through the QueryRunner Protocol with psycopg2 built lazily in the CLI handler (so a fake-runner test opens nothing); runner.run() iterates inline while run_json() routes through _collect() (genuinely separate paths). Every surviving candidate is a static text/AST/manifest read, a docs/template authoring artifact, a test over pure rules, or a read-only presenter. NONE executes a query/DAX/agent, none self-grants any of the four human-approval seams, none is C086-specific (all generic or templated). My value/feasibility scores fold in the skeptic's verification; a KILLED idea is never ADOPT. The two boundaries I enforce hardest: (1) the trend/confidence candidates (T8.6, T8.4, T1.4) must remain categorical/observed-only and never acquire a statistical baseline, threshold, or numeric score, or they cross the settled F5/F6 ML-rejection; (2) the new-rule candidates that self-grant or check approval (PP1, Publish Receipt) must verify-the-slot-exists, never populate the sign-off line.] Eligible (static text scan, no execution), but the skeptic KILLED it by double-absorption: its count half is dominated by T5.5 (Rule Registry Snapshot, the more robust upstream fix) and its duplicate-pipeline half is a subset of DP1; the synthesis itself recommends retiring it. A killed idea is never ADOPT - the value lives in T5.5 + DP1. [SHIPPED-DUPLICATION AUDITOR. I judge each candidate for consistency with shipped work using the ship-status table + KNOWN HISTORY, verified against the live repo. I penalize ideas that merely RESTATE a shipped feature (A1/B1/B2/F7/F8) and ideas that re-litigate the settled-rejected F5/F6 ML boundary without material difference. I do NOT penalize a genuine extension that names its shipped parent and states how it differs. Verified ground truth before scoring: (1) 33 EXPECTED_RULE_IDS == 34 @register set incl. A1; (2) A1's routes.py reads ONLY docs/routing/routes.yaml, never the knowledge-map, so A3's map<->manifest bijection is a genuinely unguarded boundary, not an A1 restatement; (3) semantic.py (L3) has ZERO @register so T6.4 correctly adds no rule-count (consistent with ADR-0007); (4) net-sales trace exists/filled (10514 bytes) while capability-state still says 'planned' (real SC1 target); (5) idea-engine/idea-to-spec/implement workflows sit in NO roadmap F-row (real IL1 orphaning-loop target); (6) both duplicate-doc pairs (two readiness-pipeline.md, two aggregation-grain-checklist.md) and the stale constitution '26 rules' claims (5 occurrences vs glossary's authoritative 33) are real; (7) domains/ has 11 files, no customer.md (real A4/T8.1 dangling route); (8) bi-python cleaning-review-checklist is genuinely planned (real Python-from-DAX endpoint hazard). NO candidate matches a SHIPPED feature closely enough for a SHIPPED verdict -- all are materially-new mechanisms or named extensions. The A1/B1/B2/F7/F8-adjacent rules each name their parent and hit a different target (unresolved-assumption tokens, live-surface lazy imports, route shape, map bijection, filled-scorecard invariants, shipped-ledger evidence) -- legitimate extensions, not restatements. No candidate re-proposes F5/F6: the two invariant-relationship candidates (T3.3/T6.4) are deterministic arithmetic identities, materially distinct from statistical baselines, and the trend/confidence candidates stay categorical/observed-only.] Not a restatement of shipped work, but the skeptic KILLED it by double-absorption: its count half is dominated by T5.5 (the more robust golden-file fix) and its duplicate-pipeline half is a subset of DP1; the synthesis itself recommends retiring it. Conflict (with sibling candidates, not shipped work) because shipping it alongside T5.5+DP1 would create two/three rules over the same problem -- the kind of overlap a wiring-symmetry repo must avoid. KILLED cannot be ADOPT. [VALUE/FEASIBILITY REALIST. I judge whether "feasible" is honest or whether it hides (a) a missing gold/live data source, (b) a runtime consumer that does not exist yet (chiefly F016, verified absent from src/), or (c) an unmade human ruling. I verified against repo state: EXPECTED_RULE_IDS is already 34 (synthesis assumes a 33 baseline throughout -- a cosmetic off-by-one, not a blocker); value_proxy.py + QueryRunner Protocol are real (so live-check consumers exist but ARE the deferred path); F016 has zero src/ footprint; two readiness-pipeline.md and two aggregation-grain-checklist.md genuinely co-exist; bi-python cleaning-review-checklist.md is planned/nonexistent while aggregation-grain-checklist.md exists; runner run()/run_json() are genuinely separate. The cleanest-feasibility ideas are the test-only and pure-DEFINE-doc artifacts that depend on NOTHING deferred -- they are buildable today against committed files. The riskiest-feasibility ideas are those whose value only materializes once a live recompute (value_proxy live path) or F016 exists, or once an upstream candidate (T1.1 derives_from, T3.3 invariants[]) ships first. I reward genuine analytical/system value (additivity matrix pre-empts a real DAX footgun; severity-posture lock catches a silent gate bypass; count-drift snapshot kills a verified live defect) and discount thin re-rankings and redundant third presentations.] Skeptic killed it and the realist concurs: both halves are absorbed by stronger siblings -- its count half is dominated by T5.5 (golden-file inventory, removes hand-typing entirely) and its duplicate-pipeline half is a subset of DP1. The synthesis ITSELF recommends retiring it. No standalone value left to reward; shipping it would be the redundant third count-drift mechanism.

**First step:** None.

## Rescue notes (steelman of the not-adopted)

_A reframing that MIGHT make an idea eligible later, or the irreducible reason it stays out. A reason for a human to weigh next run -- never a verdict; the gate already ruled._

- **Ambiguity Blast-Radius Index** -- rescue possible. Drop the ranking ambition entirely and reframe as a presence-only check that ships AFTER its two prerequisites land. Instead of 'an index that prioritizes by reach' (a thin re-ranking of data the lineage graph + ledger already expose), pitch it as a static read-only presenter that, given an ambiguity tag, lists the NAMED artifacts that declare it (count-of-named-artifacts, never a prioritization score). The rescue is purely sequencing: this becomes eligible-and-worthwhile only as a late add-on once T1.1's derives_from edges and the ambiguity-tag convention are both live and proven non-thin. Keep the categorical/count-of-named-artifacts framing the auditor already blessed; never let it acquire a numeric priority score. Narrowed seam: A read-only presenter over T1.1's derives_from edges + the ambiguity-tag convention that, for a given tag, emits the list of named KPIs/pages declaring it (count, not score). No new EXPECTED_RULE_ID; pure static read of committed lineage data once both upstream siblings exist.
- **Net-Sales-Class Caveat Card (returns/VAT/discount-denominator rulings)** -- rescue possible. Rescue by consolidation, not by shipping a third artifact. The PARK is driven by redundancy (it is the third presentation of the denominator-ruling concern alongside T1.2 ledger and T8.4 decision pack), not by any principle defect -- the auditor confirmed the observed-magnitude-delta framing is principle-clean and explicitly distanced from F5/F6. So the rescue is: fold the one genuinely unique field (the OBSERVED magnitude delta, e.g. the real 33.55->50.37 recompute) into T1.2's ledger as a column there, rather than standing up a separate caveat card. That preserves its only non-redundant value while killing the three-artifacts-saying-the-same-thing problem. Narrowed seam: A single OBSERVED-magnitude-delta column added to the existing T1.2 ledger (reads a recorded recompute, never invents a number, never forecasts), instead of a standalone caveat-card artifact.
- **Model-Wide KPI Answerability Rollup** -- no rescue. No rescue is possible because the rejection is not about eligibility or principle -- the idea is fully DOMINATED by its own two stronger variants (T2.2 publish-facing, T2.3 stage-gated), which the synthesis itself recommends keeping over this stage-agnostic base. Any narrowing produces one of those variants, not a distinct idea. Its incremental value over the variants is exactly zero. The durable signal: this base form is subsumed; pursue T2.2/T2.3 instead.
- **Reconciliation-Intent Roll-Forward (validation-checks worklist + recon_intent rule)** -- no rescue. No rescue is needed or appropriate: this was marked CONSIDER and the auditors confirmed BOTH halves are eligible and principle-clean (worklist = pure DEFINE aggregation of committed validation prose + Owner; recon_intent rule only FLAGS orphaned validation intent as a WARNING via static text scan, never runs the reconciliation query, adds one EXPECTED_RULE_ID consistent with B1's static posture). The eligible=false flag is the only residual concern, and the steelman cannot override the gate -- it is not a rejection on merit. The durable signal for next run: re-confirm the eligibility classification, because on substance every reviewer found it clean and valuable (turns scattered validation prose into one owner-routed worklist so a finance owner knows which reconciliations are owed before sign-off).
- **Two-Hop Contract Linter (A2) - verify route shape** -- rescue possible. Rescue by absorption into a single highest-value A-family rule rather than shipping A2 as its own EXPECTED_RULE_ID. A2 is eligible and materially-new (checks route SHAPE/order SKILL->INDEX->named-file, vs A1's target-existence), but it loses on rule-budget economics: it is the lower-value member of four A-family siblings, and the prior backlog already triaged it as 'better folded into A1'. The rescue honors that -- fold A2's shape/order assertion into A1's existing resolver as an additional check on the SAME rule, so the route-shape coverage ships without a new rule competing for the EXPECTED_RULE_ID budget. Pick A3 (map<->manifest bijection, the actually-unguarded boundary) as the standalone new rule. Narrowed seam: A2's two-hop order/shape assertion merged into A1's already-shipped routes resolver as an extra static check (still pure read of the tracked manifest + tracked files, fails closed), rather than a separate rule. Zero net new EXPECTED_RULE_ID.
- **Two-Hop Reachability Walker (whole-graph orphan check)** -- rescue possible. Rescue by demotion to a reporter-first artifact, exactly as the synthesis itself suggested. The walker is eligible (pure static graph build over tracked files, lists unreachable files, never a coverage %), and the reverse-direction check (is every artifact reachable FROM the root) is genuinely distinct from A1's forward existence check. The only blocker is feasibility-as-a-RULE: graph reachability over heterogeneous markdown is the highest-false-positive surface of the four A-family members. So ship it first as a NON-gating read-only reporter (HORIZON) that surfaces candidate-orphans for human triage, with no @register and no fail-closed gate. Promote to a rule only after the reporter has run enough to prove the false-positive surface is bounded. Narrowed seam: A non-gating, read-only orphan REPORTER (no EXPECTED_RULE_ID, no fail-closed) that lists files unreachable from the root for human review -- deferring the rule form until the false-positive rate is measured and bounded.
- **Doc-Count & Single-Source Drift Guard (constitution/capability/pipeline dupes)** -- no rescue. No rescue: the idea is DOUBLE-ABSORBED by stronger siblings and the synthesis itself recommends retiring it. Its count half is dominated by T5.5 (Rule Registry Snapshot / golden-file inventory, which removes hand-typing the count entirely) and its duplicate-pipeline half is a subset of DP1. Verified: both readiness-pipeline.md dupes co-exist and the rule count is 34, so the underlying defects are real -- but they are already owned by T5.5 + DP1. Shipping this would create the redundant third count-drift mechanism in a repo whose whole discipline is wiring-symmetry / avoiding rule overlap. Any narrowing just hands the residual to T5.5 or DP1. The durable signal: the value lives entirely in T5.5 + DP1; do not re-propose a combined drift guard.
- **Python-from-DAX Reflection (auto-derive source-prep checklist)** -- rescue possible. Re-point the endpoint and the idea becomes eligible-and-buildable today. It is principle-clean (reasons from a committed contract to a checklist instance; never executes pandas, never runs a notebook, never defines KPI meaning). Its only blocker is a VERIFIED dangling-endpoint hazard: the natural target cleaning-review-checklist.md is planned/nonexistent (confirmed -- it does not exist on disk), so building it as pitched would itself ship the dangling route that A4 flags. The rescue: re-point the generator's output to the EXISTING aggregation-grain-checklist.md (verified present in bi-python-knowledge/checklists/). With that single substitution it reasons from a shipped contract to a real, committed endpoint -- no dangling route, no deferred dependency. Narrowed seam: The contract-to-checklist generator re-pointed to the EXISTING aggregation-grain-checklist.md instead of the nonexistent cleaning-review-checklist.md -- a pure static read of a committed contract producing a checklist instance against a real endpoint.
- **Confidence-Stamped Decision Pack (assumption-grade per answer)** -- rescue possible. Rescue by binding the categorical-only constraint into the artifact's definition and merging it with the canonical convention artifact instead of shipping a third one. As written it is already eligible -- a CATEGORICAL roll-up (firm/qualified/provisional) of the contract's declared confirmed-concept/assumption markers, explicitly never a probability. The two real concerns are (1) redundancy (third 'how hard to lean on this' artifact alongside T1.4 and the answerability views) and (2) a LATENT risk it drifts into a numeric grade. The rescue addresses both: fold the firm/qualified/provisional stamp into T1.2/T1.4 as the single canonical convention artifact, AND encode the categorical-only rule as a hard constraint (an allowed-enum of exactly three string values, with a test rejecting any numeric value) so the no-fabricated-confidence boundary is mechanically enforced, not just intended. Narrowed seam: The three-value categorical stamp (firm/qualified/provisional) added to the canonical T1.2/T1.4 convention artifact, with a unit test asserting the field can only ever hold one of the three enum strings -- never a number or percentage. No standalone third artifact.
- **Reconciliation-Intent Roll-Forward (validation-checks worklist)** -- no rescue. No rescue needed: this is the worklist-only variant of the same CONSIDER idea, and both surviving auditors found it principle-clean and genuinely valuable (worklist = pure aggregation of committed validation prose + Owner; the static recon_intent rule only flags ORPHANED intent as a WARNING and never runs the reconciliation query, correctly treating the unmade reconciliation as an 'owed' blocker rather than hiding it as done; adds one EXPECTED_RULE_ID as a legitimate new gate). The only residual is the eligible=false classification, which the steelman cannot override -- it is not a merit rejection. Durable signal for next run: re-examine the eligibility ruling, since on substance no reviewer found a defect and the value (a finance owner sees which reconciliations are owed before sign-off) is real.

## SHIPPED / SETTLED (prior ideas, for the record)

- **A1 Machine-Checkable Route Registry + Planned/Built Status Manifest** -- SHIPPED. ship_status: 'Idea-A1 Route Registry Manifest' SHIPPED -- src/retail/rules/routes.py + docs/routing/routes.yaml (PR #62 abbbd73); also 'A1 route-registry resolution rule' SHIPPED at src/retail/rules/routes.py:49 @register A1
- **B1 Never-Execute Invariant Guard (NX rule family)** -- SHIPPED. ship_status: 'Idea-B1 Never-Execute Invariant Guard' SHIPPED -- src/retail/rules/never_execute.py (PR #64 9d12589); also 'B1 never-execute guard' SHIPPED at src/retail/rules/never_execute.py:151 @register B1
- **B2 Gate Observability Ledger - Structured Findings Emission (JSON/SARIF)** -- SHIPPED. ship_status: 'Idea-B2 Structured Findings Output' SHIPPED -- src/retail/runner.py run_json (PR #63 ca1431c); also 'check --format json structured output (B2)' SHIPPED -- src/retail/runner.py run_json + cli.py --format. Note: SARIF half not cited as shipped (json only).
- **F7 Decision-Question Index per KPI Domain** -- SHIPPED. ship_status: 'Idea-F7 KPI Decision-Question Index' SHIPPED -- skills/retail-kpi-knowledge/domains/*.md (VERIFIED 11 domain files; PR #65 ae471aa)
- **F8 Per-Table KPI Coverage Scorecard** -- SHIPPED. ship_status: 'Idea-F8 KPI Coverage Scorecard' SHIPPED -- skills/retail-kpi-knowledge/references/kpi-coverage-scorecard-template.md (PR #66 9d782f8)
- **F5 Demand-Signal Forecast Pack (sell-through + reorder exception)** -- SETTLED (rejected). ship_status: 'Idea-F5 Demand-Signal Forecast' REJECTED-INELIGIBLE -- docs/roadmap/idea-backlog.md PARK (same ML/statistical boundary). Prior rationale: 'Flagged for the ML/forecasting hard line, and the flag holds ... the naming (forecast pack, demand-signal) sits on the out-of-scope ML boundary.'
- **F6 Anomaly-Baseline Contracts (statistical exception flags)** -- SETTLED (rejected). ship_status: 'Idea-F6 Anomaly-Baseline Contracts' REJECTED-INELIGIBLE -- docs/roadmap/idea-backlog.md REJECT (violates ML/statistical out-of-scope hard principle). Prior rationale: 'Ineligible as framed against the ML/statistical out-of-scope principle ... the deterministic L3/L4 verification machinery ... cannot check it without becoming a stats engine.'

## Run health & self-metrics

- Yield: 55 raw -> 46 scored (ADOPT 20, CONSIDER 18, PARK 6, REJECT 2).
- Eligibility-rejection rate: 0%.
- Consistency: 34 consistent, 10 minor-tension, 2 conflict.
- Verification: 33 survived, 10 weakened, 3 killed.
- Layer coverage: retail-kpi 17, bi-dax 3, docs-spine 10, none 12, bi-python 4
- Run health: DEGRADED (see banner above).
