# Seshat BI -- Idea Bank

> **This is a future-idea bank, not a roadmap and not a commitment.** Nothing here is
> planned, scheduled, or approved. It is exploratory brainstorming output to browse for
> inspiration. The authoritative roadmap is `docs/roadmap/roadmap.md` (F-numbered features).

_Re-running regenerates this file as a HISTORY-AWARE snapshot: each run still re-reasons every
_idea from scratch, but it now READS the prior bank and Ground's verified ship-status first, so
_an idea that has since SHIPPED is recorded as shipped (with its evidence) and a settled rejection
_is not re-litigated. This is still an idea BANK, not an evolving plan: nothing here is promoted,
_scheduled, or committed. A SHIPPED tag only records that a human already took an equivalent idea
_through the normal feature process -- the engine never moves an idea onto the roadmap itself. The
_verdicts and scores are an automated reviewer's triage opinion. An idea advances only through the
_normal spec/feature process, with a human decision._

_Generated by the `idea-engine` workflow. Generated on 2026-07-01._

**42 ideas scored** (generated across 3 rounds; raw pre-dedupe 53). Verdicts: ADOPT 14, CONSIDER 9, PARK 14, REJECT 2, SHIPPED 3. Horizon: NOW 20, HORIZON 22.

## Reviewer portfolio verdict

> (Portfolio summary, per-idea dissent, and rescue notes were generated by the previous render against the pre-fix aggregation and are STALE after the eligibility-grouping fix that raised ADOPT from 4 to 14. They were DROPPED rather than shown to avoid contradicting the corrected verdicts/scores. Re-run the idea-engine on a fresh full run to regenerate coherent prose. The verdicts, V/F scores, eligibility gates, and counts below are the CORRECT pure-JS aggregation.)

## Legend

- **Verdict** (reviewer's *triage opinion only* -- not a decision to build) -- ADOPT (worth a closer look first; eligible, consistent, high value) - CONSIDER (interesting; needs a decision or dependency) - PARK (horizon / later) - REJECT (ineligible or conflicts -- kept for the record).
- **Horizon** -- `NOW` (fits the repo today) - `HORIZON` (future vision).
- **Eligibility** -- respects all hard principles, or violates one (named in the rationale).
- **V / F** -- value / feasibility (1-10), reviewer-assigned.
- **SHIPPED / SETTLED** -- a prior idea Memory matched to shipped work or a settled rejection; kept for the record, not an open candidate.

## ADOPT

### 3. Explicit Deferred-Seam Markers in bi-sql INDEX

`NOW` - **V7 / F9** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] One-file docs-spine edit, no execution/number/approval/C086. Closes a verified silent-non-coverage gap, mirrors the existing EP-slice 'Future extension' pattern. Cheap, clean, principle-clean, survived. Eligible-clean ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Passes the extension-naming gate cleanly: names its base (the existing PG execution-plan 'Future extension' marker) and states the distinction (cross-dialect + file-source are neither present nor marked -- verified against ship-status PLANNED rows). Extends an established in-repo pattern rather than duplicating it. One honest one-file docs edit; prerequisite for #1. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Verified: INDEX defers ONLY PG execution-plan; cross-dialect + file-source neither present nor marked. One-file honest docs edit mirroring an established in-repo pattern; highest feasibility. Real DX value -- turns silent non-coverage into a routed signal, and is the prerequisite #1/#5/#7 author behind. Cheap, honest, unblocks two other ideas.

**First step:** Add a '## Not owned here (deferred seams)' section to skills/bi-sql-knowledge/INDEX.md naming cross-dialect + file-source as out-of-scope-today, mirroring the EP-slice wording.

### 7. Runtime-Registration / Wiring Reconciler

`NOW` - **V8 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] ast-parses __init__.py import list vs on-disk modules -- static text read, NOT execution (no import side effects). Skeptic verified the pkgutil-test-vs-hardcoded-__init__ divergence is real and unguarded. Categorical ERROR, no score/gate-bypass/C086. Clean-on-main, cheap, distinct from A3/SC1. Two-lens convergence. Eligible-clean ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Verified primary-source: __init__.py uses a hardcoded 'from . import (...)' list while the pkgutil wiring test discovers dynamically -- a real, unguarded dead-rule divergence. Names its base (A3/SC1) and states the distinction: reconciles engine wiring, not routes or markers. Not a restatement. A3 precedent shows a clean-on-main invariant guard is valued. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] I confirmed rules/__init__.py is a hardcoded import list while the wiring test uses pkgutil dynamic discovery -- a rule file on disk but absent from __init__ is DEAD at runtime yet passes the test. Real, currently-uncatchable failure. Feasible: stdlib ast-parse of __init__ + os.listdir diff, reads files that exist today. Distinct from A3 (routes) and SC1 (markers). High value, high feasibility.

**First step:** Add src/retail/rules/wiring_integrity.py: ast-parse __init__.py ImportFrom names, diff against os.listdir of the rules package (minus dunders); any asymmetry is an ERROR; register it.

### 8. RC1 Rule-Count Reconciler

`NOW` - **V7 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] ast.parse counting @register (stdlib, not import) -- never-execute, categorical, eligible. Skeptic confirmed SC1's docstring explicitly delegates the count facet to 'a separate future sibling' = this. Materially different from the REJECTED Doc-Count guard (that was constitution/capability/pipeline dupes; this AST-counts registered rules to defeat multi-line-@register grep undercount). Ship-ordering caveat (reconcile stale '38' first) is sequencing, not a principle break -> keeps ADOPT with a first-step prereq note. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Adjacent to the settled-rejected Doc-Count drift guard, so held to the materially-new bar and the difference is stated: that guard detected constitution/capability/pipeline prose dupes; RC1 AST-counts @register decorators to defeat the multi-line-@register grep undercount. Verified: status_claims.py line 28 literally reads 'explicitly OUT of scope, delegated to a separate future sibling' -- RC1 IS that cited sibling. Distinct target, distinct mechanism. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] SC1's docstring explicitly delegates the rule-COUNT facet to a future sibling -- RC1 is that sibling, AST-counting @register to defeat the multi-line-grep undercount. Distinct from the REJECTED doc-count dupe guard. Good value (crown-jewel staleness: roadmap '38' vs 40). Feasibility docked: won't be clean-on-main until the stale line is reconciled first (ship-ordering, not a principle break), and needs a hand-curated count-claims manifest.

**First step:** First reconcile the stale roadmap '38' to 40; then add src/retail/rules/rule_count.py + docs/quality/count-claims.yaml, AST-walking @register counts vs each manifest anchor's claimed integer.

### 18. Per-KPI Required-Aux-Source Dependency Field + Blocked-Until-Mapped Gate

`HORIZON` - **V8 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static read of declared dependency vs mapping-gate evidence; defaults to blocked; the human clears via mapping approvals[] the agent cannot self-write -- P-APPROVE and P-GATE both respected. Never-execute, categorical, no score. Distinct from per-table F8/SL1 (contract-attached, travels with the metric). Two-lens field+enforcement convergence. Needs a template field authored -> CONSIDER not ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Passes the extension-naming gate: names F8 scorecard + SL1 linter and states the distinction (those are per-TABLE, layer-external; this attaches the dependency to the KPI CONTRACT so it travels with the metric). Reuses the existing ambiguities[] number_moving->blocked fail-safe. Two-lens field+enforcement convergence. Not a restatement; DEFINE/CHECK/APPROVE respected. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Distinct from per-table F8/SL1 by attaching the dependency to the contract so it travels with the metric; reuses the verified ambiguities[] number_moving->blocked fail-safe. High value -- makes 'GMROI needs cost+inventory' a machine-enforced blocked-default the agent cannot self-clear. Feasible today: the default-to-blocked-until-owner-rules IS the deliverable (not a deferred external ruling), field + CHECK are both authorable now. Field+enforcement two-lens convergence.

**First step:** Add required_aux_sources[] (name + mapping-gate reference) to templates/metric-contract.yaml with a default-to-blocked note, then a @register rule keying each declared aux source against a CLEARED mapping-gate marker.

### 19. Ratio-KPI Denominator-Zero & Filter-Symmetry Contract Field

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Net-new declared contract field (blank vs 0, filter-symmetry), never-execute, categorical (behavior present/absent), generic, DEFINE-only -- eligible, clear of P-CONF. Supplies the intent the shipped syntactic DIVIDE() rule had nothing to compare against. Needs a template field authored -> CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Not a duplicate of the shipped DIVIDE()-over-slash D-rule -- names it and states the distinction: that rule enforces SYNTAX with no contract to compare intent against; this supplies the declared intent (blank vs 0, filter-symmetry) the semantic-check was missing. Net-new categorical field, DEFINE-only, clear of F5/F6. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] I confirmed DIVIDE is enforced syntactically (AR-DIV-001/BP-021) but there is no CONTRACT field carrying the declared intent (blank vs 0, numerator/denominator filter-symmetry) for the semantic-check to compare against. Net-new declared field, DEFINE-only, categorical, feasible today. Supplies the intent the existing syntactic rule had nothing to check.

**First step:** Add zero_denominator_behavior + filter_symmetry block to templates/metric-contract.yaml + reference; cross-ref the DIVIDE() DAX rule and semantic-model-ready gate.

### 20. Comparison-Base Completeness Field

`HORIZON` - **V7 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Reusable comparison_base dependency field forcing blocked-default; never-execute, categorical (base present/absent), generic, DEFINE-only, hard-stops rather than fabricating a -100% -- eligible, clear of P-CONF. Distinct mechanism from the Target/Budget FORMULA contracts (dependency vs formula). Needs a template field -> CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Push-further of round-1's Target/Budget Variance work, and it names that base and states the difference: variance authored FORMULA contracts; this adds a reusable comparison_base DEPENDENCY field that travels with ANY comparison KPI and forces a blocked-default. Different mechanism, not a restatement. Categorical, DEFINE-only, clear of F5/F6. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Reusable comparison_base dependency field (distinct from round-1's per-KPI variance FORMULA contracts) that travels with any comparison KPI and forces a blocked-default instead of a misleading -100%/0%. DEFINE-only, categorical, no forecast (clear of F5/F6). Feasible today -- the blocked-default is the deliverable. Real value preventing partial-period fabrication.

**First step:** Add a comparison_base block to templates/metric-contract.yaml + reference; document default-to-blocked; cross-ref time-intelligence + targets domains.

### 21. Sign-Convention Declaration Field

`NOW` - **V8 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] C086 check: the deliverable is a GENERIC required-declaration field -- no C086 value baked in; negative-signed returns is only the illustrative hazard, not hardcoded logic. Owner rules the sign, agent only checks it is declared (P-APPROVE respected). Undeclared sign is a blocking_reason. Never-execute, categorical, no forecast. Verified-empty seam, cheap, clean -> ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Verified-empty seam; generalizes a real hazard (memory note records negative-signed returns as a C086 fact) into a GENERIC declaration field with no C086 value baked in. Not a restatement of any shipped field. Owner rules the sign, agent only checks declaration presence -- DEFINE-not-APPROVE. No principle touched. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Net-new categorical field forcing an owner ruling on stored sign of returns/refunds/adjustments -- a verified real hazard (memory records negative-signed returns) generalized with NO C086 value baked in. Undeclared sign is a blocking_reason; owner rules, agent only checks declaration presence. High value (same Net Sales formula flips answer), high feasibility (one template field, authorable today).

**First step:** Add a sign_convention block to templates/metric-contract.yaml + reference; cross-ref returns + sales-and-revenue domains and the net-sales contract; bake in no client value.

### 31. Fiscal-Calendar 4-4-5 Time-Intelligence + Aligned-Week YoY

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Deterministic calendar arithmetic + authoring guidance, NOT a forecast (clear of P-CONF); docs-spine + knowledge authoring only, never-execute, generic. Distinct from Gregorian-only S8/RC15; the FISCAL-SEAM1 superset first marks the honest boundary (mirrors #3) then authors the card. Two-lens convergence. CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Passes the extension-naming gate: names shipped S8/RC15 and states the distinction (those guarantee only a contiguous Gregorian date dim; fiscal 4-4-5/4-5-4 aligned-week YoY is a distinct, currently-planned slice). Superset first marks the Gregorian-only boundary honestly (mirrors the accepted #3 pattern), then authors. Deterministic arithmetic, clear of F5/F6. Not a restatement. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Distinct from Gregorian-only S8/RC15; fiscal 4-4-5/4-5-4 + aligned-fiscal-week YoY is a currently-planned slice. Deterministic calendar arithmetic (not a forecast). The FISCAL-SEAM1 superset makes the Gregorian-only boundary honest first (mirroring the accepted #3 pattern), then authors the pattern. Feasible docs+knowledge today; solid retail value (real merchandising calendars). Two-lens convergence.

**First step:** Add a 'Future extension' fiscal-calendar seam row to skills/bi-dax-knowledge/INDEX.md; then author knowledge/fiscal-calendar-time-intelligence.md (4-4-5/4-5-4 + aligned-week YoY) ending on an authoring checklist; no src/retail code.

### 32. Consumer Meaning / KPI Reading Guide

`HORIZON` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] C086 check: WALK_IN/DQ1 are illustrative examples in a generic template bound 1:1 to approved contract fields -- no client value hardcoded, eligible. DEFINE-only, never reads powerbi/, fail-closed [PENDING] on blanks, never self-grants publish (P-APPROVE/P-GATE ok). Clearly differentiated from S7A/PP1/release-notes. Horizon tie -> cautious HORIZON. CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Passes the extension-naming gate strongly: names S7A, PP1, and the shipped release-notes-generator and states each distinction (S7A=answerability status; PP1=section presence; release-notes=change log; this=interpretation from approved contract fields). DEFINE-only, fail-closed [PENDING], never self-grants publish. Not a restatement. Horizon conflict resolved to NOW (sourced entirely from already-approved contract fields; no new upstream authoring needed). [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Clearly differentiated from S7A (answerability), PP1 (section-presence), release-notes (change log): renders interpretation (includes/excludes/grain/caveat) from already-approved contract fields, fail-closed [PENDING] on blanks, never reads powerbi/ or self-approves. Feasible DEFINE-only template bound 1:1 to existing contract fields; real consumer-comprehension value the gates don't cover. Horizon: I pick NOW (template authorable immediately, matching the BI lens).

**First step:** Author templates/meaning-card.md with slots binding 1:1 to metric-contract.yaml meaning/additivity/caveat fields; add a retail-kpi route; consume at Publish as evidence, adding no new status or gate.

### 33. First-Run Gate Explainer

`NOW` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Post-run formatter over runner.run_json Findings + parsed argv; never re-runs/re-scans/connects/suppresses/auto-scopes and keeps exit 1 (P-GATE and P-EXEC both respected), self-grants nothing (P-APPROVE), invents no score. Distinct from shipped FHC (engine gate vs table author), grounded in local-verification.md. Two-lens convergence, cheap, clean-on-main -> ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Passes the extension-naming gate: names the shipped First-Hour Compass and states the distinction (FHC onboards a TABLE AUTHOR; this onboards a new agent to the ENGINE's own red-by-default gate, grounded in docs/quality/local-verification.md). Pure post-run formatter over existing B2 Findings + argv; never re-runs/suppresses/auto-scopes, keeps exit 1. Not a restatement. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Distinct from shipped FHC (which onboards a TABLE AUTHOR); this onboards a new agent to the ENGINE's red-by-default gate, grounded in local-verification.md's documented CI-green-while-local-red split. Reads only B2 structured Findings + argv, self-grants nothing, keys generically on finding-class, still returns exit 1. High DX value (biggest engine onboarding-friction gap); feasible as a post-run formatter + small manifest, though it needs a hand-curated rule_id->explanation map. Two-lens convergence.

**First step:** Add a post-run formatter over run_json Findings + argv with a hand-curated rule_id->explanation manifest (fail-closed on missing entry); classify commit-subject findings outside the effective range generically; touch cli.py output only, keep exit 1.

### 35. bi-bigdata Reason-About-Do-Not-Adopt Scope Marker in INDEX

`NOW` - **V6 / F9** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] One INDEX+SKILL text edit, never-execute by construction, generic, no gate/score. Skeptic verified bi-bigdata INDEX has zero Snowflake/BigQuery scope markers, so the reason-about-vs-adopt ambiguity is genuinely unsignposted; mirrors the accepted #3 pattern. Cheap, clean, principle-clean -> ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Verified-absent scope marker (bi-bigdata INDEX has zero Snowflake/BigQuery scope markers). Names its base (the accepted bi-sql deferred-seam pattern #3) and mirrors it -- extends an established pattern, not a duplicate. Docs-only, never-executes, no gate. Closes a real reason-about-vs-adopt ambiguity. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Verified: bi-bigdata INDEX has zero Snowflake/BigQuery scope markers, so the reason-about-vs-adopt ambiguity is unsignposted. One-file docs edit mirroring the accepted #3/bi-sql deferred-seam pattern -- highest feasibility. Good honesty/DX value; two-lens convergence.

**First step:** Add one route-by-symptom scope row + a boundary paragraph to skills/bi-bigdata-knowledge/INDEX.md and SKILL.md stating Snowflake/BigQuery/Spark are reason-about-only, never-adopt; no new file, no rule.

### 37. Contract-to-Decision-Question Backlink Reconciler

`HORIZON` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static id-reference read of domain decision-question tables vs contract ids, categorical (reachable/orphaned), never-execute -- eligible. Skeptic confirmed a DIFFERENT id graph than A3 (knowledge-map<->routes.yaml) and distinct from F7's authoring. Clean-on-empty acceptable per A3 precedent. CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Passes the extension-naming gate: names shipped F7 (authored the decision-question index) and A3 (routes.yaml bijection) and states the distinction -- this reconciles a DIFFERENT id graph (retail-kpi contracts <-> domain decision-question rows). Guards a real orphan-KPI failure. Static, categorical, never-executes; clean-on-empty acceptable per A3 precedent. Not a restatement. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Distinct id graph from A3 (routes.yaml) and from F7's authoring: reconciles retail-kpi contracts  domain decision-question rows so a shipped KPI can't become an unfindable orphan and a decision-question can't dangle. Static id-reference read of files that exist today -- feasible now. Real integrity value; clean-on-empty acceptable per A3 precedent.

**First step:** Add a reconciler rule scanning retail-kpi domains/*.md decision-question tables against contracts/*.md ids; fail-loud on a dangling or unreachable id; register it.

### 39. Row-Level Security (RLS) visibility-semantics DEFINE card

`HORIZON` - **V7 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] DEFINE-only reasoning card, never writes an RLS role or connects to a model (P-EXEC ok), feeds human approvals[] at publish (P-APPROVE ok), generic (branch/region not C086), no score. Skeptic verified no card owns who-sees-what visibility semantics + the filtered-total additivity trap. Verified-empty seam, cheap, clean -> ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Verified-empty seam (grep for row-level security returns only incidental DAX hits + bigdata storage-security notes; no card owns who-sees-what visibility semantics). DEFINE-only reasoning with a real retail hazard (branch-filtered total != enterprise total); never writes a role or connects. Not a restatement; feeds human approvals at publish. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Verified-empty seam (no who-sees-what visibility card -- only incidental DAX/storage hits). DEFINE-only reasoning card capturing identity-to-scope mapping, the filtered-total additivity trap (branch total  enterprise total), and PII least-privilege ruling; never writes a role or connects. High real-world value for multi-branch retail (a recurring real hazard), high feasibility (one knowledge card + routes).

**First step:** Author skills/bi-dax-knowledge RLS filter-context reasoning card + INDEX route + a symptom route ('filtered viewer sees a wrong total'), with a DEFINE checklist of visibility rulings an owner must record; no role authoring, no connection.

### 41. Symptom Concierge (cross-layer which-layer-owns-this-symptom router)

`NOW` - **V6 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Pure docs-spine routing table pointing only at existing anchors, never-execute, categorical, generic, two-hop-compliant, A3-guardable -- eligible, no principle touched. Skeptic confirmed distinct from task-first knowledge-map and intra-layer symptom tables; fills a real symptom-first triage gap. Cheap, clean -> ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Passes the extension-naming gate: names the shipped knowledge-map router (task-first, 22 routes) and the per-layer symptom tables, and states the distinction -- a symptom-first router-of-routers pointing at existing anchors, A3-guardable, two-hop-compliant. Fills a real symptom-first triage gap; not a restatement. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Distinct from the task-first knowledge-map (22 routes) and from intra-layer symptom tables: routes a raw symptom to the OWNING layer first (a router-of-routers, two-hop-compliant, pointing only at existing anchors, A3-guardable). Feasible as a single routing doc pointing at existing anchors. Real symptom-first triage value for an agent that only knows the symptom.

**First step:** Add a 'Symptom -> owning layer -> that layer's symptom route' table (new doc or a docs/knowledge-map.md section) pointing at existing PB-SQL / bigdata-symptom / retail-kpi-symptom anchors; add route ids so the shipped A3 reconciler guards the pointers.

## CONSIDER

### 1. Cross-Dialect SQL Portability Reasoning Cards

`HORIZON` - **V6 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Knowledge-text only, never executes, reason-about-not-adopt for Snowflake/BigQuery, generic, no numbers. Verified-empty seam (0 dialect hits). No principle touched. Depends on #3's seam marker and overlaps #4, so CONSIDER not ADOPT; HORIZON per lens majority. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Verified-empty seam (whole-layer grep for t-sql/mysql/snowflake=0 hits, corroborated by ship-status PLANNED row). Names its base (bi-sql-knowledge, Postgres-implicit today) and is genuinely unclaimed; not a restatement. Overlaps #4 on constructs but is the correctness angle. Knowledge-text only, never executes. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Verified-empty seam (0 dialect hits), knowledge-text only, never executes. Feasible: prose cards + INDEX rows, no engine touch. Value moderate for a deliberately Postgres-only shop -- teaches reasoning the shop may rarely need -- but the seam is real and two lenses converged. Sits behind #3's marker.

**First step:** Author skills/bi-sql-knowledge/knowledge/sql-dialect-portability.md with SC-DX-* ids ending on validation-gate shapes; add INDEX 'Route by dialect' rows; touch no src/retail code.

### 5. Standalone File-Source Grain Reasoning (CSV/Excel) in bi-python

`HORIZON` - **V7 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Fillable [PENDING FILE PROFILE] template + reasoning card; no runtime file read in any src/ (profiler stays DSN/psycopg2). Never-execute, categorical PENDING/warning, generic. Verified-empty seam, 3-lens convergence. Horizon tie broken to cautious HORIZON. Strong CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Verified-empty seam (file-source owned by no layer; only BD-CN-050 interchange note, matching ship-status PLANNED row). Three-lens convergence. Never-execute template mirroring source-ready.md missing-DSN handling. Not a restatement. Horizon conflict resolved to HORIZON (two of three lenses; a new cross-layer authoring effort, not a same-day docs edit). [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Verified-empty seam (file-source owned by no layer; only BD-CN-050 interchange note). Three-lens convergence. Feasible: fillable [PENDING FILE PROFILE] template + reasoning card mirroring source-ready.md's missing-DSN handling; no runtime file read. Real value -- closes a genuine gap and enables #6. Horizon: I pick NOW (docs/template is authorable immediately, matching the CP lens).

**First step:** Author skills/bi-python-knowledge/knowledge/file-source-grain.md (grain/encoding/delimiter/multi-sheet reasoning) ending on a grain statement handed to the mapping gate; add a fillable profile template, no live file reads.

### 22. RETENTION-DEP1 Durable-Identity + PII Dependency Guard

`HORIZON` - **V5 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static contract-field scan reusing template pii_sensitive + blocking_reasons; checks the DEFINITION declares identity dependency and resolves PII posture, never computes a retention curve (clear of P-CONF), never self-clears (P-APPROVE ok). Eligible. But a domain-specialization of #18's generic aux-source guard; incremental over #18 -> CONSIDER as the customer-domain instance of #18. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] My axis: names #18 and claims distinction (customer-identity+PII intersection), but it is a domain-specialization of #18's generic required_aux_sources mechanism, not a fully independent seam. Value incremental over #18. Consistency minor-tension for the overlap; principle-clean (reuses existing pii_sensitive/blocking_reasons fields, no cohort modeling). [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Principle-clean (checks the DEFINITION declares identity + PII posture, never models a curve; reuses verified pii_sensitive + blocking_reasons). But it is a domain-specialization of #18's generic required_aux_sources rather than an independent seam -- value incremental over #18. Feasible but partly subsumed; pairs with #26.

**First step:** Implement as a domain:customer specialization of #18's required_aux_sources plus a customer_identity_dependency field; new rule asserting identity dependency named AND PII posture resolved before pass.

### 24. PVM Sales Bridge (price/volume/mix decomposition)

`NOW` - **V6 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Exact arithmetic identity (dRev = dPrice + dVolume + dMix), mix convention is an explicit owner-ruling blocking_reason, gold-only, DEFINE + authoring only -- never-execute, no baseline/forecast (clear of P-CONF), eligible. Genuinely new retail-kpi content. CONSIDER; DEFINE half of #24/#25 pair. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Genuinely new retail-kpi content (no PVM contract shipped). Exact arithmetic identity (dRevenue = dPrice + dVolume + dMix), mix convention as owner-ruling blocking_reason -- clear of the settled-rejected F5/F6 statistical family. Not a restatement; DEFINE + authoring only. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Exact-identity DEFINE contract (Revenue = Price + Volume + Mix) + authoring note; mix convention is an owner-ruling blocking_reason; gold-only; no statistics (clear of F5/F6). Genuinely new retail-kpi content, feasible as contract+catalog text today. Value solid (answers 'why revenue changed'); pairs with #25.

**First step:** Author retail-kpi contracts/price-volume-mix-bridge.md as an exact_identity family with a bi-dax authoring note; the mix convention stays an owner-ruling blocking_reason; no executor.

### 26. Customer Retention / Cohort DEFINE family

`HORIZON` - **V6 / F6** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] DEFINE-only KPI meaning + durable-identity dependency + PII owner-ruling blocking_reason, explicitly NOT CLV/cohort-curve modeling -- clear of P-CONF (no statistical baseline), never-execute, gold customer keys only. Distinct from the settled F5/F6 rejections by construction (definition, not model). Fills the planned customer domain. CONSIDER; DEFINE half paired with #22. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Fills the seeded-but-planned customer domain (verified per ship-status: additivity/domains exist, per-KPI contracts planned) -- not a restatement. Explicitly DEFINE-only meaning + PII owner-ruling, NOT the settled-rejected statistical cohort/CLV modeling. Clear of F5/F6 by construction. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] DEFINE-only meaning + durable-identity dependency + PII owner-ruling blocking_reason, explicitly NOT CLV/cohort-curve modeling (clear of F5/F6). Fills the seeded-but-planned customer domain. Feasible as contract text today; value solid. Pairs with #22's CHECK.

**First step:** Author retail-kpi contracts repeat-purchase-rate.md / retention-rate.md / new-vs-returning-sales.md, each naming the identity dependency and carrying a PII owner-ruling blocking_reason; no cohort modeling.

### 27. Target/Budget Variance and Attainment pack

`NOW` - **V6 / F8** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Identity arithmetic (Variance = Actual - Target; Attainment % = Actual / Target), NOT a target model; target source is a declared blocking_reason, never fabricated; gold-only actuals -- never-execute, clear of P-CONF, eligible. Fills the planned targets-and-budgets domain and supplies the exact_identity contracts #25 reconciles. CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Fills the seeded-but-planned targets-and-budgets domain -- not a restatement. Pure identity arithmetic (Variance=Actual-Target; Attainment %=Actual/Target), NOT a target model; targets are declared dependencies never fabricated. Clear of F5/F6. Also supplies the exact_identity contracts #25 reconciles. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Pure identity-arithmetic DEFINE contracts (Variance = Actual  Target; Attainment % = Actual / Target) filling the seeded-but-planned targets-and-budgets domain; targets are declared dependencies, never modeled. Feasible as contract text today; also supplies the exact_identity contracts #25 reconciles. Good value.

**First step:** Fill retail-kpi contracts attainment.md / variance-value.md / variance-percent.md (planned->seeded) + a catalog pack; targets remain declared dependencies, never modeled.

### 28. Contract-Aggregation to DAX-Verb Consistency CHECK (D13)

`NOW` - **V6 / F5** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static token scan of committed TMDL vs a contract's definition.aggregation -- never-execute, categorical (verb equality), eligible; genuinely distinct from the L3 generator round-trip (guards hand-authored measures). But skeptic verified definition.aggregation appears only in COMMENTED template examples, so template DEFINE work is a hidden prerequisite. Mild dependency-gate -> CONSIDER not ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Names the shipped L3 semantic-check/generate round-trip and states the distinction: that verifies the GENERATOR's own output; D13 guards HAND-AUTHORED measures where no round-trip exists. Genuinely distinct, not a restatement. Not my axis: weakened on dependency (verified: definition.aggregation appears only in COMMENTED template examples, lines 213/219/221 -- some template DEFINE work is a hidden prerequisite). Consistency-clean. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] The hand-authored-measure verb check is genuinely distinct from the L3 generator round-trip. But I confirmed definition.aggregation appears in metric-contract.yaml ONLY in commented example blocks (lines 213/219/221) -- the 'reuses an existing documented field' claim is optimistic; documenting/promoting the field is a hidden template prerequisite. Value real, feasibility docked for the scaffolding gap.

**First step:** First promote definition.aggregation from commented example to a documented template field; then add @register('D13') in src/retail/rules/dax.py mapping aggregation values to DAX verbs, ERROR on mismatch, skip contracts with no definition block.

### 29. Derived-Measure Build-On-Base Convention

`NOW` - **V5 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Knowledge-card authoring keyed to references/kpi-derivation-lineage.md; never-execute, no measure written into powerbi/, categorical, generic (KPI-MC ids) -- eligible. But partly overlaps the shipped no-duplicate-logic D-rule and its value hinges on the lineage doc not already implying composition. Incremental -> CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Names the shipped D-family no-duplicate-logic rule + references/kpi-derivation-lineage.md and claims distinction (composition-vs-recomputation keyed to the lineage graph). Real but partly overlaps the shipped no-duplicate-logic rule and hinges on the lineage doc's exact content. Minor-tension on the overlap; incremental value. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Plausible net-new bi-dax authoring card at the lineage seam. I confirmed kpi-derivation-lineage.md exists (in retail-kpi, not bi-dax as the pitch states -- a mislocation to correct). Partly overlaps the shipped no-duplicate-logic D-rule; value incremental and contingent on the lineage doc not already implying composition. Feasible text card.

**First step:** Author skills/bi-dax-knowledge/knowledge/derived-measure-composition.md cross-referencing kpi-derivation-lineage.md by KPI-MC id and the no-duplicate-logic D-rule; confirm the lineage doc does not already state composition first.

### 38. Smoke-Test Registry / Run Ledger (38a index + 38b run-ledger)

`HORIZON` - **V4 / F7** - consistency: consistent - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Both variants check a cell/anchor and never execute a test nor declare a pass (P-EXEC and P-APPROVE both respected, Principle V honored). Static YAML+doc read reusing G6's placeholder mechanism, categorical, no score -- eligible. The no-index tension is verified. But with RT-* legitimately all 'Not run', 38b mostly guards a declared-exemption and 38a is a light discoverability lock -- modest payoff. CONSIDER; recommend the lighter 38a scope first. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Names SC1 (prose status) and A3 (routes) and states the distinction (reconciles test-case coverage/run-cell completeness -- a different target). Not my axis: weakened on modest payoff while every RT-* cell is legitimately 'Not run' (verified via ship-status PARTIAL row), not on duplication. Consistency-clean; reviewer picks 38a (lighter, ship-today) vs 38b scope. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Verified tension (two overlapping manual smoke tests, RT-* all 'Not run', no single index). Principle-clean (checks cells/anchors, never runs or self-passes). But with every run-cell intentionally 'Not run', 38b mostly guards a declared-exemption and 38a is a light discoverability lock -- modest payoff. Recommend the lighter 38a scope. Feasible as YAML+rule.

**First step:** Ship the lighter 38a: docs/quality/smoke-test-registry.yaml (suite->doc+anchor) + a @register rule erroring on an unindexed suite doc or an unresolvable registry row; defer 38b's per-case run-status ledger.

## PARK

### 2. Dialect Portability Passport (static non-portability lint)

`NOW` - **V3 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Adding an ast/text @register rule over committed SQL is NOT execution -- stays inside never-execute; categorical, no score. Eligible. But repo is Postgres-by-design (CLAUDE.md), so flagging Postgres-lock flags the intended state -- low value, skeptic weakened it. PARK. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Not a duplicate of shipped S1-S8 (those enforce Postgres-flavored medallion/naming correctness; this flags Postgres-lock). But low value in a deliberately Postgres-only repo (CLAUDE.md: DigitalOcean Postgres source) -- flagging Postgres-lock flags the intended state. Consistency-clean; parked on value. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Principle-clean but low value: the repo is Postgres-by-design (CLAUDE.md), so flagging Postgres-lock flags the intended state. Feasibility mid -- reliably distinguishing PG-only from portable constructs is a non-trivial AST/text surface for near-zero payoff in a single-dialect shop. Skeptic weakened; I discount value further.

**First step:** None.

### 4. Dialect-Answer-Delta Caveats (construct to KPI-semantics map)

`HORIZON` - **V4 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static prose cards, categorical construct->caveat, no threshold -- clear of P-CONF, eligible. But the correctness-vs-answer-integrity split from #1 is thin; skeptic judged it realistically a sub-section of #1. CONSIDER as a fold-in. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] My axis: substantially overlaps #1 (same constructs -- integer division, date_trunc, NULLS FIRST/LAST). The correctness-vs-answer-integrity split is a framing difference; realistically a sub-section of #1's card, not an independent deliverable. Not a shipped-match, but thin-differentiation docks value. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Skeptic weakened: the correctness-vs-answer-integrity split is thin -- realistically a sub-section of #1 keyed to the same constructs (integer division, date_trunc, NULLS FIRST/LAST). Feasible as text but low incremental value once #1 exists; fold in rather than author a second card.

**First step:** Fold the construct->KPI-property->caveat table into #1's dialect-portability card as an 'answer-integrity' subsection rather than a standalone card.

### 6. File-Source Answerability Gate (CHECK profile vs KPI contract)

`HORIZON` - **V6 / F4** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Reads two committed docs (profile + contract), categorical blocked/warning, never reads the file -- never-execute, eligible. But third in a 3-deep chain (#5->#18->#6); delivers nothing until both DEFINE artifacts land. Feasibility-gated -> PARK, never ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Not my axis: weakened on dependency (gated behind #5's template and #18's field), not on duplication. Distinct from F8 per-table scorecard (DSN-table-centric) -- this is file-source-centric and contract-keyed. Consistency-clean; enforcement seam is real, actionability deferred. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Principle-clean CHECK but third in a three-deep chain (#5->#18->#6): needs both the file-profile template AND the required_aux_sources field to exist first. Real enforcement value once those land, but not actionable now. Feasibility low on my axis precisely because it waits on two unbuilt DEFINE artifacts.

**First step:** After #5 and #18 land, write a reconciler reading the file-source profile artifact vs the contract's required_fields/grain.

### 9. Docstring Rule-Inventory Reconciler

`NOW` - **V3 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static AST/text read of module docstrings vs registered ids -- never-execute, eligible. Premise verified (dax.py understates D1-D11+C1). But guards a comment, not a behavior; adds ongoing docstring-maintenance friction per future rule, and needs docstrings normalized before clean-on-main. Low payoff -> PARK. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Not my axis (weakened on payoff, not duplication). Complements A3/SC1 without overlap; premise verified (dax.py docstring understates its D1-D11+C1 range). But guards a developer comment, not a behavior; high maintenance-tax-to-payoff. Consistency-clean, parked on value. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Premise true (dax.py header understates D1-D11+C1) but it guards a COMMENT, not a behavior or gate -- low payoff, and adds docstring-maintenance friction to every future rule. Feasible but the maintenance-tax-to-value ratio is poor. Skeptic weakened; I keep it low-value.

**First step:** None.

### 10. XR1 Cross-Layer Id-Reference Reconciler

`HORIZON` - **V4 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static id extraction vs authored ids, categorical, never-execute -- eligible. But skeptic grep-proved the dangling PY-CN ids it guards ALREADY exist, so its motivating problem and 'strengthens bi-python' claim collapse; survives only as a forward-locking invariant on the A3 precedent. Low current value -> PARK. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Its motivating example collapsed (the borrowed PY-CN-052..058 ids ARE authored in bi-python -- grep-verified), so its 'strengthens bi-python' claim fails and value drops. But I do NOT let the killed #11 DEFINE half drag this CHECK sibling: as a forward-locking invariant guard it survives on the A3 precedent. Consistency-clean; parked on diminished value. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] I confirmed the borrowed PY-CN-052..058 ids ARE authored in bi-python, so the specific dangling-id problem does not exist and the 'strengthens bi-python' claim collapses. Survives only as a forward-locking invariant (A3 precedent). Feasible as a static reconciler but low value guarding an empty set; its DEFINE half (#11) is dead.

**First step:** None.

### 12. Analyzer-Rule Graduation Ledger

`HORIZON` - **V3 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Docs ledger + static reconciler read of ledger vs registry; human ratifies, agent only CHECKs consistency -- never-execute, no self-promotion (P-APPROVE respected). Eligible. But likely over-engineered for the current low draft-candidate volume -- a governance mechanism heavier than what it governs. PARK. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Not my axis (weakened on over-engineering for the current draft-candidate volume). Distinct from the metric-contract lifecycle. Consistency-clean; a governance mechanism likely heavier than the near-static set it governs. Parked on value. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Principle-clean but over-engineered for the current draft-candidate volume: a DEFINE/CHECK graduation pipeline + ledger + reconciler governs a set that trickles in rarely. Governance heavier than the thing governed. Feasibility mid (needs a new ledger doc); value low until candidate volume justifies it.

**First step:** None.

### 14. D12 Semi-Additive SUM Guard

`NOW` - **V6 / F4** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static token scan of committed TMDL vs a contract's declared class -- never-execute, categorical (structural, not statistical), eligible; materially distinct from D5/D8. But its DEFINE half #13 is already-shipped and it needs an additivity_class enum authored into templates/metric-contract.yaml first (skeptic verified the enum does not exist). Dependency-gated feasibility hit -> PARK, never ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Its killed DEFINE half (#13) does NOT drag this CHECK: the enforcement is genuinely distinct -- verified no D-rule reads a contract's additivity class vs measure aggregation (D5 governs summarizeBy on COLUMNS, D8 gold-only sourcing). Not my axis: weakened on dependency (additivity_class enum absent from templates/metric-contract.yaml -- verified). Consistency-clean; actionability gated on the template field. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Genuinely distinct enforcement (no D-rule reads contract additivity vs measure aggregation) and a real 12x-overstatement bug, but I confirmed additivity_class does NOT exist in metric-contract.yaml -- it is only prose/checklist classification. D12 cannot fire until a new enum field is authored into the template first, and the knowledge half it enforces (#13) already ships. Value real, actionability gated on unbuilt scaffolding.

**First step:** Author the additivity_class enum into templates/metric-contract.yaml, then register D12 firing on naked SUM where class demands time-awareness.

### 16. Aggregation-Path Equivalence Note (ratio-of-sums across SQL/DAX/pandas)

`NOW` - **V4 / F8** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Prose + example snippets, exact arithmetic identity (ratio-of-sums), never-execute, DEFINE-only -- eligible, clear of P-CONF. But per-layer content exists (AR-ADD-001 / PY-CN-056 / additivity classification); only the thin cross-layer binding note is new, and it competes with #17 for the same seam. Small binding card -> CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] My axis: each layer already teaches ratio-of-sums within itself (AR-ADD-001, PY-CN-056, retail-kpi additivity), so only the thin cross-layer binding note is new, and it overlaps #17 on the same additivity-across-layers seam. Not a full shipped-match but the net-new content is a thin cross-reference; value docked for overlap. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Each layer already teaches ratio-of-sums within itself (AR-ADD-001, PY-CN-056, retail-kpi additivity). Only the cross-layer binding pointer is new and thin, and it overlaps #17. Feasible as a small text card but low incremental value -- the substance already exists per-layer.

**First step:** Author a retail-kpi cross-reference card stating total-numerator/total-denominator (recompute per level), linking the additivity cards in bi-sql/bi-dax/bi-python; text only. Reconcile scope against #17 before authoring.

### 17. Additivity Triangulation Card (claim across contract/summarizeBy/SQL grain)

`HORIZON` - **V4 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Reasoning/authoring card, categorical (consistent/conflict-named), generic, never-execute -- eligible. The three-way invariant is a legitimate net-new cross-layer statement, but bi-dax is NOT the near-empty layer claimed (has retail-examples/patterns/checklists) and it overlaps #16. Modest value -> CONSIDER; reviewer should pick #16 or #17, not both. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] My axis: the three faces already carry additivity discipline individually; the triangulation invariant is net-new but modest, and it overlaps #16. Its 'strengthens bi-dax (near-empty)' claim is undercut -- bi-dax already has substantial additivity content (verified: retail-examples, patterns, checklists). Value docked for overlap and the false near-empty premise. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] The three faces already carry additivity discipline individually; the three-way invariant statement is real but modest and overlaps #16. Its 'strengthens bi-dax (near-empty)' claim is undercut -- bi-dax already has substantial additivity content (examples, patterns, checklists). Feasible text card, modest value.

**First step:** Author one cross-layer card stating additivity->summarizeBy->grain must agree, with a generic ratio example; decide #16-vs-#17 scope first to avoid duplicate seams.

### 25. MC1 Exact-Identity Bridge Completeness CHECK

`HORIZON` - **V6 / F4** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static YAML-set read reconciling declared components against authored siblings; checks structural completeness, never computes arithmetic (clear of P-CONF), never-execute -- eligible; distinct from PP1/SC1/A3. But requires a new family+components[]+total vocabulary AND #24/#27 landing first; dependency-gated -> PARK, never ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Not my axis: weakened on dependency (needs a new family+components[]+total vocabulary and #24/#27 to land first), not duplication. Names PP1/SC1/A3 and states the distinction: reconciles a metric's internal component-set, a different target. Consistency-clean; never computes arithmetic, only structural completeness. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Distinct structural reconciler (never computes arithmetic, checks components present) but gated on new family/components[]/total contract vocabulary AND on #24/#27 authoring exact_identity contracts first. Real enforcement value once those land; not actionable now -- feasibility docked for the unbuilt vocabulary and sibling dependency.

**First step:** After #24/#27, add family+components[]+total fields to the template and a reconciler asserting each component + total resolve to an authored contract.

### 30. Contract Handoff-Consistency Convention

`HORIZON` - **V3 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Template + docs authoring + a checklist item; never-execute, categorical, generic -- eligible. But its premise (four independently hand-authored handoff notes that can drift) is unverified; if the notes derive from one field/grain/exclusion set they cannot diverge and it guards a non-problem. Overlaps #18/XR1. Contingent value -> PARK. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] My axis: overlaps #18 and XR1's consistency instinct, and its premise (four independently hand-authored handoff notes that can drift) is unverified -- if they derive from one source the drift it guards cannot occur. Names #18/XR1 but the differentiation is contingent. Value docked; parked. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Legitimate DEFINE convention ONLY IF the four discipline handoff notes are hand-authored independently and can drift -- an unverified premise. If they derive from one field/grain/exclusion set the drift it guards cannot occur. Overlaps #18 and XR1's consistency instinct. Value contingent; feasible text but the payoff hinges on an unproven assumption.

**First step:** None.

### 36. Scale-Reconciliation Handoff Bridge

`HORIZON` - **V3 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Reasoning + checklist authoring, never-execute, categorical, generic -- eligible. But skeptic read validation-and-reconciliation-at-scale.md: the inbound reconciliation discipline (partial-aggregate, idempotency, penny-parity) is already authored; 'hands into thin air' is false. Only a thin cross-layer pointer is new. Modest value -> PARK. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] My axis: premise overstated. Primary-source verified: validation-and-reconciliation-at-scale.md already covers partial-aggregation, idempotency, and pre-handoff trust, and aggregation-and-grain-at-scale.md covers recomposition. The reconciliation LANDING largely exists ('hands into thin air' is false); only a thin retail-kpi->bigdata pointer is new. Names F015 and distinguishes correctly, but value is a thin pointer. Docked. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Premise overstated: validation-and-reconciliation-at-scale.md already covers earn-trust-before-handoff, partial-aggregate for additive totals, and non-idempotent-rerun inspection, and aggregation-and-grain-at-scale.md covers recomposition. The landing largely exists; only a thin retail-kpi->bigdata pointer is new. Feasible but low incremental value.

**First step:** None.

### 40. Mapping-stage business-semantic open-questions ledger

`HORIZON` - **V4 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Docs-spine template extension, invents no answers (each open ruling is a blocking_reason for the human owner), does not self-grant the mapping approval (P-APPROVE/P-GATE ok), categorical open/closed, no score -- eligible. But the net delta over the existing unresolved-questions.md is a ruling-class field + KPIs-blocked back-reference; decision-improving payoff is asserted, not demonstrated. Incremental -> CONSIDER. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Extends the existing unresolved-questions.md template that already drives the zero-open-rows mapping gate, so risks cosmetic re-plumbing; the net-new delta is a ruling-class field + KPIs-blocked back-reference. Minor-tension for the overlap with the existing template; decision-improving payoff is asserted, not demonstrated. Docked; parked. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Principle-clean docs-spine artifact tied to the real zero-open-rows mapping gate, but the delta over the existing unresolved-questions.md template is a classification field (grain/PII/rollup/product-identity) + KPIs-blocked back-reference -- useful but incremental, and its decision-improving payoff is asserted, not demonstrated. Feasible; value modest.

**First step:** Extend the unresolved-questions.md template with a required ruling-class field (grain/PII/rollup/product-identity) + a KPIs-blocked back-reference to the KPI catalog; optionally a categorical CHECK; add no new status.

### 42. Cross-Visual KPI Consistency Reconciler

`NOW` - **V5 / F3** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Static PBIR read that REVIEWS existing human-authored pages (dodges FR-008/009, P-EXEC ok), asserts nothing, no score -- eligible and genuinely distinct from R1/F034. But skeptic flagged the KPI-identity grouping across free-form visual.json titles/measure refs as a fragile heuristic that will mis-group or miss, producing false positives/negatives on the exact credibility signal it sells. Reliability unestablished -> PARK on feasibility. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Names shipped R1 (reads only the relative model path) and F034 (design-vs-implemented) and states the distinction (reconciles the SAME KPI's binding across separate pages) -- genuinely distinct, not a restatement. Not my axis: weakened on FEASIBILITY (KPI-identity grouping across free-form visual.json titles/measure refs is a fragile heuristic risking false positives/negatives). Consistency-clean; reviews human pages so dodges FR-008/009. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Chases the single most valuable dashboard credibility bug (same KPI, two pages, different number) and is principle-clean (static PBIR read, reviews human pages so dodges FR-008/009, distinct from R1/F034). But feasibility is the problem: grouping visuals by 'KPI identity' across free-form visual.json titles + measure refs is a fragile heuristic that will mis-group (same title/different measure) or miss (different title/same measure), producing false positives/negatives on the exact signal it sells. R1 deliberately reads only the trivially-stable byConnection field for this reason. High value, low reliable feasibility as pitched.

**First step:** Prototype the grouping heuristic against real committed visual.json to establish false-positive rate before registering; only proceed if reliably groupable.

## REJECT

### 23. Per-KPI Data-Dependency Precondition field (merged into #18)

`HORIZON` - **V1 / F1** - consistency: conflict - **ELIGIBILITY SPLIT -- a reviewer flagged a hard-principle concern; needs human review**

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Not an independent deliverable -- merged into #18 as its DEFINE half. Retained only as a ledger pointer for row-for-row alignment; scoring it as live would double-count #18. No standalone action. Eligible trivially (same static/DEFINE mechanism as #18). [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Deliberately not double-counted: the synthesis folded this into #18 as its DEFINE half. Scoring it as a standalone would double-count #18's value. Marked ineligible as a merge-pointer, zero standalone value; its merit lives entirely in #18. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] Pointer entry -- the BI R1 DEFINE half correctly folded into #18. Scored as #18 to keep the three-reviewer item set aligned; not an independent deliverable, so no standalone refutation applies. See #18 for the real assessment.

**First step:** None -- folded into #18; do not build separately.

### 34. Static Source-Profile Snapshot Comparator

`HORIZON` - **V3 / F5** - consistency: conflict - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] As scoped (static diff of two committed markdown snapshots, per-class magnitude as evidence not a score) it is eligible on principles -- never-execute, clear of P-CONF. But skeptic read source-drift.md l17-19 verbatim: 'diffing two profiles programmatically ... NO retail drift CLI and NO comparator in src/retail/ here' is the explicitly-deferred seam, and #34 proposes exactly that comparator/CLI; the live-only reading is contradicted by the doc. Extends F014 (PARTIAL). Re-proposes a settled-deferred seam -> REJECT. (Would become P-EXEC-ineligible only if widened to the live re-profile path.) [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] The comparator is NOT shipped (F014's design shipped, ship-status PARTIAL/design-only), so not SHIPPED and not ineligible. But primary-source verified: source-drift.md's out-of-scope list reads verbatim 'diffing two profiles programmatically ... There is NO retail drift CLI and NO comparator in src/retail/ here' -- #34 proposes exactly that comparator/CLI. Its 'deferred for the live path only' reading is contradicted by the doc text. This re-proposes an explicitly-deferred seam; conflict, settled. PARK is the honest verdict -- it arrives with the live boundary, not now. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] KILLED / relitigation: I read source-drift.md lines 17-19 verbatim -- the deferred seam is 'diffing two profiles programmatically... NO retail drift CLI and NO comparator in src/retail/ here', NOT qualified as live-only. #34 proposes exactly 'a retail drift --baseline X --observed Y static comparator', the verbatim deferred item. The F014 taxonomy shipped (design-only); this re-opens an explicitly-settled deferral. The live-only reading is an interpretation the doc's own text contradicts.

**First step:** None.

## SHIPPED (panel judged this a duplicate of shipped work)

### 11. PY- Spine Authoring Pass

`HORIZON` - **V1 / F3** - consistency: conflict - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Knowledge authoring only, no execution/number/C086 -- eligible on principles. But skeptic grep-proved PY-CN-052/053/054/056/057/058 are ALREADY authored in bi-python (checklists + aggregation-and-grain-at-scale + training-set); the ids this pass exists to write already exist. Killed as already-shipped -> SHIPPED, never ADOPT. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] Highest-confidence duplication call. Primary-source verified: the borrowed ids the pass exists to write (PY-CN-052/056, present in aggregation-grain-checklist.md) are already authored in bi-python. The work it proposes is mostly already done; bi-bigdata's provenance tags are not dangling. Restates shipped content. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] KILLED by primary source: I grepped and confirmed PY-CN-052/053/054/056/057/058 are already authored in bi-python (aggregation-grain-checklist.md, agent-training-set). The ids this pass exists to write already exist; bi-bigdata's provenance tags are not dangling. Nothing to author -- the work already ships.

**First step:** None.

### 13. Semi-Additive Measure Authoring Pattern

`NOW` - **V1 / F3** - consistency: conflict - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Definitional knowledge authoring, no statistics/execution -- eligible on principles. But skeptic grep-proved the LASTNONBLANK semi-additive card already ships in dax-retail-examples.md section 5 + dax-patterns.json 'semi-additive-balance'. Killed as already-shipped -> SHIPPED. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] High-confidence duplication call. Primary-source verified: dax-retail-examples.md and dax-patterns.json already carry the LASTNONBLANK / semi-additive-balance pattern the pitch claims is missing. The 'no authoring guidance exists' premise is false. Restates shipped bi-dax content. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] KILLED by primary source: dax-retail-examples.md already contains the LASTNONBLANK semi-additive end-of-period-inventory shape, and dax-patterns.json has a semi-additive-balance pattern. The authoring card this proposes already ships. Its enforcement sibling #14 loses its DEFINE half.

**First step:** None.

### 15. Additivity Safety Atlas at Scale

`NOW` - **V1 / F3** - consistency: conflict - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY only, against five named hard principles: (P-EXEC) never-execute -- a candidate is ineligible if the deliverable runs code, imports the target module, or opens a DB/model connection (note: adding an ast.parse/text-read @register rule over committed files is NOT execution -- the engine is never-execute by design, so rule proposals stay eligible); (P-GATE) never bypass a gate; (P-CONF) never fabricate confidence -- no numeric score/threshold/baseline/forecast (F5/F6); a candidate that emits an observed magnitude as evidence does NOT trip this; (P-APPROVE) never self-grant approval -- clearing must route to a human approvals[] entry; (P-C086) not client-specific -- ineligible only if the deliverable HARDCODES a C086 value/logic, not merely cites it as an illustrative example in a generic artifact. Presumption of ineligibility is my default posture, but on honest application nearly every candidate touches only static/read-only/DEFINE-CHECK seams, so most are eligible=true; strictness lives in precise principle-naming plus low value/feasibility and REJECT/SHIPPED/PARK verdicts, NOT manufactured ineligibility. survived_verification is TAKEN from the skeptic (killed != ineligible). Verdict caps: ADOPT reserved for eligible + survived + not-gated + cheap/clean-on-main (verified-empty-seam docs edits, verified-divergence guards, explicitly-delegated siblings); dependency-gated or ship-ordering items are feasibility hits -> CONSIDER/PARK, never ADOPT; skeptic-killed-because-already-exists -> SHIPPED; skeptic-killed-on-scope-reproposal -> REJECT. consistency: consistent = aligns with never-execute + DEFINE/CHECK/APPROVE separation + shipped architecture; minor-tension = ship-ordering or overlap friction; conflict = requires relaxing a principle or re-litigates a settled rejection. Horizon single-enum resolution: majority-of-lenses, tie -> cautious HORIZON (so #5->HORIZON, #32->HORIZON). #38 scored as one row, a/b scope noted in rationale. One row per candidate 1-42 including the #23 merged pointer, for row-for-row orchestrator alignment.] Reasoning-card authoring, reason-about-not-adopt, categorical, no score -- eligible on principles. But skeptic read aggregation-and-grain-at-scale.md: never-sum-non-additive (l13), recompute-from-base PY-CN-056 (l32), distinct-counts-not-summable (l53) already taught. The 'bi-bigdata=0 whitespace' premise is false. Killed as already-shipped -> SHIPPED. [Shipped-duplication auditor. I judge each idea's CONSISTENCY with shipped work using the ship-status table + known history, grounded in primary-source verification of the actual repo. I penalize ideas that merely RESTATE shipped work (verdict SHIPPED, consistency conflict), do NOT penalize genuine extensions that name their shipped base and state the distinction, and hold anything adjacent to settled-rejected history to a materially-new bar with the difference stated. I inherit the skeptic's survived_verification verbatim and split its "weakened" set by cause: overlap/thin-differentiation is my axis (discount value, minor-tension); dependency/feasibility gating is not my axis (stays consistent, another reviewer owns it). This is triage for an idea bank, never a build decision.] High-confidence duplication call. Primary-source verified: bi-bigdata aggregation-and-grain-at-scale.md already teaches never-sum-non-additive, weighted averages, and recompute-from-base (PY-CN-056). The 'trap the layer doesn't yet name' / 'bi-bigdata=0 whitespace' premise is false. Restates shipped bi-bigdata content. [Value/Feasibility Realist. I grade the axis the skeptic did not: is "feasible" hiding an unbuilt template field, an unbuilt sibling DEFINE, or a missing runtime consumer -- and does the idea catch/enable something genuinely uncatchable today versus restating shipped content. I verified every disputed premise against primary source (rules/__init__.py hardcoded import list; metric-contract.yaml aggregation only in commented examples; PY-CN-052..058 already authored in bi-python; LASTNONBLANK already in dax-retail-examples.md; bigdata aggregation-and-grain-at-scale.md lines 14/32/53; source-drift.md lines 17-19 deferring the comparator; bi-sql/bi-bigdata INDEX scope markers; RLS absence). Eligibility is principle-based so it is true across the board (skeptic found no hard-principle break); I do NOT treat "survived" as "valuable" -- thin cross-reference cards and dependency-gated CHECKs score low on my axis even when principle-clean. DEFINE fields that default-to-blocked-until-an-owner-rules are feasible today because the block IS the deliverable; I penalize only ideas waiting on unbuilt engine/template scaffolding.] KILLED by primary source: aggregation-and-grain-at-scale.md lines 14/32/53 already teach never-sum-non-additive, weighted averages, recompute-from-base at the target grain, and distinct-counts-cannot-be-summed. The 'trap bi-bigdata doesn't name' is already named. bi-bigdata=0 whitespace premise is false.

**First step:** None.

## SHIPPED / SETTLED (prior ideas, for the record)

- **ADL Per-Contract Ambiguity Decision Ledger (force A1-A10 to a recorded ruling)** -- SHIPPED. Ground row 'kraken batch SL1/AL1/S7A/DQ1/FHC/LVR/ADL (38 -> 40 rules)' SHIPPED (shipped-ideas.yaml lines 76-106; PRs #101-#107); ledger ADL=shipped 'PR #106 (spec 058 per-contract-ambiguity-decision-ledger; ambiguities[] block on metric-contract.yaml)'
- **S7A Stage 7 Answerability Summary (executive-readable)** -- SHIPPED. Ground row 'kraken batch SL1/AL1/S7A/DQ1/FHC/LVR/ADL (38 -> 40 rules)' SHIPPED (shipped-ideas.yaml lines 76-106); ledger S7A=shipped 'PR #101 (spec 053 stage-7-answerability-summary; templates/handoff/answerability-summary.md)'
- **FHC First-Hour Compass / New-Table Author Onboarding Cockpit** -- SHIPPED. Ground row 'kraken batch SL1/AL1/S7A/DQ1/FHC/LVR/ADL (38 -> 40 rules)' SHIPPED (shipped-ideas.yaml lines 76-106); ledger FHC=shipped 'PR #103 (spec 055 first-hour-compass; templates/first-hour-compass.md + skill + tools doc)'
- **AL1 Assumption Ledger Rule (AL1)** -- SHIPPED. Ground row 'kraken batch SL1/AL1/S7A/DQ1/FHC/LVR/ADL (38 -> 40 rules)' SHIPPED (SL1+AL1 registered); ledger AL1=shipped 'PR #107 (spec 059 assumption-ledger-rule) @register AL1'
- **SL1 Coverage Scorecard Linter** -- SHIPPED. Ground row 'kraken batch SL1/AL1/S7A/DQ1/FHC/LVR/ADL (38 -> 40 rules)' SHIPPED (SL1+AL1 registered); ledger SL1=shipped 'PR #104 (spec 056 coverage-scorecard-linter) @register SL1'
- **B3 Live-Surface Import Boundary Guard (B3)** -- SHIPPED. Ground row 'idea-loop A3/B3/PP1/SC1/DF1' SHIPPED (shipped-ideas.yaml lines 54-74; PRs #95-#99; all five registered); ledger B3=shipped 'PR #96 (spec 048 live-surface-import-boundary-guard)'; also Ground row 'never-execute ... B3 live-surface guard' evidence src/retail/rules/live_surface_boundary.py
- **PP1 Publish-pack completeness gate (PP1)** -- SHIPPED. Ground row 'idea-loop A3/B3/PP1/SC1/DF1' SHIPPED (shipped-ideas.yaml lines 54-74; PR #97); ledger PP1=shipped 'PR #97 (spec 049 publish-pack-completeness-gate)'
- **A3 Route-Registry Coverage Reconciler (A3) - map rows == routes.yaml ids** -- SHIPPED. Ground row 'idea-loop A3/B3/PP1/SC1/DF1' SHIPPED (shipped-ideas.yaml lines 54-74; PR #95); ledger A3=shipped 'PR #95 (spec 047 route-registry-coverage-reconciler)'
- **SC1 Stale-Marker Sweep / Status-Claim Reconciler (SC1)** -- SHIPPED. Ground row 'idea-loop A3/B3/PP1/SC1/DF1' SHIPPED (shipped-ideas.yaml lines 54-74; PR #98); ledger SC1=shipped 'PR #98 (spec 050 stale-marker-sweep)'
- **IL1 Idea-Bank Memory Seam / Shipped-row->roadmap-F-row hard link (IL1)** -- SHIPPED. Ground row 'IL1 Idea-Bank Memory Seam (shipped-ideas.yaml, read-only)' SHIPPED -- docs/roadmap/shipped-ideas.yaml (present, structured, read-only per header) + roadmap.md:229. NOTE: not present as an id key inside the ledger itself; shipped per Ground (authoritative state source)
- **DQ1 DQ-Signal Interpretation Note (-1 unknown-member counts as business caveat)** -- SHIPPED. Ground row 'kraken batch SL1/AL1/S7A/DQ1/FHC/LVR/ADL (38 -> 40 rules)' SHIPPED (shipped-ideas.yaml lines 76-106); ledger DQ1=shipped 'PR #102 (spec 054 dq-signal-interpretation-note; templates/handoff/dq-signal-interpretation.md)'
- **LVR Live-validation evidence recorder (validate.py Findings -> readiness-status block)** -- SHIPPED. Ground row 'kraken batch SL1/AL1/S7A/DQ1/FHC/LVR/ADL (38 -> 40 rules)' SHIPPED, evidence 'src/retail/readiness_evidence.py present (LVR)'; ledger LVR=shipped 'PR #105 (spec 057 live-validation-evidence-recorder; src/retail/readiness_evidence.py)'
- **Model-Wide KPI Answerability Rollup Model-Wide KPI Answerability Rollup** -- SETTLED (rejected). Prior REJECT verdict, nothing changed: portfolio verdict line 24 rejects it 'outright, both on thin/duplicative value' (no Ground ship_status row); rejected-settled
- **Doc-Count & Single-Source Drift Guard Doc-Count & Single-Source Drift Guard (constitution/capability/pipeline dupes)** -- SETTLED (rejected). Prior REJECT verdict, nothing changed: portfolio verdict line 24 rejects it 'outright, both on thin/duplicative value' (no Ground ship_status row); rejected-settled
- **A1 Machine-Checkable Route Registry + Planned/Built Status Manifest** -- SHIPPED. Ground row 'idea-bank execution sequence A1/B2/B1/F7/F8' SHIPPED (shipped-ideas.yaml lines 22-42; A1/B1 registered in rules); ledger A1=shipped 'PR #62 abbbd73'; appendix: src/retail/rules/routes.py + docs/routing/routes.yaml
- **B1 Never-Execute Invariant Guard (NX rule family)** -- SHIPPED. Ground row 'idea-bank execution sequence A1/B2/B1/F7/F8' SHIPPED (shipped-ideas.yaml lines 22-42; A1/B1 registered); ledger B1=shipped 'PR #64 9d12589'; appendix: src/retail/rules/never_execute.py
- **B2 Gate Observability Ledger - Structured Findings Emission (JSON/SARIF)** -- SHIPPED. Ground row 'idea-bank execution sequence A1/B2/B1/F7/F8' SHIPPED; ledger B2=shipped 'PR #63 ca1431c (json only; SARIF half not shipped)'; Ground 'retail check JSON output' SHIPPED src/retail/runner.py (run_json). SARIF half not cited as shipped
- **F7 Decision-Question Index per KPI Domain** -- SHIPPED. Ground row 'idea-bank execution sequence A1/B2/B1/F7/F8' SHIPPED; ledger F7=shipped 'PR #65 ae471aa'; appendix: skills/retail-kpi-knowledge/domains/*.md (11 domain files)
- **F8 Per-Table KPI Coverage Scorecard** -- SHIPPED. Ground row 'idea-bank execution sequence A1/B2/B1/F7/F8' SHIPPED; ledger F8=shipped 'PR #66 9d782f8'; appendix: skills/retail-kpi-knowledge/references/kpi-coverage-scorecard-template.md
- **F5 Demand-Signal Forecast Pack (sell-through + reorder exception)** -- SETTLED (rejected). ledger F5=settled 'idea-backlog.md PARK -- ML/forecasting out-of-scope boundary'; appendix rationale: 'Flagged for the ML/forecasting hard line, and the flag holds ... the naming (forecast pack, demand-signal) sits on the out-of-scope ML boundary.'
- **F6 Anomaly-Baseline Contracts (statistical exception flags)** -- SETTLED (rejected). ledger F6=settled 'idea-backlog.md REJECT -- ML/statistical out-of-scope hard principle'; appendix rationale: 'Ineligible as framed against the ML/statistical out-of-scope principle ... the deterministic L3/L4 verification machinery ... cannot check it without becoming a stats engine.'
- **DF1 Parked-On Map / dependency-edge reconciler** -- SHIPPED. Ground row 'idea-loop A3/B3/PP1/SC1/DF1' SHIPPED (shipped-ideas.yaml lines 54-74; PR #99; all five registered); ledger DF1=shipped 'PR #99 (spec 051 parked-on-map)'; appendix: src/retail/rules/parked_on.py @register DF1

## Run health & self-metrics

- Yield: 53 raw -> 42 scored (ADOPT 14, CONSIDER 9, PARK 14, REJECT 2).
- Eligibility-rejection rate: 0%.
- Consistency: 19 consistent, 18 minor-tension, 5 conflict.
- Verification: 20 survived, 18 weakened, 4 killed.
- Layer coverage: bi-sql 4, retail-kpi 14, bi-python 3, none 11, bi-dax 5, bi-bigdata 3, docs-spine 2
- Run health: all lenses reported.
