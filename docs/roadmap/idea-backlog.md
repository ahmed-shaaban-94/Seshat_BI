# Seshat BI -- Idea Bank

> **This is a future-idea bank, not a roadmap and not a commitment.** Nothing here is
> planned, scheduled, or approved. It is exploratory brainstorming output to browse for
> inspiration. The authoritative roadmap is `docs/roadmap/roadmap.md` (F-numbered features).

_Re-running regenerates this file as a HISTORY-AWARE snapshot: each run still re-reasons every
_idea from scratch, but it now READS the prior bank and Ground's verified ship-status first, so
_an idea that has since SHIPPED is recorded as shipped (with its evidence) and a settled rejection
_is not re-litigated. This is still an idea BANK, not an evolving plan: nothing here is promoted,
_scheduled, or committed. A SHIPPED tag only records that a human already took an equivalent idea
_through the normal feature process -- the engine never moves an idea onto the roadmap itself. The
_verdicts and scores are an automated reviewer's triage opinion. An idea advances only through the
_normal spec/feature process, with a human decision._

_Generated by the `idea-engine` workflow. Generated on 2026-07-08._

**7 ideas scored** (generated across 3 rounds; raw pre-dedupe 107). Verdicts: ADOPT 0, CONSIDER 3, PARK 3, REJECT 1. Horizon: NOW 5, HORIZON 2.

## Your Ideas (expanded + reviewed)

_Your own idea(s), grown from your words into a reviewable shape and run through the same
skeptic + reviewer panel as every other idea. Each shows how the engine READ your words (so
you can catch a misread), the verdict, and V/F scores. The verdict is a triage opinion, never
a decision to build. A rejected idea stays here (not hidden below) with its reason -- and gets
a steelman note in the Rescue section if a narrower eligible version exists._

- **retail drift -- the missing runtime seam for the F014 source-drift taxonomy (PROTECTED user idea)** -- CONSIDER - `NOW` - V8 / F6 - respects principles
  - Your words: "Focus on added-value features for Seshat BI"
  - Read as: I read this broad directive (which is verbatim the run's FOCUS line, not a hidden specific feature request) as: select and shape the single highest-added-value NET-NEW capability that serves a real human (operator/end-user) rather than adding yet another internal self-check, sourced from a gap the repo already names.
  - Other readings not taken (tell me if I misread you): That 'added-value' meant a NEW governance/self-check rule or reconciler (the saturated tool-internal category) -- rejected because that category is already dense with shipped work and reads as ignoring the 'added-value / for users' emphasis.; That it meant maturing the least-complete knowledge layer, bi-python-knowledge (3 live vs ~14 planned files) -- a real gap, but that is content-backfill of an existing planned layer, which reads more as 'finish what's designed' than as a new value-adding FEATURE.; That it meant unblocking F016 (the deferred Power BI execution adapter) -- rejected because it is deliberately gated by hard rule #6 (no execution before Semantic Model Ready) and would require an executor, so it cannot be the eligible 'add a feature now' reading.; That it was a literal instruction to me to keep refactoring CodeScene hotspots on this branch (its stated purpose) -- rejected because this task is idea-interpretation for the idea bank, and refactoring is neither a user-facing feature nor an idea to shape.

## Reviewer portfolio verdict

> Six ideas cleared eligibility with no principle-based rejections. One idea -- the silver/gold migration authoring aid -- was rejected outright on value/feasibility grounds (all four reviewers scored it 2, unanimous REJECT). Of the remainder, two reach CONSIDER with real strength: the retail drift runtime seam for F014 (value 8, feasibility 6) is the standout, with two of four reviewers pushing for outright ADOPT on the strength of pre-specced taxonomy scaffolding, while the Approver Decision Surface (value 6, feasibility 7) draws unanimous CONSIDER with only minor value-score disagreement. The Personal-Data-Touch Notice also lands at CONSIDER (value 5, feasibility 8) despite one dissenting PARK vote from the Value/Feasibility Realist, who discounts it for resting on deferred groundwork. Three ideas settle at PARK: the Publish-Readiness Preflight Card (unanimous, blocked on deferred F016 execution), the F010 contract-vs-model fixture shape (unanimous PARK but with a wide feasibility spread -- auditors see it as a cheap static artifact, the Realist sees it as tied to a still-deferred check), and Kit Surface Budget (mostly PARK, with one dissent to CONSIDER after the Shipped-Duplication Auditor found concrete overlap with the shipped E7 doctor's stale-route detection). Practical read: prioritize retail drift and the Approver Decision Surface for near-term work; treat the PII notice as a lower-cost next candidate; leave the three PARKed ideas for later unless the deferred F016/F010 dependencies resolve first.

## Legend

- **Verdict** (reviewer's *triage opinion only* -- not a decision to build) -- ADOPT (worth a closer look first; eligible, consistent, high value) - CONSIDER (interesting; needs a decision or dependency) - PARK (horizon / later) - REJECT (ineligible or conflicts -- kept for the record).
- **Horizon** -- `NOW` (fits the repo today) - `HORIZON` (future vision).
- **Eligibility** -- respects all hard principles, or violates one (named in the rationale).
- **V / F** -- value / feasibility (1-10), reviewer-assigned.
- **SHIPPED / SETTLED** -- a prior idea Memory matched to shipped work or a settled rejection; kept for the record, not an open candidate.

## Design Foundation

_A first-class CATEGORICAL cohort of the Power BI presentation FOUNDATION layer
(`strengthens_layer = design-system`): theme-token contracts, background/canvas
conventions, layout blueprints, accessibility/contrast, design-review evidence --
governance of the design layer, never report authoring. This is a cross-reference
grouping, not a re-score: each idea keeps its existing verdict and is detailed in
its verdict section above. No numeric score is attached here (roadmap hard rule #9),
and the lane never promotes an idea onto the roadmap or assigns an F-row (Principle V)._

_(No design-foundation ideas in this run. The lane stays present as a first-class cohort.)_

## CONSIDER

### Personal-Data-Touch Notice -- answer-level PII trust signal (MERGED reader-facing PII disclosure)

`NOW` - **V5 / F8** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY ONLY against the named hard-stops: never_execute (B1/B3), no gate bypass, never fabricate confidence (#9), never_self_grant_approval (Principle V), and no C086-specific coupling. I presume ineligible until an idea proves it touches only reasoning/static/read-only seams, and I refuse to manufacture an ineligibility_reason where I cannot name a violated principle. I verified the bar against source: never_execute.py/live_surface_boundary.py confine live DB touches to the four live-surface modules; source-drift.md forbids a rolled-up drift score and keeps grain/PII/returns as Principle-V human seams; approval-console (F027) owns the write-back; metric-contract-store confirms F010 is a deferred void; PII flags (pii:true/false + RC4) are recorded per column; retail doctor (E7) is a shipped read-only advisory digest. Value/feasibility scores here are a triage courtesy weighed from the skeptic; my load-bearing output is the eligible bool and the named-principle reason.] Eligibility is clean against every hard-stop: it READS committed pii: flags (confirmed recorded per column in source-map.yaml as pii:true/false) plus the RC4 disposition in assumptions.md/unresolved-questions.md, invents no meaning, emits NO score (#9 holds), adds NO gate, opens no connection (B1/B3 untouched), and self-grants nothing (Principle V n/a). Not C086-specific (generic table-parameterized composer). Verified it does not duplicate F040 consumer-data-dictionary: that skill composes MEANING (what a column/metric means), explicitly scoped away from a trust/disclosure signal. It follows the shipped answerability-summary OPTIONAL-companion precedent. Value is genuinely shadowed -- the backlog PARKed the H11 reader digest of this shape as low-value -- which pulls value down but never touches eligibility. [SHIPPED-DUPLICATION AUDITOR -- I judge each candidate for CONSISTENCY with shipped work using the ship-status table + known history. I penalize an idea that merely RESTATES a shipped feature (via consistency=conflict/minor-tension + low value_score + PARK/REJECT), but I do NOT penalize a genuine extension that builds ON shipped work AS LONG AS it names the shipped feature it extends. I hold settled-rejected history (the F5/F6 stats-engine line) to a materially-new bar. Eligibility is the hard-principle auditor's lane, not mine -- the skeptic ruled none of these seven principle-ineligible, so I carry eligible=true / ineligibility_reason='' across the board, and I carry horizon/origin/serves/strengthens_layer/survived_verification unchanged from the candidate + skeptic blocks. I opened load-bearing evidence myself: consumer-data-dictionary/SKILL.md (F040 composes MEANING, explicitly scoped away from a trust/disclosure signal -- so the PII notice is an extension, not a duplicate); approval-console/SKILL.md (F027 WRITES the recorded decision into unresolved-questions.md + readiness-status.yaml approvals[] -- a write-loop, so the read-only Approver reorder is distinct); doctor.py (E7 aggregates A1 routes + A3 route-coverage + SC1 status-claims + a load-bearing-doc probe -- Kit Surface Budget's orphan/stale detection overlaps this heavily); publish_pack.py (PP1 checks the handoff pack's own six-section completeness -- the Publish Preflight reuses its semantics and re-treads E7's aggregator pattern); metric-contract-store.md (F010/011 contract-vs-model check is DEFERRED, 'nothing here reads powerbi/<Model>.SemanticModel/'); metric_drift.py (a DAX measure-drift checker -- NOT source-profile drift, so retail drift has no shipped duplicate); source-drift.md (F014 is a fully-specced 9-class taxonomy that has ALREADY pre-empted the H5/F5/F6 stats-engine objection: measured before/after magnitudes + status + blockers allowed, rolled-up drift SCORE forbidden, grain/identity/returns/PII are Principle-V seams it measures but never re-judges); and idea-backlog.md (H11 answerability digest was PARKed as low-value -- shadows the PII notice's value, not its eligibility; APPROVE-adjacency is a standing eligibility SPLIT the panel keeps flagged on C1/H8/J1 but is the hard-principle lane, not mine).] NOT a duplicate of SHIPPED F040 consumer-data-dictionary: I opened its SKILL.md and it composes a MEANING reference (what a column/measure means), explicitly scoped AWAY from a trust/disclosure signal -- so this answer-level PII-touch disclosure is a genuine sibling, not a restatement, and it names F040 as the neighbour it differs from. It follows the SHIPPED answerability-summary OPTIONAL-companion precedent (non-gating, invents no meaning, emits no score, adds no gate). The one dent from my seat is value, not consistency: the PARKed H11 answerability-digest (a reader-facing digest of similar shape) was judged low-value, and reading committed pii: flags + RC4 disposition is a thin composition -- so I mark minor-tension and cap value at 6. Survived the skeptic clean on eligibility. [Value/Feasibility Realist -- I judge whether "feasible" is hiding a missing gold source, a runtime consumer that does not yet exist, or an unmade human ruling. I reward genuine analytical/operator value and discount ideas whose feasibility rests on something deferred (notably F016 execution and F010 contract-vs-model). I opened source-drift.md, publish_pack.py, doctor.py, consumer-data-dictionary/SKILL.md, approval-console/SKILL.md, metric-contract-store.md, and the real source-map.yaml pii: flags to ground every score.] Feasibility is genuinely high: the mechanism is real and confirmed -- pii: true/false is recorded per column in source-map.yaml (retail_store_sales flags customer-facing columns), with the governance-Q1 disposition noted inline, and a pure-prose composer that GAP-marks undecided columns invents no meaning, emits no score, adds no gate. It is NOT a duplicate of F040 consumer-data-dictionary -- that skill's own text says its dictionary composes MEANING and that the handoff data-dictionary 'exists to satisfy the Publish Ready gate, not to serve the analyst'; a trust/disclosure signal is a distinct audience/mechanism. It follows the shipped answerability-summary OPTIONAL-companion precedent, so eligibility is clean. But value is the weak axis: the backlog already PARKed the H11 reader-facing digest of this exact shape as low-value, and a one-line disclosure sentence is a thin end-user convenience over evidence a determined reader could already find. Easy to build is not the same as worth building; the value shadow, not any principle, is what holds it at PARK. [DESIGN-FOUNDATION reviewer. My eligibility bar is surface-blending / F016 execution: an idea is INELIGIBLE only if it authors a PBIP/PBIR, generates DAX, invents a metric, or bakes data into a static background/theme. Critically, NONE of these eight is a design/presentation idea -- not one is tagged strengthens_layer=design-system, and the one design-adjacent sibling (the designer-lens "Design-review reading-path blueprint") was explicitly split OUT of the approver surface (#2). So my high-confidence contribution is exactly two things: (1) the four-surface eligibility screen, on which all eight pass except a close call on #5, and (2) honest, conservative deferral on the non-design majority with a low-confidence flag, per my "defer rather than distort the panel" instruction. I do NOT import the skeptic's axes (APPROVE-adjacency, YAGNI) as design ineligibility -- those are governance/scope concerns owned by other lenses; I carry the skeptic's survived_verification disposition and let his concerns land in value_score, but I score eligibility purely on surface-blending. I deliberately do not mirror the skeptic across all eight, which would make the design seat redundant in the median. Verdicts skew CONSIDER/PARK, with REJECT on the skeptic-killed #4; no ADOPT, because none of these gives me a design-merit case that would justify an ADOPT from this seat.] Design-eligible cleanly: a read-only prose composer over committed pii: flags + recorded RC4 disposition; authors no PBIP/PBIR, generates no DAX, invents no metric, bakes no data into a theme -- it emits a fixed disclosure sentence and GAP-marks undecided columns. From a presentation-hygiene view it is genuinely reader-facing (a decision-maker's trust signal), follows the shipped answerability-summary OPTIONAL-companion precedent (spec 053, non-gating), and I confirmed consumer-data-dictionary (F040) composes MEANING not a trust/disclosure signal, so not a duplicate. Value is capped: this is a docs-spine one-liner, not a durable design seam, and the skeptic's H11 low-value shadow on reader digests is a real value drag. Serves end_user, which is the healthy side of the serves signal.

**Panel dissent:** Three reviewers land on CONSIDER (v5-6/f7-8), but the Value/Feasibility Realist dissents to PARK (v4/f8), discounting the idea's value because it leans on deferred groundwork rather than a runtime consumer that exists today.

**First step:** Author a fixed-form disclosure template that reads committed pii: flags + their recorded RC4 disposition for one table and renders one disclosure sentence per answer surface, GAP-marking any column with no recorded decision; no classification engine, no score.

### Approver Decision Surface -- ordered refutation-first 'what am I signing and why is it safe' reader view (3-lens convergence)

`NOW` - **V6 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY ONLY against the named hard-stops: never_execute (B1/B3), no gate bypass, never fabricate confidence (#9), never_self_grant_approval (Principle V), and no C086-specific coupling. I presume ineligible until an idea proves it touches only reasoning/static/read-only seams, and I refuse to manufacture an ineligibility_reason where I cannot name a violated principle. I verified the bar against source: never_execute.py/live_surface_boundary.py confine live DB touches to the four live-surface modules; source-drift.md forbids a rolled-up drift score and keeps grain/PII/returns as Principle-V human seams; approval-console (F027) owns the write-back; metric-contract-store confirms F010 is a deferred void; PII flags (pii:true/false + RC4) are recorded per column; retail doctor (E7) is a shipped read-only advisory digest. Value/feasibility scores here are a triage courtesy weighed from the skeptic; my load-bearing output is the eligible bool and the named-principle reason.] Eligible ONLY BECAUSE it writes nothing and self-grants nothing: refutation-first ordering is PRESENTATION, not decision-resolution, so never_self_grant_approval (Principle V) holds absolutely and F027 approval-console retains the write-back (verified: F027 WRITES the recorded human answer into unresolved-questions.md + readiness-status.yaml -- a write-loop, mechanically distinct from this read-only reorderer, so not a duplicate). It reads already-committed evidence only (B1/B3 untouched), emits no score (#9), is not C086-specific. The minor-tension: re-sequencing evidence 'refusal-first' for the human signer sits on the exact read-only-vs-decision-resolving line the backlog flagged as an eligibility SPLIT for high-scoring ideas -- eligible with an explicit read-only scope statement, which the pitch supplies ('records nothing, self-grants nothing, moves no stage'). Weakened per skeptic on that adjacency, not killed. [SHIPPED-DUPLICATION AUDITOR -- I judge each candidate for CONSISTENCY with shipped work using the ship-status table + known history. I penalize an idea that merely RESTATES a shipped feature (via consistency=conflict/minor-tension + low value_score + PARK/REJECT), but I do NOT penalize a genuine extension that builds ON shipped work AS LONG AS it names the shipped feature it extends. I hold settled-rejected history (the F5/F6 stats-engine line) to a materially-new bar. Eligibility is the hard-principle auditor's lane, not mine -- the skeptic ruled none of these seven principle-ineligible, so I carry eligible=true / ineligibility_reason='' across the board, and I carry horizon/origin/serves/strengthens_layer/survived_verification unchanged from the candidate + skeptic blocks. I opened load-bearing evidence myself: consumer-data-dictionary/SKILL.md (F040 composes MEANING, explicitly scoped away from a trust/disclosure signal -- so the PII notice is an extension, not a duplicate); approval-console/SKILL.md (F027 WRITES the recorded decision into unresolved-questions.md + readiness-status.yaml approvals[] -- a write-loop, so the read-only Approver reorder is distinct); doctor.py (E7 aggregates A1 routes + A3 route-coverage + SC1 status-claims + a load-bearing-doc probe -- Kit Surface Budget's orphan/stale detection overlaps this heavily); publish_pack.py (PP1 checks the handoff pack's own six-section completeness -- the Publish Preflight reuses its semantics and re-treads E7's aggregator pattern); metric-contract-store.md (F010/011 contract-vs-model check is DEFERRED, 'nothing here reads powerbi/<Model>.SemanticModel/'); metric_drift.py (a DAX measure-drift checker -- NOT source-profile drift, so retail drift has no shipped duplicate); source-drift.md (F014 is a fully-specced 9-class taxonomy that has ALREADY pre-empted the H5/F5/F6 stats-engine objection: measured before/after magnitudes + status + blockers allowed, rolled-up drift SCORE forbidden, grain/identity/returns/PII are Principle-V seams it measures but never re-judges); and idea-backlog.md (H11 answerability digest was PARKed as low-value -- shadows the PII notice's value, not its eligibility; APPROVE-adjacency is a standing eligibility SPLIT the panel keeps flagged on C1/H8/J1 but is the hard-principle lane, not mine).] NOT a duplicate of the shipped approval trio, and it names them: I opened approval-console/SKILL.md (F027) -- it WRITES the recorded human decision back into unresolved-questions.md + readiness-status.yaml approvals[] (a write-loop); J1/F035 ASSEMBLE or TRANSCRIBE evidence. This candidate is a read-only RE-SEQUENCER that reorders already-committed blockers/approvals[]/unresolved-questions refutation-first and writes NOTHING (F027 keeps the write), so it is mechanically distinct from all three -- a genuine extension, not a restatement. 3-lens convergence is a real value signal. From my seat consistency is minor-tension (heavy adjacency to the shipped approval surfaces) and the skeptic WEAKENED it (it sits on the APPROVE-adjacency line the panel keeps flagged on C1/H8/J1 pending a one-line read-only scope statement) -- that eligibility flag is the hard-principle auditor's lane, so I carry eligible=true and let weakened cap the verdict below ADOPT. [Value/Feasibility Realist -- I judge whether "feasible" is hiding a missing gold source, a runtime consumer that does not yet exist, or an unmade human ruling. I reward genuine analytical/operator value and discount ideas whose feasibility rests on something deferred (notably F016 execution and F010 contract-vs-model). I opened source-drift.md, publish_pack.py, doctor.py, consumer-data-dictionary/SKILL.md, approval-console/SKILL.md, metric-contract-store.md, and the real source-map.yaml pii: flags to ground every score.] Real value: the human APPROVER standpoint is a genuine 3-lens blind-spot fill -- every shipped approval artifact (J1, F027 approval-console, F035) ASSEMBLES or TRANSCRIBES evidence; none composes an ordered, refutation-first plain-language reading view for the signer. I confirmed F027's skill WRITES the recorded decision back into unresolved-questions.md + readiness-status.yaml (a write-loop), so a read-only re-sequencing composer that writes nothing is mechanically distinct -- not a duplicate. never_self_grant_approval holds because F027 owns the write and this composer records nothing, so it stays eligible. The minor-tension is real, not fatal: ordering 'what would make me refuse to sign' first sits on the APPROVE-adjacency line the backlog flagged as needing a tight read-only scope statement (same clarification demanded of A1/C1/A3). Value survives that; hence CONSIDER, weakened, not ADOPT -- it needs the scope line before it could ever be promoted, and I never promote. [DESIGN-FOUNDATION reviewer. My eligibility bar is surface-blending / F016 execution: an idea is INELIGIBLE only if it authors a PBIP/PBIR, generates DAX, invents a metric, or bakes data into a static background/theme. Critically, NONE of these eight is a design/presentation idea -- not one is tagged strengthens_layer=design-system, and the one design-adjacent sibling (the designer-lens "Design-review reading-path blueprint") was explicitly split OUT of the approver surface (#2). So my high-confidence contribution is exactly two things: (1) the four-surface eligibility screen, on which all eight pass except a close call on #5, and (2) honest, conservative deferral on the non-design majority with a low-confidence flag, per my "defer rather than distort the panel" instruction. I do NOT import the skeptic's axes (APPROVE-adjacency, YAGNI) as design ineligibility -- those are governance/scope concerns owned by other lenses; I carry the skeptic's survived_verification disposition and let his concerns land in value_score, but I score eligibility purely on surface-blending. I deliberately do not mirror the skeptic across all eight, which would make the design seat redundant in the median. Verdicts skew CONSIDER/PARK, with REJECT on the skeptic-killed #4; no ADOPT, because none of these gives me a design-merit case that would justify an ADOPT from this seat.] Design-eligible: it blends no surfaces -- a read-only composer that RE-ORDERS already-committed readiness evidence into a decision-first reading order and writes nothing (I confirmed F027 approval-console owns the write-loop, so this is mechanically distinct, not a duplicate). The skeptic's APPROVE-adjacency concern is a governance/never-self-grant axis, NOT surface-blending, so it is not my ineligibility_reason; I carry weakened and let it depress value. As a pure presentation/reading-order artifact this is squarely in the reader-experience space, and refutation-first ordering (unresolved human seams first, reassurance last) is a real information-hierarchy contribution. But value is limited by the strong overlap with shipped approval material (J1/F027/F035) and the still-open read-only scope clarification the skeptic flagged. strengthens_layer=none, serves end_user.

**Panel dissent:** All four reviewers converge on CONSIDER; the only friction is a value-score spread (5-7), with the Value/Feasibility Realist most bullish (v7) and the Design-Foundation reviewer most conservative (v5) -- a difference in emphasis, not a verdict split.

**First step:** Write a one-line read-only scope contract ('this surface reorders committed evidence, writes nothing, grants nothing') then prototype the ordering over readiness-status.yaml blockers + approvals[] slots + unresolved-questions.md, with a standing 'nothing here grants approval' banner.

### retail drift -- the missing runtime seam for the F014 source-drift taxonomy (PROTECTED user idea)

`NOW` - **V8 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY ONLY against the named hard-stops: never_execute (B1/B3), no gate bypass, never fabricate confidence (#9), never_self_grant_approval (Principle V), and no C086-specific coupling. I presume ineligible until an idea proves it touches only reasoning/static/read-only seams, and I refuse to manufacture an ineligibility_reason where I cannot name a violated principle. I verified the bar against source: never_execute.py/live_surface_boundary.py confine live DB touches to the four live-surface modules; source-drift.md forbids a rolled-up drift score and keeps grain/PII/returns as Principle-V human seams; approval-console (F027) owns the write-back; metric-contract-store confirms F010 is a deferred void; PII flags (pii:true/false + RC4) are recorded per column; retail doctor (E7) is a shipped read-only advisory digest. Value/feasibility scores here are a triage courtesy weighed from the skeptic; my load-bearing output is the eligible bool and the named-principle reason.] Eligible against every hard-stop, and I applied the same standard as any engine idea (no inflation for being the human's). Verified against source-drift.md: it operates ONLY on RECORDED profiles (committed baseline + a re-profiled snapshot) -- retail drift does NOT itself open a DB; live re-profiling stays in the retail-validate live-surface path (B1/B3 hold, no static/reasoning path opens a connection). It emits measured magnitudes + class + status + blockers and the doc FORBIDS a rolled-up drift score (#9 holds -- a magnitude like 'missingness 3.1%->11.7%' is a MEASUREMENT, allowed; a 'drift score' is forbidden). grain/identity/returns/PII drift are Principle-V human seams the detector MEASURES and CLASSIFIES but NEVER auto-rejudges (Principle V holds). It ADDS a gate rule via the retail check wiring -- that is normal registration, not a bypass or self-grant. Not C086-specific. Relitigation vs the REJECTED-settled F5/F6 stats-engine zone: materially-new -- it measures magnitude + class and forbids a score, and never supplies a tolerance verdict on the Principle-V classes, so it does not smuggle a stats engine (the H5 ineligibility trap is pre-empted by the doc). prior_status=open-prior: F014 is a planned/deferred roadmap item with a fully-specced taxonomy and zero runtime seam, not new-from-scratch. The minor-tension pulling verdict to CONSIDER not ADOPT: source-drift.md brands the runtime a DEFERRED 'Later'-tier seam and repo CLAUDE.md says 'add the seam, not the implementation' -- building it now is proposing the deferred implementation. Idea-bank-appropriate and the strongest survivor, but the deferred-tier tension keeps it at CONSIDER. [SHIPPED-DUPLICATION AUDITOR -- I judge each candidate for CONSISTENCY with shipped work using the ship-status table + known history. I penalize an idea that merely RESTATES a shipped feature (via consistency=conflict/minor-tension + low value_score + PARK/REJECT), but I do NOT penalize a genuine extension that builds ON shipped work AS LONG AS it names the shipped feature it extends. I hold settled-rejected history (the F5/F6 stats-engine line) to a materially-new bar. Eligibility is the hard-principle auditor's lane, not mine -- the skeptic ruled none of these seven principle-ineligible, so I carry eligible=true / ineligibility_reason='' across the board, and I carry horizon/origin/serves/strengthens_layer/survived_verification unchanged from the candidate + skeptic blocks. I opened load-bearing evidence myself: consumer-data-dictionary/SKILL.md (F040 composes MEANING, explicitly scoped away from a trust/disclosure signal -- so the PII notice is an extension, not a duplicate); approval-console/SKILL.md (F027 WRITES the recorded decision into unresolved-questions.md + readiness-status.yaml approvals[] -- a write-loop, so the read-only Approver reorder is distinct); doctor.py (E7 aggregates A1 routes + A3 route-coverage + SC1 status-claims + a load-bearing-doc probe -- Kit Surface Budget's orphan/stale detection overlaps this heavily); publish_pack.py (PP1 checks the handoff pack's own six-section completeness -- the Publish Preflight reuses its semantics and re-treads E7's aggregator pattern); metric-contract-store.md (F010/011 contract-vs-model check is DEFERRED, 'nothing here reads powerbi/<Model>.SemanticModel/'); metric_drift.py (a DAX measure-drift checker -- NOT source-profile drift, so retail drift has no shipped duplicate); source-drift.md (F014 is a fully-specced 9-class taxonomy that has ALREADY pre-empted the H5/F5/F6 stats-engine objection: measured before/after magnitudes + status + blockers allowed, rolled-up drift SCORE forbidden, grain/identity/returns/PII are Principle-V seams it measures but never re-judges); and idea-backlog.md (H11 answerability digest was PARKed as low-value -- shadows the PII notice's value, not its eligibility; APPROVE-adjacency is a standing eligibility SPLIT the panel keeps flagged on C1/H8/J1 but is the hard-principle lane, not mine).] The strongest survivor and the clear ADOPT from my seat. NO shipped duplicate: I confirmed metric_drift.py is a DAX measure-drift checker (contract-vs-DAX), NOT source-profile drift, and F014 source-drift.md is PLANNED design with zero runtime ('There is NO retail drift CLI and NO comparator in src/retail'). It is a genuine EXTENSION that NAMES all three shipped/planned rails it builds on: SHIPPED B2 structured-findings emission (JSON/SARIF), the SHIPPED retail check gate-registration choreography, and the PLANNED F014 9-class taxonomy it finally consumes. On the settled-rejection bar (F5/F6 stats-engine, and PARKed H5): relitigation=materially-new -- source-drift.md has ALREADY pre-empted the H5 objection with two firm rules: (1) the report carries MEASURED before/after magnitudes + status + blockers, a rolled-up drift SCORE is FORBIDDEN (a magnitude like 'missingness 3.1%->11.7%' is a measurement, not a threshold verdict); (2) grain/identity/returns/PII drift are Principle-V human seams the detector MEASURES and CLASSIFIES but NEVER auto-rejudges. So unlike H5's human-declared bands, no threshold semantics are smuggled in -- it measures, it does not judge. It operates only on RECORDED profiles (committed baseline + re-profiled snapshot), so no reasoning/static path opens a DB (B1/B3 hold; live re-profiling stays in retail-validate). Fills a verbatim ground-map gap and serves the operator directly. Scored by the same standard as any engine idea; user origin neither inflated nor docked. [Value/Feasibility Realist -- I judge whether "feasible" is hiding a missing gold source, a runtime consumer that does not yet exist, or an unmade human ruling. I reward genuine analytical/operator value and discount ideas whose feasibility rests on something deferred (notably F016 execution and F010 contract-vs-model). I opened source-drift.md, publish_pack.py, doctor.py, consumer-data-dictionary/SKILL.md, approval-console/SKILL.md, metric-contract-store.md, and the real source-map.yaml pii: flags to ground every score.] The clear top of the set and it earns the score on merit, not protection -- I scored it by the same standard as any engine idea. It fills a VERBATIM ground-map gap: source-drift.md is a fully-specced 9-class taxonomy with zero runtime seam and NO retail drift CLI, and it serves the OPERATOR directly, not tool-internal self-checking -- the kind of net-new external value my standpoint rewards. I opened source-drift.md and confirmed it has already pre-empted the H5/F5/F6 stats-engine objection: two firm rules govern the whole taxonomy -- a rolled-up drift 'score' is FORBIDDEN (a measured before/after magnitude like 'missingness 3.1%->11.7%' is a MEASUREMENT and allowed), and grain/identity/returns/PII drift are Principle-V human seams the detector MEASURES and CLASSIFIES but NEVER auto-rejudges. So cardinality-shift emits a warning plus the measured numbers, not a threshold verdict -- it measures, it does not judge. It operates ONLY on RECORDED profiles (committed baseline + a re-profiled snapshot), so no reasoning/static path opens a DB (B1/B3 hold); live re-profiling stays in the retail-validate surface. It reuses shipped B2 structured-findings emission and the retail check wiring choreography. Feasibility is a realistic 6, not a 9: unlike the read-only prose composers, this needs a genuine net-new comparator module plus a CLI subcommand plus the known 4-5-place wiring (defended by SC2 + scaffold --doctor) -- real code, more than a template. relitigation=materially-new because the doc's forbid-score + measure-not-judge design is a genuinely different mechanism from the settled F5/F6/H5 stats-engine rejection zone; this does NOT reopen that ruling, it stays on the safe side of it. prior_status=open-prior since F014 is a documented-but-unbuilt planned feature. ADOPT. [DESIGN-FOUNDATION reviewer. My eligibility bar is surface-blending / F016 execution: an idea is INELIGIBLE only if it authors a PBIP/PBIR, generates DAX, invents a metric, or bakes data into a static background/theme. Critically, NONE of these eight is a design/presentation idea -- not one is tagged strengthens_layer=design-system, and the one design-adjacent sibling (the designer-lens "Design-review reading-path blueprint") was explicitly split OUT of the approver surface (#2). So my high-confidence contribution is exactly two things: (1) the four-surface eligibility screen, on which all eight pass except a close call on #5, and (2) honest, conservative deferral on the non-design majority with a low-confidence flag, per my "defer rather than distort the panel" instruction. I do NOT import the skeptic's axes (APPROVE-adjacency, YAGNI) as design ineligibility -- those are governance/scope concerns owned by other lenses; I carry the skeptic's survived_verification disposition and let his concerns land in value_score, but I score eligibility purely on surface-blending. I deliberately do not mirror the skeptic across all eight, which would make the design seat redundant in the median. Verdicts skew CONSIDER/PARK, with REJECT on the skeptic-killed #4; no ADOPT, because none of these gives me a design-merit case that would justify an ADOPT from this seat.] Origin:user, carried unchanged and scored by the same bar -- neither inflated for being the human's own nor dismissed for being rough. Design-eligible: a static comparator over two RECORDED profiles authors no PBIP/PBIR/DAX/metric/theme and opens no DB (live re-profiling stays in retail-validate). This is an operator/runtime seam outside my design expertise, so I defer conservatively to the operator/technical lenses on core value and flag low design confidence. I did open source-drift.md to confirm eligibility on my own axis: the taxonomy forbids a rolled-up drift score (allows measured before/after magnitudes only) and treats grain/identity/returns/PII as Principle-V human seams the detector measures but never re-judges -- so it invents no meaning and bakes no verdict, clearing my bar. The skeptic's clean-survive (it pre-empts the H5 stats-engine objection, fills a verbatim ground-map gap: F014 has zero runtime seam, reuses shipped B2 findings + retail check wiring) is well-grounded. CONSIDER, deferring the operator-value magnitude to the owning lenses.

**Panel dissent:** The Shipped-Duplication Auditor and Value/Feasibility Realist both push to ADOPT (v8), citing F014's pre-specced taxonomy as ready-to-fill scaffolding, but the Hard-Principle Auditor (v7/f6) and Design-Foundation reviewer (v6/f6) hold the line at CONSIDER -- the aggregate settles on CONSIDER, short of the two ADOPT votes.

**First step:** Prototype a static comparator module that takes two already-RECORDED source profiles (committed baseline + a re-profiled snapshot) and classifies differences into the nine documented drift classes, emitting B2-style structured findings with measured before/after magnitudes and no rolled-up score; leave live re-profiling in retail-validate and wire the CLI subcommand through the known rule/CLI registration seam under SC2 + scaffold --doctor.

## PARK

### Publish-Readiness Preflight Card -- human-read non-executing preconditions before deferred F016

`NOW` - **V4 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY ONLY against the named hard-stops: never_execute (B1/B3), no gate bypass, never fabricate confidence (#9), never_self_grant_approval (Principle V), and no C086-specific coupling. I presume ineligible until an idea proves it touches only reasoning/static/read-only seams, and I refuse to manufacture an ineligibility_reason where I cannot name a violated principle. I verified the bar against source: never_execute.py/live_surface_boundary.py confine live DB touches to the four live-surface modules; source-drift.md forbids a rolled-up drift score and keeps grain/PII/returns as Principle-V human seams; approval-console (F027) owns the write-back; metric-contract-store confirms F010 is a deferred void; PII flags (pii:true/false + RC4) are recorded per column; retail doctor (E7) is a shipped read-only advisory digest. Value/feasibility scores here are a triage courtesy weighed from the skeptic; my load-bearing output is the eligible bool and the named-principle reason.] Eligible: hard rule #6 forbids EXECUTION, not a non-executing readiness read -- the publish stage EXISTS today (publish-ready.md + shipped PP1 publish_pack.py), so a read-only preflight opens no connection (B1/B3), wires no adapter (YAGNI seam-only), emits no score (#9), never self-grants the publish approval (Principle V). Not C086-specific. No manufactured ineligibility applies. The minor-tension and the value damage: its advertised 'every metric bound to gold not silver' precondition depends on the DEFERRED F010/011 contract-vs-model check (verified: metric-contract-store confirms nothing reads powerbi/<Model>.SemanticModel/) -- that slice reads a void and is infeasible/low-value today, and its non-void parts (secret-in-tracked-file scan, PP1 completeness, gold-only binding presence) overlap the SHIPPED retail doctor (E7) read-only digest. Eligible but shrunken; PARK. [SHIPPED-DUPLICATION AUDITOR -- I judge each candidate for CONSISTENCY with shipped work using the ship-status table + known history. I penalize an idea that merely RESTATES a shipped feature (via consistency=conflict/minor-tension + low value_score + PARK/REJECT), but I do NOT penalize a genuine extension that builds ON shipped work AS LONG AS it names the shipped feature it extends. I hold settled-rejected history (the F5/F6 stats-engine line) to a materially-new bar. Eligibility is the hard-principle auditor's lane, not mine -- the skeptic ruled none of these seven principle-ineligible, so I carry eligible=true / ineligibility_reason='' across the board, and I carry horizon/origin/serves/strengthens_layer/survived_verification unchanged from the candidate + skeptic blocks. I opened load-bearing evidence myself: consumer-data-dictionary/SKILL.md (F040 composes MEANING, explicitly scoped away from a trust/disclosure signal -- so the PII notice is an extension, not a duplicate); approval-console/SKILL.md (F027 WRITES the recorded decision into unresolved-questions.md + readiness-status.yaml approvals[] -- a write-loop, so the read-only Approver reorder is distinct); doctor.py (E7 aggregates A1 routes + A3 route-coverage + SC1 status-claims + a load-bearing-doc probe -- Kit Surface Budget's orphan/stale detection overlaps this heavily); publish_pack.py (PP1 checks the handoff pack's own six-section completeness -- the Publish Preflight reuses its semantics and re-treads E7's aggregator pattern); metric-contract-store.md (F010/011 contract-vs-model check is DEFERRED, 'nothing here reads powerbi/<Model>.SemanticModel/'); metric_drift.py (a DAX measure-drift checker -- NOT source-profile drift, so retail drift has no shipped duplicate); source-drift.md (F014 is a fully-specced 9-class taxonomy that has ALREADY pre-empted the H5/F5/F6 stats-engine objection: measured before/after magnitudes + status + blockers allowed, rolled-up drift SCORE forbidden, grain/identity/returns/PII are Principle-V seams it measures but never re-judges); and idea-backlog.md (H11 answerability digest was PARKed as low-value -- shadows the PII notice's value, not its eligibility; APPROVE-adjacency is a standing eligibility SPLIT the panel keeps flagged on C1/H8/J1 but is the hard-principle lane, not mine).] This is my strongest live duplication finding. The pitch EXPLICITLY reuses SHIPPED PP1 (publish_pack.py) semantics for pack completeness, and its non-void slices (secret-in-tracked-file scan, gold-only binding presence, doc-existence) re-tread the SHIPPED retail doctor E7 read-only-aggregator pattern -- I opened both. It names PP1 (good) but the aggregation is largely a re-composition of shipped reads. Worse, its headline 'every metric bound to gold not silver' precondition depends on the DEFERRED F010/011 contract-vs-model check (metric-contract-store.md confirms nothing reads the SemanticModel yet) -- that slice reads a void. Publish stage exists (publish-ready.md, PP1 shipped) so it is eligible and does not violate hard rule #6, but once the F010-dependent slice is removed and the E7/PP1 overlap is netted out, little net-new remains. Skeptic WEAKENED; from the duplication seat that plus the deferred-void slice lands it at PARK. [Value/Feasibility Realist -- I judge whether "feasible" is hiding a missing gold source, a runtime consumer that does not yet exist, or an unmade human ruling. I reward genuine analytical/operator value and discount ideas whose feasibility rests on something deferred (notably F016 execution and F010 contract-vs-model). I opened source-drift.md, publish_pack.py, doctor.py, consumer-data-dictionary/SKILL.md, approval-console/SKILL.md, metric-contract-store.md, and the real source-map.yaml pii: flags to ground every score.] The publish stage EXISTS today (publish-ready.md + shipped PP1 publish_pack.py), so a non-executing preflight read does not violate hard rule #6 (which forbids execution, not a readiness read) -- eligibility is clean. But my standpoint's core discount lands hard here: the headline 'every metric bound to gold not silver' precondition IS the DEFERRED F010/011 contract-vs-model check, and metric-contract-store.md states plainly 'Nothing here reads powerbi/<Model>.SemanticModel/' -- that slice reads a void with no runtime, so feasibility on the marquee promise is illusory. Strip it and the non-void remainder (secret-in-tracked-file scan, PP1 pack-completeness, gold-only-binding presence) largely overlaps the SHIPPED retail doctor E7 read-only digest, which I confirmed already aggregates route/coverage/status-claim checks plus a load-bearing-doc probe. What remains is a thin re-composition of shipped checks around a deferred core. The 'de-risk a DEFERRED feature from the DEFINE side' framing also collides with repo CLAUDE.md YAGNI ('add the seam, not the implementation; not unless explicitly requested'). PARK. [DESIGN-FOUNDATION reviewer. My eligibility bar is surface-blending / F016 execution: an idea is INELIGIBLE only if it authors a PBIP/PBIR, generates DAX, invents a metric, or bakes data into a static background/theme. Critically, NONE of these eight is a design/presentation idea -- not one is tagged strengthens_layer=design-system, and the one design-adjacent sibling (the designer-lens "Design-review reading-path blueprint") was explicitly split OUT of the approver surface (#2). So my high-confidence contribution is exactly two things: (1) the four-surface eligibility screen, on which all eight pass except a close call on #5, and (2) honest, conservative deferral on the non-design majority with a low-confidence flag, per my "defer rather than distort the panel" instruction. I do NOT import the skeptic's axes (APPROVE-adjacency, YAGNI) as design ineligibility -- those are governance/scope concerns owned by other lenses; I carry the skeptic's survived_verification disposition and let his concerns land in value_score, but I score eligibility purely on surface-blending. I deliberately do not mirror the skeptic across all eight, which would make the design seat redundant in the median. Verdicts skew CONSIDER/PARK, with REJECT on the skeptic-killed #4; no ADOPT, because none of these gives me a design-merit case that would justify an ADOPT from this seat.] Design-eligible: a non-executing readiness read authors no surface and opens no connection -- hard rule #6 forbids execution, not a preflight read, and I confirmed the publish stage exists today (publish-ready.md, PP1). Outside my design expertise, so I defer conservatively to the operator/architect lenses on core value. The skeptic's grounding matters here: its advertised 'every metric bound to gold not silver' precondition depends on the DEFERRED F010/011 check that has no runtime (metric-contract-store.md confirms nothing reads powerbi/), so that slice reads a void, and its non-void parts (secret-in-tracked-file scan, PP1 completeness, gold-only binding) overlap the shipped retail doctor E7 read-only digest I inspected. Weakened; parked rather than advanced. serves operator.

**Panel dissent:** Unanimous PARK across all four lenses; no verdict dissent, only a narrow 1-point feasibility spread reflecting how heavily each reviewer discounts the idea's dependency on the deferred F016 execution path.

**First step:** None.

### F010 contract-vs-model fixture shape -- author the deferred check's spec as a static artifact today

`HORIZON` - **V3 / F7** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY ONLY against the named hard-stops: never_execute (B1/B3), no gate bypass, never fabricate confidence (#9), never_self_grant_approval (Principle V), and no C086-specific coupling. I presume ineligible until an idea proves it touches only reasoning/static/read-only seams, and I refuse to manufacture an ineligibility_reason where I cannot name a violated principle. I verified the bar against source: never_execute.py/live_surface_boundary.py confine live DB touches to the four live-surface modules; source-drift.md forbids a rolled-up drift score and keeps grain/PII/returns as Principle-V human seams; approval-console (F027) owns the write-back; metric-contract-store confirms F010 is a deferred void; PII flags (pii:true/false + RC4) are recorded per column; retail doctor (E7) is a shipped read-only advisory digest. Value/feasibility scores here are a triage courtesy weighed from the skeptic; my load-bearing output is the eligible bool and the named-principle reason.] Eligible: it reads nothing live, checks nothing, adds no rule, emits no score (#9), and the illustrative TMDL fragment honors Principle III (generic, not a universal template) and is not C086-specific -- a pure static example artifact, no principle violated. The minor-tension is pure VALUE/YAGNI, not eligibility: its only consumer is the DEFERRED F010 (metric-contract-store verified: nothing reads powerbi/<Model>.SemanticModel/), so per repo CLAUDE.md 'add the seam, not the implementation ... not unless explicitly requested' it is a golden fixture with no reader until an uncommitted build starts -- weakest on the value axis. Eligible, weakened to a hold; PARK. [SHIPPED-DUPLICATION AUDITOR -- I judge each candidate for CONSISTENCY with shipped work using the ship-status table + known history. I penalize an idea that merely RESTATES a shipped feature (via consistency=conflict/minor-tension + low value_score + PARK/REJECT), but I do NOT penalize a genuine extension that builds ON shipped work AS LONG AS it names the shipped feature it extends. I hold settled-rejected history (the F5/F6 stats-engine line) to a materially-new bar. Eligibility is the hard-principle auditor's lane, not mine -- the skeptic ruled none of these seven principle-ineligible, so I carry eligible=true / ineligibility_reason='' across the board, and I carry horizon/origin/serves/strengthens_layer/survived_verification unchanged from the candidate + skeptic blocks. I opened load-bearing evidence myself: consumer-data-dictionary/SKILL.md (F040 composes MEANING, explicitly scoped away from a trust/disclosure signal -- so the PII notice is an extension, not a duplicate); approval-console/SKILL.md (F027 WRITES the recorded decision into unresolved-questions.md + readiness-status.yaml approvals[] -- a write-loop, so the read-only Approver reorder is distinct); doctor.py (E7 aggregates A1 routes + A3 route-coverage + SC1 status-claims + a load-bearing-doc probe -- Kit Surface Budget's orphan/stale detection overlaps this heavily); publish_pack.py (PP1 checks the handoff pack's own six-section completeness -- the Publish Preflight reuses its semantics and re-treads E7's aggregator pattern); metric-contract-store.md (F010/011 contract-vs-model check is DEFERRED, 'nothing here reads powerbi/<Model>.SemanticModel/'); metric_drift.py (a DAX measure-drift checker -- NOT source-profile drift, so retail drift has no shipped duplicate); source-drift.md (F014 is a fully-specced 9-class taxonomy that has ALREADY pre-empted the H5/F5/F6 stats-engine objection: measured before/after magnitudes + status + blockers allowed, rolled-up drift SCORE forbidden, grain/identity/returns/PII are Principle-V seams it measures but never re-judges); and idea-backlog.md (H11 answerability digest was PARKed as low-value -- shadows the PII notice's value, not its eligibility; APPROVE-adjacency is a standing eligibility SPLIT the panel keeps flagged on C1/H8/J1 but is the hard-principle lane, not mine).] NOT a duplicate -- I confirmed via metric-contract-store.md that F010/011 is DEFERRED and no fixture exists, so consistency is consistent and it names the F009/F010 split it honors. It is a pure static example (reads nothing live, checks nothing, no rule, no score; the illustrative TMDL fragment respects Principle III). But from the duplication + value seat its ONLY consumer is the DEFERRED F010: a golden fixture with no reader until an uncommitted build starts. High feasibility, but value is contingent on a feature nobody has committed to build -- the sharpest YAGNI collision in the set. Skeptic WEAKENED it to a hold pending an F010 commitment. PARK. [Value/Feasibility Realist -- I judge whether "feasible" is hiding a missing gold source, a runtime consumer that does not yet exist, or an unmade human ruling. I reward genuine analytical/operator value and discount ideas whose feasibility rests on something deferred (notably F016 execution and F010 contract-vs-model). I opened source-drift.md, publish_pack.py, doctor.py, consumer-data-dictionary/SKILL.md, approval-console/SKILL.md, metric-contract-store.md, and the real source-map.yaml pii: flags to ground every score.] This is the purest case of the pattern my standpoint discounts: its ENTIRE consumer is deferred F010, and metric-contract-store.md confirms nothing reads powerbi/<Model>.SemanticModel/ and that the check is a SEPARATE later feature. Eligibility is clean -- it reads nothing live, checks nothing, adds no rule, emits no score, and the illustrative TMDL fragment honors Principle III (generic, not a universal template). But a golden fixture with no reader until an uncommitted build starts is deferred value, and 'easy to write as text' must NOT inflate feasibility -- I score BOTH value and feasibility low because a deliverable whose only runtime consumer does not exist is not meaningfully feasible value, it is speculative spec-work that collides with YAGNI ('not unless explicitly requested'). PARK pending an actual F010 build commitment; the minor-tension is the YAGNI collision, not a principle breach. [DESIGN-FOUNDATION reviewer. My eligibility bar is surface-blending / F016 execution: an idea is INELIGIBLE only if it authors a PBIP/PBIR, generates DAX, invents a metric, or bakes data into a static background/theme. Critically, NONE of these eight is a design/presentation idea -- not one is tagged strengthens_layer=design-system, and the one design-adjacent sibling (the designer-lens "Design-review reading-path blueprint") was explicitly split OUT of the approver surface (#2). So my high-confidence contribution is exactly two things: (1) the four-surface eligibility screen, on which all eight pass except a close call on #5, and (2) honest, conservative deferral on the non-design majority with a low-confidence flag, per my "defer rather than distort the panel" instruction. I do NOT import the skeptic's axes (APPROVE-adjacency, YAGNI) as design ineligibility -- those are governance/scope concerns owned by other lenses; I carry the skeptic's survived_verification disposition and let his concerns land in value_score, but I score eligibility purely on surface-blending. I deliberately do not mirror the skeptic across all eight, which would make the design seat redundant in the median. Verdicts skew CONSIDER/PARK, with REJECT on the skeptic-killed #4; no ADOPT, because none of these gives me a design-merit case that would justify an ADOPT from this seat.] This is the one candidate that genuinely tests my eligibility bar, because it ships an illustrative TMDL fragment -- so I ruled it explicitly rather than reflexively. An illustrative-only fixture is NOT 'authoring a PBIP / generating DAX': Principle III makes the fragment illustrative-not-a-universal-template, the paired contracts are already-pass (metrics carried, not invented), it reads nothing live and adds no rule or score. So eligible=true, reason empty. On value it is outside my design expertise and I defer: the skeptic's YAGNI point is decisive -- its ONLY consumer is the DEFERRED F010 (confirmed nothing reads powerbi/<Model>.SemanticModel/), making it a golden fixture with no reader until an uncommitted build starts. Weakened, parked pending an F010 commitment. serves operator, strengthens_layer bi-dax.

**Panel dissent:** All reviewers agree on PARK, but feasibility scores diverge sharply (3-8): the Hard-Principle Auditor and Shipped-Duplication Auditor rate it highly feasible as a static artifact (f7-8), while the Value/Feasibility Realist scores it much lower (f3), treating its grounding in the still-deferred F010 check as a feasibility drag rather than a pure documentation exercise.

**First step:** None.

### Kit Surface Budget -- negative-space skill proposing what to RETIRE, not add

`HORIZON` - **V4 / F6** - consistency: minor-tension - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY ONLY against the named hard-stops: never_execute (B1/B3), no gate bypass, never fabricate confidence (#9), never_self_grant_approval (Principle V), and no C086-specific coupling. I presume ineligible until an idea proves it touches only reasoning/static/read-only seams, and I refuse to manufacture an ineligibility_reason where I cannot name a violated principle. I verified the bar against source: never_execute.py/live_surface_boundary.py confine live DB touches to the four live-surface modules; source-drift.md forbids a rolled-up drift score and keeps grain/PII/returns as Principle-V human seams; approval-console (F027) owns the write-back; metric-contract-store confirms F010 is a deferred void; PII flags (pii:true/false + RC4) are recorded per column; retail doctor (E7) is a shipped read-only advisory digest. Value/feasibility scores here are a triage courtesy weighed from the skeptic; my load-bearing output is the eligible bool and the named-principle reason.] Eligible: advisory-only -- it PROPOSES retirement candidates with evidence + a named decider, never self-deletes and never self-grants (Principle V holds), writes/mutates nothing, opens no connection (B1/B3), emits no score (#9), not C086-specific. It extends the doctor pattern rather than adding a gate, so no gate-bypass concern. Value is weakened by heavy overlap: verified retail doctor (E7, src/retail/doctor.py) already ships a read-only repo-wide drift digest aggregating A1 route-resolution + A3 route-coverage + SC1 status-claim honesty + a load-bearing-doc existence probe, so the orphan/stale-surface DETECTION substrate largely exists; the only net-new sliver is the decision-first 'propose retirement' framing over reader-artifact overlap. Apt creative provocation (a code-health worktree with no kit-health pruning idea) but a thin extension, not a new capability. Eligible, weakened; PARK. [SHIPPED-DUPLICATION AUDITOR -- I judge each candidate for CONSISTENCY with shipped work using the ship-status table + known history. I penalize an idea that merely RESTATES a shipped feature (via consistency=conflict/minor-tension + low value_score + PARK/REJECT), but I do NOT penalize a genuine extension that builds ON shipped work AS LONG AS it names the shipped feature it extends. I hold settled-rejected history (the F5/F6 stats-engine line) to a materially-new bar. Eligibility is the hard-principle auditor's lane, not mine -- the skeptic ruled none of these seven principle-ineligible, so I carry eligible=true / ineligibility_reason='' across the board, and I carry horizon/origin/serves/strengthens_layer/survived_verification unchanged from the candidate + skeptic blocks. I opened load-bearing evidence myself: consumer-data-dictionary/SKILL.md (F040 composes MEANING, explicitly scoped away from a trust/disclosure signal -- so the PII notice is an extension, not a duplicate); approval-console/SKILL.md (F027 WRITES the recorded decision into unresolved-questions.md + readiness-status.yaml approvals[] -- a write-loop, so the read-only Approver reorder is distinct); doctor.py (E7 aggregates A1 routes + A3 route-coverage + SC1 status-claims + a load-bearing-doc probe -- Kit Surface Budget's orphan/stale detection overlaps this heavily); publish_pack.py (PP1 checks the handoff pack's own six-section completeness -- the Publish Preflight reuses its semantics and re-treads E7's aggregator pattern); metric-contract-store.md (F010/011 contract-vs-model check is DEFERRED, 'nothing here reads powerbi/<Model>.SemanticModel/'); metric_drift.py (a DAX measure-drift checker -- NOT source-profile drift, so retail drift has no shipped duplicate); source-drift.md (F014 is a fully-specced 9-class taxonomy that has ALREADY pre-empted the H5/F5/F6 stats-engine objection: measured before/after magnitudes + status + blockers allowed, rolled-up drift SCORE forbidden, grain/identity/returns/PII are Principle-V seams it measures but never re-judges); and idea-backlog.md (H11 answerability digest was PARKed as low-value -- shadows the PII notice's value, not its eligibility; APPROVE-adjacency is a standing eligibility SPLIT the panel keeps flagged on C1/H8/J1 but is the hard-principle lane, not mine).] Detection substrate overlaps the SHIPPED retail doctor E7 (I opened doctor.py: it already aggregates A1 route resolution + A3 route-coverage bijection + SC1 status-claim honesty + a load-bearing-doc existence probe, which is exactly the orphaned-route / skill-no-route-reaches / stale-citation detection this proposes) -- so the discovery half is largely duplicative and it must name E7 as what it extends. The one net-new sliver is genuine: a decision-first 'propose retirement candidate' framing over reader-artifact OVERLAP, which E7's drift digest does not do, and it is advisory-only (proposes candidates + evidence + named decider, self-deletes nothing) exactly like the E7 pattern. Thin extension of a shipped surface, not a new capability. Skeptic WEAKENED (heavy overlap); minor-tension, CONSIDER as an E7 extension, correctly HORIZON. [Value/Feasibility Realist -- I judge whether "feasible" is hiding a missing gold source, a runtime consumer that does not yet exist, or an unmade human ruling. I reward genuine analytical/operator value and discount ideas whose feasibility rests on something deferred (notably F016 execution and F010 contract-vs-model). I opened source-drift.md, publish_pack.py, doctor.py, consumer-data-dictionary/SKILL.md, approval-console/SKILL.md, metric-contract-store.md, and the real source-map.yaml pii: flags to ground every score.] Eligible and thematically apt (a code-health worktree with no kit-health pruning idea is a fair provocation) -- advisory-only, proposes candidates + evidence + named decider, self-deletes nothing, self-grants nothing, extends the doctor pattern rather than adding a gate. But the detection substrate heavily overlaps the SHIPPED retail doctor E7, which I confirmed already aggregates route-registry resolution, route-coverage bijection, status-claim honesty, and a load-bearing-doc existence probe -- the orphaned-route / skills-no-route-reaches / stale-citation (phantom CHANGELOG, which I confirmed is absent) discovery is largely already done. The one net-new sliver is the decision-first 'propose retirement candidate' framing over reader-artifact overlap, which E7 does not do -- real but thin. Value is a modest maintainer convenience layered on an existing surface, not a new capability; feasibility is moderate but the framing does most of the novel lifting. PARK. The overlap is a value discount, not a prior_status change -- prior_status stays new. [DESIGN-FOUNDATION reviewer. My eligibility bar is surface-blending / F016 execution: an idea is INELIGIBLE only if it authors a PBIP/PBIR, generates DAX, invents a metric, or bakes data into a static background/theme. Critically, NONE of these eight is a design/presentation idea -- not one is tagged strengthens_layer=design-system, and the one design-adjacent sibling (the designer-lens "Design-review reading-path blueprint") was explicitly split OUT of the approver surface (#2). So my high-confidence contribution is exactly two things: (1) the four-surface eligibility screen, on which all eight pass except a close call on #5, and (2) honest, conservative deferral on the non-design majority with a low-confidence flag, per my "defer rather than distort the panel" instruction. I do NOT import the skeptic's axes (APPROVE-adjacency, YAGNI) as design ineligibility -- those are governance/scope concerns owned by other lenses; I carry the skeptic's survived_verification disposition and let his concerns land in value_score, but I score eligibility purely on surface-blending. I deliberately do not mirror the skeptic across all eight, which would make the design seat redundant in the median. Verdicts skew CONSIDER/PARK, with REJECT on the skeptic-killed #4; no ADOPT, because none of these gives me a design-merit case that would justify an ADOPT from this seat.] Design-eligible: advisory-only, proposes retirement candidates + evidence + a named decider, authors/removes nothing, self-grants nothing -- no surface blending. Prior_status is new (a new proposal), though I confirmed it extends the shipped retail doctor E7: I read doctor.py and it already aggregates routes (A1), route-coverage (A3), status-claims (SC1) and a load-bearing-doc existence probe -- precisely the orphaned/stale-surface detection substrate this proposes. The only net-new sliver is the decision-first 'propose retirement' framing over reader-artifact overlap, which E7 does not do. The anti-doc-bloat instinct is apt (this very worktree is code-health hotspot remediation with no kit-health pruning peer), and pruning reader surfaces is design-hygiene-adjacent, but it is a thin extension of an existing surface, not a new capability. Weakened by heavy overlap; parked. serves operator.

**Panel dissent:** Three reviewers rule PARK; the Shipped-Duplication Auditor alone dissents to CONSIDER (v4/f6), having surfaced concrete overlap with the shipped E7 doctor's orphan/stale-route detection and viewing that overlap as a reason to take the idea more seriously as a named extension rather than shelve it.

**First step:** Extend the retail doctor E7 digest (not a new surface) with an advisory 'retirement-candidate' section that cross-references route registry + status-claim manifest + doc citations to flag orphaned/overlapping/stale surfaces, outputting candidates + evidence + a named human decider; mutates/removes nothing.

## REJECT

### Silver/Gold migration authoring aid -- map->SQL skeleton composer + Phase-5 order-preservation static rule

`NOW` - **V2 / F2** - consistency: conflict - respects principles

**Why this verdict:** [HARD-PRINCIPLE AUDITOR. I judge ELIGIBILITY ONLY against the named hard-stops: never_execute (B1/B3), no gate bypass, never fabricate confidence (#9), never_self_grant_approval (Principle V), and no C086-specific coupling. I presume ineligible until an idea proves it touches only reasoning/static/read-only seams, and I refuse to manufacture an ineligibility_reason where I cannot name a violated principle. I verified the bar against source: never_execute.py/live_surface_boundary.py confine live DB touches to the four live-surface modules; source-drift.md forbids a rolled-up drift score and keeps grain/PII/returns as Principle-V human seams; approval-console (F027) owns the write-back; metric-contract-store confirms F010 is a deferred void; PII flags (pii:true/false + RC4) are recorded per column; retail doctor (E7) is a shipped read-only advisory digest. Value/feasibility scores here are a triage courtesy weighed from the skeptic; my load-bearing output is the eligible bool and the named-principle reason.] Eligibility is clean -- this is a category the skeptic KILLED on FEASIBILITY and VALUE, NOT on any named principle, so writing an ineligibility_reason here would be a category error. The skeleton composer only places what the CLEARED source-map already decided and marks everything else GAP (executes no SQL, invents no transform -- B1/YAGNI honored), and the order check is a pure static marker read reporting a named blocker (no score, #9 holds); neither self-grants nor bypasses a gate; not C086-specific. It fails on feasibility+value: verified warehouse/migrations use FREEFORM prose comments, not an ordered marker vocabulary, so the order rule has nothing to read and needs a marker convention invented+back-filled first (unstated dependency); and the retail-build-warehouse SKILL already ships the numbered Phase-5 order + skeletons and deliberately accepts an order-blind gate. eligible=true, survived_verification=killed -> REJECT. [SHIPPED-DUPLICATION AUDITOR -- I judge each candidate for CONSISTENCY with shipped work using the ship-status table + known history. I penalize an idea that merely RESTATES a shipped feature (via consistency=conflict/minor-tension + low value_score + PARK/REJECT), but I do NOT penalize a genuine extension that builds ON shipped work AS LONG AS it names the shipped feature it extends. I hold settled-rejected history (the F5/F6 stats-engine line) to a materially-new bar. Eligibility is the hard-principle auditor's lane, not mine -- the skeptic ruled none of these seven principle-ineligible, so I carry eligible=true / ineligibility_reason='' across the board, and I carry horizon/origin/serves/strengthens_layer/survived_verification unchanged from the candidate + skeptic blocks. I opened load-bearing evidence myself: consumer-data-dictionary/SKILL.md (F040 composes MEANING, explicitly scoped away from a trust/disclosure signal -- so the PII notice is an extension, not a duplicate); approval-console/SKILL.md (F027 WRITES the recorded decision into unresolved-questions.md + readiness-status.yaml approvals[] -- a write-loop, so the read-only Approver reorder is distinct); doctor.py (E7 aggregates A1 routes + A3 route-coverage + SC1 status-claims + a load-bearing-doc probe -- Kit Surface Budget's orphan/stale detection overlaps this heavily); publish_pack.py (PP1 checks the handoff pack's own six-section completeness -- the Publish Preflight reuses its semantics and re-treads E7's aggregator pattern); metric-contract-store.md (F010/011 contract-vs-model check is DEFERRED, 'nothing here reads powerbi/<Model>.SemanticModel/'); metric_drift.py (a DAX measure-drift checker -- NOT source-profile drift, so retail drift has no shipped duplicate); source-drift.md (F014 is a fully-specced 9-class taxonomy that has ALREADY pre-empted the H5/F5/F6 stats-engine objection: measured before/after magnitudes + status + blockers allowed, rolled-up drift SCORE forbidden, grain/identity/returns/PII are Principle-V seams it measures but never re-judges); and idea-backlog.md (H11 answerability digest was PARKed as low-value -- shadows the PII notice's value, not its eligibility; APPROVE-adjacency is a standing eligibility SPLIT the panel keeps flagged on C1/H8/J1 but is the hard-principle lane, not mine).] The skeptic KILLED this (killed cannot be ADOPT), and it is a conflict from the duplication seat on BOTH halves. The composer half re-treads the SHIPPED retail-build-warehouse skill, which already documents the numbered load-bearing Phase-5 order and already ships skeletons (medallion-playbook Phase 5/6 + Appendix A/B) -- a map->skeleton composer restates that shipped content. The check half is infeasible as written: real migrations (warehouse/migrations/0003_*.sql) carry freeform prose comments, NOT an ordered section-marker vocabulary, so the proposed static order rule has nothing to read and would require inventing + back-filling a marker convention first (an unstated dependency); and retail-build-warehouse explicitly accepts an order-blind gate with a manual self-review diff BY DESIGN, so the check attacks a designed-in choice. Restatement of shipped skill + infeasible check = REJECT. [Value/Feasibility Realist -- I judge whether "feasible" is hiding a missing gold source, a runtime consumer that does not yet exist, or an unmade human ruling. I reward genuine analytical/operator value and discount ideas whose feasibility rests on something deferred (notably F016 execution and F010 contract-vs-model). I opened source-drift.md, publish_pack.py, doctor.py, consumer-data-dictionary/SKILL.md, approval-console/SKILL.md, metric-contract-store.md, and the real source-map.yaml pii: flags to ground every score.] Killed by the skeptic and my grounding agrees, so per my brief this cannot be ADOPT -- REJECT. The order-check half is infeasible AS SPECIFIED: real migrations carry freeform prose comments ('-- No junk-row filter', '-- No sentinel UPDATEs'), NOT an ordered marker vocabulary, so the proposed static order rule has nothing to read and would first require inventing and back-filling a marker convention across every migration -- an unstated dependency, which is exactly the kind of hidden infeasibility my standpoint exists to catch. The skeleton-composer half re-treads shipped content: retail-build-warehouse SKILL already documents the numbered load-bearing Phase-5 order and already ships skeletons (Phase 5/6 + Appendix A/B), and it explicitly accepts an order-blind gate with a manual self-review diff BY DESIGN -- so the check half attacks a deliberately-made design choice with an unbuildable mechanism. Both halves damaged; low value, near-zero feasibility as written. [DESIGN-FOUNDATION reviewer. My eligibility bar is surface-blending / F016 execution: an idea is INELIGIBLE only if it authors a PBIP/PBIR, generates DAX, invents a metric, or bakes data into a static background/theme. Critically, NONE of these eight is a design/presentation idea -- not one is tagged strengthens_layer=design-system, and the one design-adjacent sibling (the designer-lens "Design-review reading-path blueprint") was explicitly split OUT of the approver surface (#2). So my high-confidence contribution is exactly two things: (1) the four-surface eligibility screen, on which all eight pass except a close call on #5, and (2) honest, conservative deferral on the non-design majority with a low-confidence flag, per my "defer rather than distort the panel" instruction. I do NOT import the skeptic's axes (APPROVE-adjacency, YAGNI) as design ineligibility -- those are governance/scope concerns owned by other lenses; I carry the skeptic's survived_verification disposition and let his concerns land in value_score, but I score eligibility purely on surface-blending. I deliberately do not mirror the skeptic across all eight, which would make the design seat redundant in the median. Verdicts skew CONSIDER/PARK, with REJECT on the skeptic-killed #4; no ADOPT, because none of these gives me a design-merit case that would justify an ADOPT from this seat.] Not a design idea and outside my expertise -- it authors SQL, not a PBIP/PBIR/DAX/metric/theme, so from my surface-blending bar it is eligible, NOT ineligible; I do not misclassify a SQL-authoring idea as surface-blending. On merit I defer to the SQL/architect lens, and the skeptic KILLED it on grounded evidence I have no reason to overturn: real migrations (warehouse/migrations/0003_*.sql) carry freeform prose comments, not an ordered marker vocabulary, so the Phase-5 order-preservation rule has nothing to read without first inventing+backfilling a marker convention, and the retail-build-warehouse SKILL already ships the numbered order + skeletons and deliberately accepts an order-blind gate. A killed idea cannot be ADOPT; REJECT. serves operator, strengthens_layer bi-sql.

**First step:** None.

## Rescue notes (steelman of the not-adopted)

_A reframing that MIGHT make an idea eligible later, or the irreducible reason it stays out. A reason for a human to weigh next run -- never a verdict; the gate already ruled._

- **Publish-Readiness Preflight Card -- human-read non-executing preconditions before deferred F016** -- rescue possible. Drop the F010-dependent 'every metric bound to gold not silver' precondition entirely (it reads a void feature that doesn't exist yet). Instead, explicitly scope the card as a named, opt-in EXTENSION of the shipped retail-doctor E7 digest -- not a new aggregator -- that adds exactly one net-new check E7 doesn't do today: a secret-in-tracked-file scan gated to the publish pack's file set. Everything else (pack completeness, gold-only binding presence) should literally call into E7/PP1 rather than re-implement their semantics. Narrowed seam: A single new check function contributed to the existing E7 doctor surface (secret-in-tracked-file scan scoped to files listed in the PP1 handoff pack), rather than a standalone preflight aggregator.
- **Silver/Gold migration authoring aid -- map->SQL skeleton composer + Phase-5 order-preservation static rule** -- no rescue. The order-check half has no data to read: real migrations use freeform prose comments, not a marker vocabulary, so the rule is infeasible without first inventing and back-filling a convention across every existing migration file -- a prerequisite project of its own, not a narrowing of this one. The composer half restates content the retail-build-warehouse skill already ships (numbered Phase-5 order + skeletons + an order-blind gate that was a deliberate design choice, not an oversight). Both halves attack settled, working shipped surfaces rather than filling a gap; there is no smaller slice that isn't either already-shipped or blocked on the unstated marker-convention dependency.
- **F010 contract-vs-model fixture shape -- author the deferred check's spec as a static artifact today** -- rescue possible. Do not build the fixture speculatively. Instead, fold the illustrative TMDL fragment into the F010 feature's own future spec/plan doc as an inline example when (and only when) someone actually opens a plan to build F010 -- i.e., treat it as a paragraph to add to that future plan.md, not a standalone artifact shipped today with zero readers. Narrowed seam: A documentation note attached to the existing F010 deferred-feature entry (e.g. a comment in metric-contract-store.md pointing to where an illustrative fixture would go), rather than a freestanding fixture file.
- **Kit Surface Budget -- negative-space skill proposing what to RETIRE, not add** -- rescue possible. Do not build a new aggregator. Instead, add a single new output section to the existing shipped retail-doctor E7 digest -- a 'retirement candidates' list -- that reuses E7's already-computed route/coverage/status-claim/doc-existence signals and reframes items E7 already flags as stale/orphaned into a decision-first proposal (evidence + a named human decider), rather than just a passive drift report. Narrowed seam: One additive output block in doctor.py's existing E7 aggregation (a 'proposed retirements' section derived from signals E7 already computes), explicitly named as an extension of E7 rather than a new skill or surface.

## SHIPPED / SETTLED (prior ideas, for the record)

- **A1 Machine-Checkable Route Registry + Planned/Built Status Manifest (appendix)** -- SHIPPED. shipped-ideas.yaml A1: shipped -- PR #62 abbbd73; Ground row '67-rule-static-gate' family / routes.py @register A1
- **A1 Token->Theme Compile Reconciler (CONSIDER heading, design)** -- SHIPPED. shipped-ideas.yaml DL3: shipped -- PR #146 (design arc; snapshot A1) @register DL3 src/retail/rules/design_theme_fidelity.py -- token->theme fidelity
- **A2 Theme JSON Token-Contract / Four-Surface Purity Linter** -- SHIPPED. shipped-ideas.yaml DL1: shipped -- PR #119 (spec 060 theme-json-purity-linter; snapshot A2) src/retail/rules/design_theme.py @register DL1
- **A3 Route-Registry Coverage Reconciler (appendix)** -- SHIPPED. shipped-ideas.yaml A3: shipped -- PR #95 (spec 047 route-registry-coverage-reconciler)
- **A3 WCAG Contrast Pre-Check (from static hex tokens) (CONSIDER heading, design)** -- SHIPPED. shipped-ideas.yaml CT1: shipped -- PR #146 (design arc; snapshot A3) @register CT1 src/retail/rules/design_contrast.py -- WCAG contrast pre-check
- **A7 Grid Arithmetic-Closure Self-Check** -- SHIPPED. shipped-ideas.yaml DL5: shipped -- PR #147 (snapshot A7) @register DL5 src/retail/rules/design_grid_closure.py -- grid arithmetic-closure self-check
- **A8 background-spec forbidden_dynamic_content Assertion Rule** -- SHIPPED. shipped-ideas.yaml DL2: shipped -- PR #124 (spec 064 background-spec-purity; snapshot A8) src/retail/rules/design_background.py @register DL2
- **A9 Anti-Pattern Self-Check Harvester** -- SHIPPED. shipped-ideas.yaml A9: shipped -- PR #180 0bb0e22 @register DL6 src/retail/rules/design_visual_selfcheck.py -- visual-spec anti-pattern self-check
- **ADL Per-Contract Ambiguity Decision Ledger** -- SHIPPED. shipped-ideas.yaml ADL: shipped -- PR #106 (spec 058 per-contract-ambiguity-decision-ledger; ambiguities[] block on metric-contract.yaml)
- **AL1 Assumption Ledger Rule (AL1)** -- SHIPPED. shipped-ideas.yaml AL1: shipped -- PR #107 (spec 059 assumption-ledger-rule) @register AL1; Ground row '67-rule-static-gate'
- **B1 Never-Execute Invariant Guard (NX rule family) (appendix, MIS-MAPPED)** -- SHIPPED. Ground row 'never-execute invariant (B1/B3)' SHIPPED -- src/retail/rules/never_execute.py, live_surface_boundary.py (the appendix's own PR #64 citation for this bullet was corrected/disowned by the ledger's B1 entry as a mis-mapping; do not cite PR #64 for this)
- **B1 Anti-Pattern Parity Rule (workflow<->prose thirteen-in-lockstep) (CONSIDER heading, design; = ledger's true B1)** -- SHIPPED. shipped-ideas.yaml B1: shipped -- PR #181 a136f47 (+ #183 16f2bac fix) @register AP1 src/retail/rules/rule_ap1.py -- visual-qa <-> dashboard-qa anti-pattern parity; ledger explicitly corrects the prior PR #64 mis-mapping
- **B2 Gate Observability Ledger - Structured Findings Emission (JSON/SARIF)** -- SHIPPED. shipped-ideas.yaml B2: shipped -- PR #63 ca1431c (json only; SARIF half not shipped); appendix: src/retail/runner.py run_json + cli.py --format
- **B3 Live-Surface Import Boundary Guard (B3)** -- SHIPPED. shipped-ideas.yaml B3: shipped -- PR #96 (spec 048 live-surface-import-boundary-guard); Ground row 'never-execute invariant (B1/B3)' SHIPPED src/retail/rules/live_surface_boundary.py
- **C1 Design-Review Evidence Schema + Static Completeness Gate (all C1 headings, ADOPT+CONSIDER, deduped -- same idea repeated 3x by different reviewer lenses)** -- SHIPPED. shipped-ideas.yaml DL4: shipped -- PR #146 (design arc; snapshot C1) @register DL4 src/retail/rules/design_review_evidence.py + templates/design-review-evidence.md
- **D1 Confusable-Design-Skill + Path Foot-Gun + Two-System Boundary Guard** -- SHIPPED. shipped-ideas.yaml D1: shipped -- PR #158 c98f5a3 (snapshot D1) @register DR1 src/retail/rules/design_routes.py -- design-layer route path foot-gun + stale-phrase guard
- **D2 Adopt orphaned validate_cards.py into the governed check surface (all D2 headings deduped, ADOPT+CONSIDER repeats)** -- SHIPPED. shipped-ideas.yaml D2: shipped -- PR #148 (snapshot D2) tests/unit/test_design_cards.py + docs/quality/design-cards-count.yaml
- **DF1 Parked-on dependency map / Parked-On Map (F016 bottleneck edge)** -- SHIPPED. shipped-ideas.yaml DF1: shipped -- PR #99 (spec 051 parked-on-map); ship_status 'SC1 status-claims + DF1 parked-on drift manifests' SHIPPED docs/quality/parked-on.yaml
- **DQ1 DQ-Signal Interpretation Note** -- SHIPPED. shipped-ideas.yaml DQ1: shipped -- PR #102 (spec 054 dq-signal-interpretation-note; templates/handoff/dq-signal-interpretation.md)
- **E1 5-Place Wiring Meta-Gate / Registry Lockstep Self-Check** -- SHIPPED. shipped-ideas.yaml E1: shipped -- PR #121 (spec 061 wiring-meta-gate) tests/unit/test_wiring_meta_gate.py
- **E2 Generated Wiring-Truth / Golden-Record Single-Source Collapse** -- SHIPPED. shipped-ideas.yaml E2: shipped -- PR #161 c7f26ef (snapshot E2, shrunk) test: glossary rule-table <-> live-registry bijection
- **E3 Never-Execute Self-Audit (prove B1/B3 cover every rule module)** -- SHIPPED. shipped-ideas.yaml E3: shipped -- never_execute.py governs rule modules by the src/retail/rules/ PREFIX over git ls-files; tests/unit/test_never_execute.py globs every rule module (2026-07-07 residue-audit reconciliation)
- **E4 Design-Lint Adversarial Fixture Corpus + Golden-Findings Lock** -- SETTLED (rejected). shipped-ideas.yaml E4: settled -- 'DROPPED 2026-07-02 -- duplicate/superseded: DL1 (#119) + DL2 (#124) already ship the adversarial good/bad fixture corpora + fail-closed exact-locator/severity/count tests; E4's headline red/green spine is already delivered.'
- **E6 retail scaffold-rule / new-rule Authoring Generator + Doctor** -- SHIPPED. shipped-ideas.yaml E6: shipped -- PR #122 (spec 062 scaffold-rule-generator) src/retail/scaffold.py + cli.py `scaffold`/--doctor; Ground row 'retail scaffold --doctor' SHIPPED
- **E7 retail doctor -- Proactive Lockstep-Drift Diagnostician (read, never fix)** -- SHIPPED. shipped-ideas.yaml E7: shipped -- PR #160 6cbc099 (snapshot E7) src/retail/doctor.py + `retail doctor`
- **F1 Navigation Regression Harness** -- SHIPPED. shipped-ideas.yaml F1: shipped -- PR #159 ba76441 (snapshot F1) navigation regression harness for the COMPASS fast-routing table
- **F4 Rule-Count Claim Reconciler (SC2)** -- SHIPPED. shipped-ideas.yaml SC2: shipped -- PR #125 (spec 065 rule-count-reconciler; snapshot F4) @register SC2 src/retail/rules/rule_count_claims.py
- **F5 Demand-Signal Forecast Pack (sell-through + reorder exception)** -- SETTLED (rejected). shipped-ideas.yaml F5: settled -- idea-backlog.md PARK, ML/forecasting out-of-scope boundary
- **F6 Anomaly-Baseline Contracts (statistical exception flags)** -- SETTLED (rejected). shipped-ideas.yaml F6: settled -- idea-backlog.md REJECT, violates ML/statistical out-of-scope hard principle
- **F7 Decision-Question Index per KPI Domain** -- SHIPPED. shipped-ideas.yaml F7: shipped -- PR #65 ae471aa; appendix: skills/retail-kpi-knowledge/domains/*.md (11 domain files)
- **F8 Per-Table KPI Coverage Scorecard** -- SHIPPED. shipped-ideas.yaml F8: shipped -- PR #66 9d782f8; appendix: skills/retail-kpi-knowledge/references/kpi-coverage-scorecard-template.md
- **FHC First-Hour Compass / New-Table Author Onboarding Cockpit** -- SHIPPED. shipped-ideas.yaml FHC: shipped -- PR #103 (spec 055 first-hour-compass; templates/first-hour-compass.md + skill + tools doc)
- **G1 Design-Foundation Idea Lane + Backlog Seed** -- SHIPPED. shipped-ideas.yaml G1: shipped -- PR #126 (spec 066 design-foundation-idea-lane; snapshot G1) idea-engine design lane render + ledger shape reuse
- **H1 Additivity-Consistency Lineage Rule** -- SHIPPED. shipped-ideas.yaml H1: shipped -- PR #133 (spec 068 additivity-consistency-rule) @register AD1 src/retail/rules/additivity_consistency.py; registry 45->46
- **H2 Cross-Contract Assumption-Coherence Rule** -- SHIPPED. shipped-ideas.yaml H2: shipped -- PR #129 cc606b8 (@register AL2 src/retail/rules/assumption_coherence.py; hand-built, no committed spec, provenance debt per C5)
- **H3 Decision-Question Answerability Reconciler** -- SHIPPED. shipped-ideas.yaml H3: shipped -- PR #134 (spec 069 answerability-reconciler) @register AQ1 src/retail/rules/answerability_reconciler.py; registry 46->47
- **H4 Contract-Sufficiency Card** -- SHIPPED. shipped-ideas.yaml H4: shipped -- PR #157 f7e78ad (snapshot H4) templates/kpi-sufficiency-card.md; owner ruled 'separate template' not folded into F8 (this ledger entry post-dates and supersedes the backlog's own stale inline comment claiming H4 'gets no key')
- **H6 Comparison-Baseline Declaration Guard** -- SHIPPED. shipped-ideas.yaml H6: shipped -- 2026-07-07 @register CB1 src/retail/rules/comparison_baseline.py + tests/unit/test_comparison_baseline.py
- **I1 Seed-Layer Route Honesty Rule** -- SHIPPED. shipped-ideas.yaml I1: shipped -- PR #132 661800e (spec 067-seed-route-honesty-rule; snapshot I1) extends shipped A1 route rule with a 'seed' status state
- **I2 Land bi-python's Planned Cleaning Artifacts** -- SHIPPED. shipped-ideas.yaml I2: shipped -- PR #127 a5da4a1 + PR #157 (spec 067-bi-python-cleaning-artifacts; snapshot I2) bi-python cleaning-review checklist + groupby-aggregation-grain knowledge
- **I3 Shared-Checklist Fork Detector** -- SHIPPED. shipped-ideas.yaml I3: shipped -- PR #182 a5d6f21 @register SF1 src/retail/rules/rule_sf1.py -- cross-layer checklist fork detector (spec 086); registry -> 55
- **IL1 Idea-Bank Memory Seam / Shipped-row->roadmap-F-row hard link (IL1)** -- SHIPPED. appendix cites: 'docs/roadmap/shipped-ideas.yaml exists on disk (the IL1 memory-seam artifact itself...) -- inferred from artifact presence, not from a ledger key or ship_status row'
- **J1 Approval Evidence Pack for the Named-Human Stage Gate** -- SHIPPED. shipped-ideas.yaml J1: shipped -- PR #123 (spec 063 approval-evidence-pack, roadmap F035) .claude/skills/approval-evidence-pack/SKILL.md + templates/approval-evidence-pack.md; Ground row F035-F037,F039 SHIPPED
- **LVR Live-validation evidence recorder (validate.py Findings -> readiness-status block)** -- SHIPPED. shipped-ideas.yaml LVR: shipped -- PR #105 (spec 057 live-validation-evidence-recorder; src/retail/readiness_evidence.py)
- **PP1 Publish-pack completeness gate (PP1)** -- SHIPPED. shipped-ideas.yaml PP1: shipped -- PR #97 (spec 049 publish-pack-completeness-gate) @register PP1
- **S7A Stage 7 Answerability Summary (executive-readable)** -- SHIPPED. shipped-ideas.yaml S7A: shipped -- PR #101 (spec 053 stage-7-answerability-summary; templates/handoff/answerability-summary.md)
- **SC1 Stale-Marker Sweep / Status-Claim Reconciler (SC1)** -- SHIPPED. shipped-ideas.yaml SC1: shipped -- PR #98 (spec 050 stale-marker-sweep); ship_status 'SC1 status-claims...' SHIPPED docs/quality/status-claims.yaml
- **SL1 Coverage Scorecard Linter** -- SHIPPED. shipped-ideas.yaml SL1: shipped -- PR #104 (spec 056 coverage-scorecard-linter) @register SL1
- **rule-registry-snapshot-manifest Rule Registry Snapshot Manifest (golden-file rule inventory)** -- SHIPPED. ship_status 'Rule-count lockstep (manifest + severity-posture + glossary)' SHIPPED -- docs/rules/rules-manifest.json (41); src/retail/manifest.py
- **severity-posture-regression-lock Severity-Posture Regression Lock (golden severity table)** -- SHIPPED. ship_status 'Rule-count lockstep (manifest + severity-posture + glossary)' SHIPPED -- docs/rules/severity-posture.json (41); src/retail/severity_posture.py

## Run health & self-metrics

- Yield: 107 raw -> 7 scored (ADOPT 0, CONSIDER 3, PARK 3, REJECT 1).
- Eligibility-rejection rate: 0%.
- Consistency: 0 consistent, 6 minor-tension, 1 conflict.
- Verification: 2 survived, 4 weakened, 1 killed.
- Layer coverage: docs-spine 2, none 3, bi-sql 1, bi-dax 1
- Who it serves (meta-bloat signal): end-user 2, operator 5, tool-internal 0. A run heavy on tool-internal is over-producing self-checking; prefer ideas that reach the end user or the operator.
- Run health: all lenses reported.
