# Seshat BI -- v0.4.1 patch release candidate

This patch candidate records the changes merged after the frozen v0.4.0 release note. It does not rewrite the historical claims captured in `docs/releases/v0.4.md`.

## What this patch adds

- **Activated the governed `dagster-dbt` engine seam** (spec 135, PR #307): selected silver and gold layers can route through the governed `seshat.dbt` control path while migrations remain the default, parity oracle, and rollback path.
- **Added dependency co-resolution and freshness governance** (spec 136, PR #308): CI now resolves declared environments and cross-products fail-closed, with a separate advisory freshness report.
- **Enabled GitHub Sponsors and refreshed repository presentation** (PRs #309 and #313).
- **Updated pinned GitHub Actions** for checkout and Python setup (PRs #310 and #311).
- **Reconciled post-v0.4.0 documentation and Dependabot behavior** (PR #314), including disabling unintended automatic labels with `labels: []`.

## Honest boundaries

- The dbt engine path remains a governed rehearsal into isolated shadow schemas and records `warehouse_updated: false`.
- Live dbt compile, build, test, and parity evidence still require a named live profile and remain `[PENDING LIVE PROFILE]`.
- Dagster schedules and sensors remain STOPPED until a named human starts them.
- This candidate does not itself create a tag, publish to PyPI, create a GitHub Release, or submit to public Claude, Codex, or OpenAI catalogs.
