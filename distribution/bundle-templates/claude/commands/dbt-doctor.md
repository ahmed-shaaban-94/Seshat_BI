---
description: Check and explain governed dbt runtime prerequisites
---

Load the `dbt-workflows` skill. Run `seshat dbt doctor --format json` and
explain its sanitized blockers. This route is read-only: do not connect to a
database, edit the profile, or simulate a successful check.
