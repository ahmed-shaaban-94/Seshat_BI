<!--
=============================================================================
 release-notes.md  --  the copy-me per-RELEASE note (the seven required blocks)
=============================================================================
 Seshat BI Agent Kit  -  feature F033 (Release & Maturity Management).
 On-disk spec: specs/027-release-maturity-management/  (dir 027 == roadmap F033;
 when the dir number and the F-number disagree, the roadmap F-number wins).
 See: .claude/skills/release-notes-generator/SKILL.md (the draft-and-assess verb),
      templates/maturity-report.md (the SEPARATE point-in-time ladder snapshot --
      a distinct artifact; never merge the two),
      docs/architecture/product-modules.md (F024: this skill is Maintenance
      Automation -- it drafts derived evidence, creates no truth, grants no approval).

 WHAT THIS IS
   A GENERIC, copy-me per-release note. One "release" == one shipped roadmap F-slice,
   keyed by its roadmap F-number. The note maps 1:1 to the delivered-ledger row it
   summarizes. A FILLED copy lives under docs/releases/<F-number>/release-notes.md
   (e.g. docs/releases/F033/release-notes.md). A batch (e.g. F024-F033) is recorded as
   a GROUP of per-slice notes, NEVER one merged note. Git tags / version numbers /
   package publishes are out of scope -- release execution stays a human action.

 NO UNBACKED CLAIM  (FR-008 -- the load-bearing rule)
   A "what became possible" / "production ready" / "GA" / "enterprise grade" claim is
   permitted ONLY when a committed evidence rung backs it and is cited. With no backing
   rung the claim is forbidden. An unsupported capability is OMITTED from "what became
   possible" (and, if relevant, listed under "known limitations -- not yet evidenced"),
   never asserted. Every "became possible" line MUST cite the committed source
   (file/commit, or the cited F028 evidence-pack entry) that makes it true; a claim with
   no traceable source is a defect (FR-011).

 CONSUME, NEVER RE-MEASURE  (FR-009)
   This note COMPOSES already-committed evidence. The generator READS the F028 evidence
   pack (-> "what became possible"), the F032 compatibility matrix (-> "what changed" +
   "migration notes"), and the roadmap ledger + commit refs. It runs NO seshat check /
   retail validate, profiles NO source, opens NO DB connection, reads NO powerbi/. A
   missing input is recorded "evidence not available", never fabricated.

 NO FAKE CONFIDENCE  (hard rule #9)
   No percentage, no 0-100 health number, no average, no number that reads as confidence
   anywhere in a filled copy. Maturity is reported in the SEPARATE maturity-report.md as
   evidence-gated milestone rungs (achieved / not achieved), never a score.

 HOW TO USE
   Copy into docs/releases/<F-number>/, fill every <ANGLE-BRACKET> field, delete this
   banner, keep it committed. GENERIC -- no C086 / retail_store_sales values baked in
   (Principle VII; the worked tables may be CITED as evidence in a filled instance, never
   inlined into this blank template). ASCII + UTF-8 no BOM; '--' and '->' only; short
   repo-relative paths (Windows MAX_PATH).
=============================================================================
-->

# Release Notes -- <RELEASE F-NUMBER> <release short title>

- **Roadmap feature (this release):** `<Fxxx>`  **On-disk spec:** `<specs/0NN-...>`
- **Maps to delivered-ledger row:** `<the docs/roadmap/roadmap.md row this 1:1 summarizes>`
- **Generated by:** `release-notes-generator` (F033, Maintenance Automation -- drafts
  derived evidence; creates no truth, grants no approval)
- **Status:** `draft -- awaiting release-owner approval`  *(draft | approved -- only a named release owner moves this to approved)*
- **Approvals:** `[]`  *(for approved: [{ owner: <named release owner>, at: <YYYY-MM-DD> }])*

> Status is `draft` until a NAMED release owner approves. The generator never
> self-approves a release, self-confirms a level, or publishes (Core Authority /
> Principle V).

## 1. What became possible

Each line states a NEW capability this release made possible AND cites the committed
evidence behind it. A capability with no backing evidence rung does NOT appear here.

- `<capability that became possible>` -- evidence: `<file/commit or F028 evidence-pack entry>`
- `<...>` -- evidence: `<...>`
- *(if a capability is requested but unsupported: omit it here; record it in block 5 as
  "not yet evidenced" -- never assert it)*

## 2. What changed

What moved at the version / interoperability level this release. CITE the F032
compatibility matrix rows and the commit refs; never recompute the matrix.

- `<changed dependency / adapter / assumption>` -- source: `<F032 matrix row / commit ref>`
- `<... or: evidence not available -- F032 compatibility matrix not yet on disk>`

## 3. Readiness stages affected

The per-table readiness-spine deltas this release produced, if any. Release & maturity
is product-level and advances NO stage itself -- this block RECORDS deltas other tools
made; it never moves a stage to `pass`.

- `<stage affected>` for `<table>` -- evidence: `<readiness-status.yaml delta + committed support>`
- `<... or: none -- this release advanced no per-table readiness stage>`

## 4. New modules / adapters

Each new surface shipped this release + its F024 authority category (and sub-axis, if
the category has one -- Module pins a capability level, Adapter a connectivity level;
Maintenance Automation and the other two have none).

- `<new surface>` -- category: `<Core Authority | Official Workflow Skill | Product Module (<level>) | Execution Adapter (<level>) | Maintenance Automation>` -- spec: `<specs/0NN-...>`
- `<...>`

## 5. Known limitations

What is NOT yet possible / NOT yet evidenced. This block MUST list at least the unbuilt
maturity rungs (see maturity-report.md). Be explicit; never round a gap away.

- Unbuilt maturity rungs: `<e.g. L4 dbt transformation adapter, L5 Dagster orchestration, L6 official Power BI execution adapter -- each not built, missing artifact named>`
- Not-yet-evidenced capability (requested but unbacked): `<capability + why no rung backs it yet>`
- Recorded caveats: `<e.g. L3 repeatable silver/gold is proven for the worked tables; generic repeatability beyond them is the next evidence (forward scope-note, not an unmet gate)>`

## 6. Migration notes

What a consumer / maintainer must do to move onto this release. CITE the F032
compatibility matrix (supported version ranges + required smoke tests); never recompute it.

- `<migration step>` -- source: `<F032 matrix row (supported version range / required smoke test)>`
- `<... or: evidence not available -- F032 compatibility matrix not yet on disk; no migration claim asserted>`

## 7. Next best slice

The recommended next undelivered roadmap slice. RECOMMEND; do not commit -- sequencing is
the release owner's call.

- `<next roadmap row + one-line why>` -- source: `<docs/roadmap/roadmap.md next undelivered row>`

## Conflicts surfaced (if any)

When inputs disagree (e.g. the F032 matrix asserts an adapter version interoperates but
no adapter exists in-repo), the conflict is RECORDED here as a finding and left for the
human to resolve -- the generator never picks a side (Principle V).

- `<conflicting inputs + the two sources>` -- *(unresolved; release owner decides)*
- `<... or: none>`

## See also

- The draft-and-assess verb: `../../.claude/skills/release-notes-generator/SKILL.md`.
- The SEPARATE point-in-time ladder snapshot: `maturity-report.md` (a distinct artifact;
  never merged with this note).
- The consumed inputs (by id + role): `../../docs/tools/evidence-pack-generator.md` (F028),
  the F032 compatibility matrix (`../../specs/026-adapter-compatibility-matrix/spec.md`).
- The release history of record: `../../docs/roadmap/roadmap.md` (delivered ledger).
- The authority contract (this skill is Maintenance Automation): `../../docs/architecture/product-modules.md`.
- The spec: `../../specs/027-release-maturity-management/spec.md`.
