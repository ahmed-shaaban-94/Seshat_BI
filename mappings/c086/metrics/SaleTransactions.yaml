# Metric contract -- SaleTransactions (filled instance for C086 El Ezaby pharmacy)
# F009 define-layer. PROPOSAL: status not_started until the metric owner approves.
# formula_intent is plain-language INTENT, NOT DAX. binds_to is gold-only.


name: "SaleTransactions"
grain: "a distinct count valid at any rollup; NOT additive."
formula_intent: >-
  Distinct invoices that contain at least one non-return line; all-return baskets
  excluded. VERIFY LIVE -- no independent ground-truth anchor (at most 102,579).
owner: "Pharmacy Sales Analytics lead"
binds_to:
  gold_table: "gold.fct_sales"
  columns:
    - "invoice"
    - "is_return"
  pii_sensitive: false

readiness:
  status: "not_started"
  evidence: []
  blocking_reasons: []
