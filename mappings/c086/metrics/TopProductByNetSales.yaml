# Metric contract -- TopProductByNetSales (filled instance for C086 El Ezaby pharmacy)
# F009 define-layer. PROPOSAL: status not_started until the metric owner approves.
# formula_intent is plain-language INTENT, NOT DAX. binds_to is gold-only.


name: "TopProductByNetSales"
grain: "a scalar text label valid at any rollup; NOT a number."
formula_intent: >-
  The NAME of the single best-selling product by net sales in the current filter.
  Returns a text label, not a number. VERIFY LIVE -- do not fabricate a name.
owner: "Pharmacy Sales Analytics lead"
binds_to:
  gold_table: "gold.fct_sales"
  columns:
    - "gross_sales"
    - "gold.dim_product.product_sk"
    - "gold.dim_product.product_name"
  pii_sensitive: false

readiness:
  status: "not_started"
  evidence: []
  blocking_reasons: []
