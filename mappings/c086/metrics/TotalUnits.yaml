# Metric contract -- TotalUnits (filled instance for C086 El Ezaby pharmacy)
# F009 define-layer. PROPOSAL: status not_started until the metric owner approves.
# formula_intent is plain-language INTENT, NOT DAX. binds_to is gold-only.


name: "TotalUnits"
grain: "additive at invoice-line grain."
formula_intent: >-
  Units sold net of returned units. Quantity is signed (returns negative), so a
  plain sum nets returned units. Fractional values valid (part-packs). Full model = ~286.1K units (exact figure held outside git; masked 2026-07-02).
owner: "Pharmacy Sales Analytics lead"
binds_to:
  gold_table: "gold.fct_sales"
  columns:
    - "quantity"
  pii_sensitive: false

readiness:
  status: "not_started"
  evidence: []
  blocking_reasons: []
