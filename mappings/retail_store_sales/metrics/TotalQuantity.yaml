# Metric contract -- TotalQuantity (filled instance for retail_store_sales)
# F009 define-layer. PROPOSAL: status not_started until the metric owner approves.

name: "TotalQuantity"
grain: "valid at fct_sales_rss transaction grain; additive across all dimensions and over time"
formula_intent: "The total number of units sold across all retail transactions -- the sum of the per-transaction quantity. Rows with a missing quantity (604, ~4.8%) contribute nothing (NULL ignored), not zero."
owner: "data_owner"   # approved 2026-06-25 (see readiness evidence + readiness-status approvals[])
binds_to:
  gold_table: "gold.fct_sales_rss"
  columns:
    - "quantity"
  pii_sensitive: false

# Machine-readable base-measure definition (L3 contract<->DAX drift check).
definition:
  kind: base
  aggregation: sum
  filter: []

readiness:
  status: "pass"
  evidence:
    - "approved by data_owner on 2026-06-25"
    - "gold.fct_sales_rss built + live-validated 2026-06-25 (penny-exact quantity sum = 66,276)"
  blocking_reasons: []
