# PBIP adoption fixtures

The symlink-escape case is created by its safety test at runtime because Git and
Windows checkout support do not preserve a portable linked-directory fixture.
All fixture content is generic and contains no real host, credential, PII, or
worked-example-specific business assumption.
