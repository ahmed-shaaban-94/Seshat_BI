source_kind: db-schema
source_identity: restricted
tables:
  - table_id: restricted.visible_table
    columns: {id: bigint}
    unavailable:
      item: declared relationships
      reason: metadata permission denied
